### Fixed

- `just review exchange` and `just land` now resolve the issue-named worktree before reading or publishing a candidate, and refuse a caller in another worktree instead of acting on its `HEAD`.
