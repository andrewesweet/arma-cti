### Fixed

- The changelog gate now rejects a header-only fragment — one holding category headers with no entry under any of them — so a source change can no longer land with a fragment that carries no entry.
