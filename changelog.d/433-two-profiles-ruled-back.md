### Added

- **Registered `zai-glm53-high` and `opus-medium` in the dispatch profile registry (#433, human ruling 2026-08-19).** Both gaps were met by #432's codex-absence substitution table: its Luna row asks for GLM 5.3 at High and its Terra row for Opus at medium, and neither name existed. The zai lane's effort measurement survives in the registry comment, which now records that the ruling overruled the one-name-per-model conclusion: the endpoint's ignoring of `thinking.budget_tokens` was measured and stands, but the step from it to "all five effort levels are one configuration" was an assertion about the runner — the installed 2.1.235 sends the effort level as its own request field, `output_config.effort`, and takes the thinking budget from the model's upper limit, not the effort level — so whether the two GLM 5.3 names differ on that lane is unmeasured, and they follow the ruling's table rather than a measured distinction.
