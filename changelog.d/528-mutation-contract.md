### Added

- `just mutation --rules` prints the mutation-smoke contract from the enforcing
  constants, including separate Python and shell floors, caps, budgets, ratchet
  slack, timeout bounds, seed settings, and reasoned exemptions. It points at
  CLAUDE.md's failure-class table, labels its explanatory narration, exits zero,
  and does not run or write the smoke (#528).
