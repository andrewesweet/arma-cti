# Fixed

- The `just review` command-table row attributes the remote-SHA verification to the `exchange_outside_issue_worktree` refusal instead of to the push it belongs to, leaving the success path's guarantee unstated.
