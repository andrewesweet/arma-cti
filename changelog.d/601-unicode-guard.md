### Security

- Add a deny-by-default `check-unicode` gate for invisible, bidirectional, tag and unallowlisted confusable characters in gated paths and repository Markdown. The vendored `docs/reference/arma-wiki/` injection channel remains explicitly excluded pending the principal's scope decision.
