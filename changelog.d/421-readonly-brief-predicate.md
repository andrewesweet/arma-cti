### Fixed

- **One predicate — the seat's forced permission mode — now governs both dispatch briefs, so a read-only `recon` dispatch is no longer handed gate, commit and landing instructions (#421).** The composed brief branched its gate, flake and landing sections on the registry's `reviews` column while the dispatcher's default brief branched its gate line on the forced `permission_mode`: two predicates over one question, with a test pinning each and both green. `recon` — the seat #407 forced read-only — fell between them and received the implementer's asks verbatim. `tools/brief.py`'s `Seat.judgement_only` derives from the same registry column the default brief reads and #339 derived queue surfaces from, the three sections follow it, and within that arm only the reviewer is handed the paste contract (`recon` gets the same prohibitions phrased for a seat that reads rather than judges). The two tests that pinned contradictory predicates now assert the same rule. A review brief also no longer commands `just worktree add`/`just worktree done` — both calls write, dispatch verifies the tree before launch, and any forced-read-only seat gets the assignment named with the commands forbidden instead. The paste contract the review reads now requires the gate rungs **with their result counts** and the sampled-or-exhaustive classification **unconditionally** rather than only where a kill rate is quoted — the brief that carried #353's own review demonstrated the gap by arriving with neither — and the dispatcher's thin brief states the prohibition it means ("run no gate and no tests", reading named as the work) instead of "rather than executing anything", which read as forbidding the reading a `recon` dispatch exists to do.
