### Changed

- The `AGENTS.md` command table gains a `just check-observatory` row beside the other check recipes, narrows the `just gated-paths` row's delegated-route sentence to #548's behaviour (a delegated ADR authorises only itself, and every other gated path in the diff still owes its own approval or its own marker), adds `report` to the `just queue` verb list with its terminus prompts (#553), and updates the `just watch-report` row to name the turn-top reads it folds together. The row content was approved by the human on 2026-08-24, with the `check-observatory` boundary wording corrected to match the landed code as #493's round-2 review settled it.
