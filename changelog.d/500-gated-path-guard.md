### Fixed

- `just check`'s gated-path leg now refuses a path-identifiable human sign-off change unless the current diff carries a changed ADR with an exact `Delegated-decision: yes` line in the field block parsed by `check_adr_form.py`, or the gate finds a versioned, issue-bound record for that path's exact change. A text approval survives a same-path edit on a descendant baseline only when its normalised diff still names the same hunk and an exact three-way replay of the recorded approved result produces the current bytes: line offsets and text-file blob IDs do not bind, while another branch-side hunk, an altered hunk or anchor, a path or mode change, a non-descendant baseline, and every binary or non-regular same-path baseline change require fresh approval. Rename sources and destinations are both checked. The hook, check leg, retained trial audit, ADR path, and ADR-0013 marker read one catalogue; the Claude hook remains registered and keeps its immediate generated-file and acceptance-spec refusals. Every check verdict states that it does not judge content or quality, prove human identity, or recognise semantic gates. (#500)
