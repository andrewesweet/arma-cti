### Changed

- `tests/unit/test_client_lock.py` now measures `spike/client-lock.sh` instead of exempting the module, after its fixed test transitions were replaced with condition-driven synchronization (#456).
