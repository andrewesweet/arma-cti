### Fixed

- Added `just observatory backfill` to reconcile historical landed issues from exact `just land` audit SHAs, dispatch windows, and tracker state, recording produced relations or reasoned unrecoverable markers without writing the projection.
- Added landing-source fields and journal-coverage reporting so recovered, fallback-derived, and unresolved landing provenance remain distinguishable.
