### Changed

- The `AGENTS.md` command table now documents `just gated-paths` in its own row beside the other check recipes, names `gated-path check` in the `just check` row's leg list, and carries a paragraph on the `just dispatch` row describing the Codex instruction-delivery preflight (#502). Both row texts were approved by the human on 2026-08-23 and land verbatim.
