### Changed

- The `fast` gate-clock anchor is re-set to 187.96 seconds from the ten comparable green runs documented in `tools/gate-clock-anchor.json`, with a fresh timestamp that bounds future comparisons to post-set rows.
- The derivation records each selected run's wall time, load, foreign gate-process count, and collected test count, names the laptop box the runs were taken on, and states the inability to separate suite growth from box load in this observational sample.
