# Fixed

- `just brief`'s module docstring now enumerates every invariant-half section `compose()` emits — adding the handoff in both its carried and its unavailable state, the review seat's implementer gate report, the escalation, the single-shot contract and reserved surfaces to the list that named only seven (#357). No behaviour change.
