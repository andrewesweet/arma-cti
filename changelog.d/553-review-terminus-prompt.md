### Changed

- `just queue report`, including the turn-top `just watch-report` read, now lists every
  readable review loop whose separate terminus closeout has not reached `landing.json` and
  omits completed loops; it does not make terminus a landing prerequisite.
