### Added

- `.claude/commands/orchestrator-tick.md` is now tracked, so a fresh clone carries the
  `/orchestrator-tick` command and the orchestrator's worktree no longer reports it as an
  untracked file. The file's content is unchanged by this commit; where its text disagrees
  with the tooling it names, that is reported on the issue rather than edited here (#468).
