### Fixed

- Review gate-report transport now describes a clean thread with no marked comment as marker
  absence, requires the shared marker in default implementer briefs, and exposes truncation when
  a carried report exceeds its transport bound (#655).
