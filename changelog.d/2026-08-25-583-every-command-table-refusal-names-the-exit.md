### Fixed

- Every command-table refusal — unresolved recipe and unreadable table included, not only the confinement escape, and from the standalone `just check-command-table` as well as the gate route — now names the direct approval command with the live content_id where one is computable, so a human meeting any of them can unblock without prior knowledge.
