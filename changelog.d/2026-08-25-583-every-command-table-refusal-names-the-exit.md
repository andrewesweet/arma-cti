### Fixed

- Every command-table refusal now names its exit. The confinement, recipe and table refusals — from the gate route and the standalone `just check-command-table` alike — carry the human's direct approval command with the live content_id where one is computable, and the standalone CLI's unreadable-repository refusal, which previously printed no `action=` line at all, names the repair instead, because no unreadable tree can produce the content_id the approval command needs. Three rounds of hand-enumerating these exits each missed one, so a test now enumerates every `refusal=` emission in `tools/check_command_table.py` and `tools/gated_paths.py` mechanically and fails on any that reaches its exit without an `action=` remedy.
