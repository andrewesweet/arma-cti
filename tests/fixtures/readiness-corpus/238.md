Human ruling, 2026-08-05, in the orchestrator session: the z.ai lane is used only in off-peak times, as a hard rule — a dispatch-time refusal, not guidance.

Build: a rung in the dispatch ladder (beside the admission and breaker reads) that refuses lane zai outside the off-peak window, with a typed refusal naming the window, its source, and when it next opens. No agent-facing override — the rule is the human's and only they amend it. The window itself comes from the lane's recorded plan_charge schedule (#225 landed schedule/window/window_source on the dispatch record; the registry or lane config is the one home — do not hard-code a second copy). Verify the window's definition against z.ai's published off-peak terms as the window_source names them; if the source is ambiguous about timezone or boundaries, state the reading taken and flag it on #221 rather than guessing silently. The breaker is the wrong home (it trips on failures; this is policy), but its state print should show the lane's window so a refused dispatcher sees why.

Also record: the first probation dispatch (d-20260805-211109-5b17eb, issue 92) launched before this rule existed — check its launch time against the window and state on #221 whether it happened to comply.

Tests both directions (inside window passes, outside refuses, boundary stated); the refusal class is infra_unavailable-shaped only if the vocabulary fits — say what you chose and why. CLAUDE.md or command-table text, if any is warranted, is a proposal to the standing gated vehicle, not landed here.
