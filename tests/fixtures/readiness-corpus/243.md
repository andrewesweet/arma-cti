The Codex lane: OpenAI's ChatGPT Plus subscription as the second foreign provider, unblocked in structure by the 2026-08-05T22:00Z per-lane credential ruling on #229 (Codex OAuth may reach third-party harnesses within the stated bounds: personal use, no commercial service, no account pooling; the credential never leaves the box, never argv, never logs — the dispatcher's environment assembly already enforces that).

Hard blocker, named by the ruling itself and NOT this ticket's to discharge: the authoritative OpenAI terms text is unread (403 to automated fetches). The human reads it before any Codex work runs; if it narrows the ruling, claude-code-router and LiteLLM's chatgpt provider get re-examined first, since only the Anthropic half of their rejection is known to hold.

Scope, gated behind that read:
1. The deferred substrate spike, now live: native codex CLI vs opencode, decided by evidence (ADR-0061's substrate ruling). Both may hold: codex CLI for this lane and opencode proven here for #234's Herma. The hook-parity suite (ADR-0061 D4) gates whichever harness gets authority — pytest-proven enforcement before anything lands from it.
2. Telemetry parity: cti.* resource attributes reaching the ledger from the chosen harness — the prior-art sweep found no off-the-shelf OTel hook target for either Codex or opencode, so this is engineering; the Codex metrics_exporter off-box default is already disabled in the generated config (#230) and must be verified on first use with the network watched, per the prereqs check's own instruction.
3. Lane wiring on the landed spine: registry profiles (the human names gpt-5.6 sol and terra as the models of interest — verify actual identifiers from the harness, never assume), breaker row, ledger pool (subscription window meter — cap_fraction basis needs its own calibration, the #218 method), off-peak flag false unless OpenAI publishes a discount, admission route starting at zero like every foreign route.
4. One live proof dispatch in #225's shape: auth, model mapping proven from the wire, telemetry arrival, ledger row, admission boundary honoured.

Sequenced after the terms read; before #234 (Herma inherits the substrate outcome). Refs #221, #229, #234, #225.
