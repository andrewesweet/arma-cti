From the #154 Clean Code second pass. Same-surface small findings, batched; no behaviour change except the stated performance shapes.

- `src/cti_daemon/daemon.py:261-269` — `_handlers()` rebuilds the dispatch dict per request; module-level table, the fix #90 already applied to `port.submit`.
- `src/cti_daemon/daemon.py:399-405` — `_poll` re-encodes the whole candidate reply once per pending entry (quadratic in outbox depth); encode incrementally or measure-then-slice.
- `src/cti_daemon/daemon.py:52` — `POLL_GUARD_BYTES` restates `budget.REPORT_GUARD_BYTES` by value, held by a test; derive it so the relation is structural (or add the one-sentence reason a derivation is wrong).
- `src/cti_daemon/daemon.py:14,33` — `Path` imported twice (runtime + TYPE_CHECKING).
- `src/cti_daemon/contacts.py:132` — `_seen` holds banded `_Record`s; "seen" is the Sighting vocabulary CONTEXT.md's Avoid list reserves for raw input → `_records`.
- `src/cti_daemon/contacts.py:199-206` — `_Record` defined after its only consumer; move above.
- `src/cti_daemon/squads.py:107-110` — `roll`'s docstring cites the renamed `of` (now `owned_by`).
- `src/cti_daemon/transport.py:206` — `port_` trailing-underscore parameter; rename.
- `src/cti_daemon/commands.py:39-73` — `CATALOGUE`/`EFFECTS` argument lists and the port handlers' hand-written validation are two declarations of one schema; fold or state why two.

Acceptance criteria:
- [ ] Each item applied or declined with a sentence at the site.
- [ ] `just fast` green; no behaviour change beyond the stated performance shapes.

Cross-reference: #154 (findings 4-9, 11-13), #90.
