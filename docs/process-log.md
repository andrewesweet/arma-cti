# Process log

Audit trail of how the development process evolved. One entry per retro. See ADR-0009.

## 2026-07-30 — bootstrap (not a retro)

Initial process written during the founding grilling session, before any practical use. All artefacts stamped `unproven`: `CLAUDE.md`, `.claude/skills/{playtest-brief,playtest-ingest,retro}`, `docs/agents/*`. First real retro due at end of Phase 0.

## 2026-07-30 — user-directed amendment: hooks and language toolchains (not a retro)

Claude Code hooks added (`.claude/hooks/` + `.claude/settings.json`, all tested): deny edits to generated files and acceptance specs (mechanises the F1 oracle mitigation), deny `git commit --no-verify`, auto-format `.py`/`.rs` on edit. A fourth (lint-after-edit) deferred pending Phase 0 latency measurement. Python toolchain decided: uv, ruff, ty (Astral, user's explicit choice over pyright), pytest/hypothesis, coverage.py, mutmut. Rust: pinned toolchain, clippy `-D warnings`, rustfmt. Blanket strictness principle: warnings are errors; suppressions need inline justification.

## 2026-07-30 — user-directed amendment: versioning standards (not a retro)

Adopted Conventional Commits 1.0.0, Keep a Changelog 1.1.0, SemVer 2.0.0 (ADR-0010). Enforcement: cocogitto 7.0.0 installed, `commit-msg` hook active and verified rejecting bad messages, existing history checked clean, `CHANGELOG.md` seeded, CLAUDE.md contract updated, `cog check` earmarked for `just check`.

## 2026-07-30 — user-directed amendment: prompting-guide alignment (not a retro)

Agent guidance reviewed against Anthropic's official Opus 5 and Fable 5 prompting guides, at the user's direction, optimising each document for its likelier consumer (opus[1m] for day-to-day surfaces). Changes: `CLAUDE.md` gained a Working style section (scope discipline, verification capped at the project gates, subagent-delegation limits, evidence-grounded progress claims, report-everything review passes, deliverable-length calibration); `retro` gained a bias-toward-removal rule (current models degrade under over-prescription); `playtest-brief` gained document-length calibration. `docs/agents/*` (mechanical command reference) and `CONTEXT.md`/ADRs (domain facts, not behavioural prompts) reviewed, unchanged. No existing instruction conflicted with the guides; notably nothing instructs reasoning reproduction (a Fable 5 refusal trigger).

## 2026-07-30 — user-directed amendment: full wiki snapshot (not a retro)

The Phase 0 retro added `docs/reference/arma-wiki/` to *Read first* on the strength of a research failure; it held nine hand-picked pages, which is thin cover for "consult the wiki first". A full BIKI export became available and is now vendored: 6,690 pages, tier A + B plus the non-article namespaces. *Read first* gained the navigation an agent needs to use it — guessable paths, `MANIFEST.json` as the lookup, per-directory `INDEX.md` instead of listing a 2,672-file directory — and two traps worth naming. Game applicability is in each file's `// categories:` header and never in the wikitext, because BIKI generates categories from templates, so grepping page source for `Arma 3` finds nothing. And pre-Arma-3-only pages are excluded, so a miss is not proof the wiki lacks the page.

## 2026-07-30 — retro: Phase 0 spike (#2)

First real retro. Trigger: phase completion.

**Findings.** The dominant one is a research failure, not an execution failure: eleven configuration hypotheses were tested and eliminated over most of a day before the headless-client blocker turned out to be one sentence of first-party documentation ("Don't forget to set NAME property"). The wiki was Cloudflare-blocked from this environment throughout, so the source holding the answer was the one source not consulted. Secondary: the `just` command table listed recipes that did not exist and omitted every recipe that did; the failure-class table earned its keep when `infra_unavailable` fired on a stale daemon holding a port and correctly refused to be interpreted as a result; the lint-after-edit hook deferred on 2026-07-30 became decidable once latency was measured.

**Applied** (all human-approved): command-surface table replaced with the recipes that exist, with the acceptance tiers marked as Phase 1 work; `docs/reference/arma-wiki/` added to *Read first* with an instruction to consult it before experimenting on engine behaviour; failure-class table marked `validated ×1`; a port-collision line added to *Contract*, since the Arma tier shares this machine with the human's play sessions under WSL2 mirrored networking; `lint-after-edit.py` enabled for `.sqf`/`.cpp`/`.rs` (132 ms and 121 ms measured, against 20–56 ms for the hooks already running), advisory-only so `just check` remains the gate.

**Rejected / no change.** `block-no-verify.py` false-positived on a Bash call whose heredoc *contained* the blocked phrase as text; it fails safe and the workaround (use Edit/Write) is obvious, so no change. The Contract's "never extend a timeout to make a test pass" was briefly ambiguous when a harness timeout was extended to fix a harness race rather than to pass a test; judged not worth extra words. Python left out of the lint hook: `uv run ruff check` on one file costs 183 ms, almost entirely `uv run` resolving the environment.

**Defects fixed in passing.** `format-on-edit.py` ran `rustfmt --edition 2021` against an edition-2024 crate, so it wrote files `cargo fmt --check` then rejected — an auto-formatter that disagrees with the gate is worse than no formatter. `ruff` 0.16 formats Python inside Markdown, which would silently rewrite third-party code quoted verbatim in research documents; `docs/` is now excluded, and the general rule is that a formatter must never rewrite a quotation.

**Status.** `CLAUDE.md` failure classes: `unproven` → `validated ×1`. The rest of `CLAUDE.md` survived the phase unchanged apart from the amendments above. `playtest-brief`, `playtest-ingest` and `retro` remain `unproven` — `retro` has now had one use, this entry.

## 2026-07-30 — retro: ad-hoc, Phase 1 (#9-#12)

Trigger: user-directed, on discovering `toJSON`/`fromJSON`.

**Findings.** *Read first* told an agent to consult the wiki "before experimenting on engine behaviour". That is a narrower trigger than it reads: the expensive miss this phase was not an experiment but an assumption. SQF was assumed to have no JSON parser, so `tools/generate_manifest_sqf.py` and `tools/generate_command_sqf.py` were built to render authored data into SQF literals — while `toJSON`/`fromJSON` had been engine-native since 2.18 and the server runs 2.20. The wiki was consulted repeatedly and correctly this phase (`random` syntax 3, `getUserInfo`, `CfgFunctions`, `loadFile`), so the instruction works when it fires; it simply did not fire for "does this already exist". Same shape, second instance: `skipLobby = 1` — the fix that made the headed client join unattended — was documented all along and sat on a closed issue's eliminated list.

**Applied** (human-requested): *Read first* now says to check the wiki before experimenting on engine behaviour **and before writing your own version of anything the engine might already do**, with the generator as the second worked example beside Phase 0's lost day.

**Also applied, on sign-off.** A *Working style* line that an elimination holds only in the context it was tested in — `skipLobby` was written off against a server whose mission was not initialised, and against one that was, it worked first time. And the failure-class table goes `validated ×1` → `×2`: a deliberately emptied manifest produced `assertion_failed` in-world and the harness refused to pass rather than booting an empty world, which is the second time the table earned its keep.

**Issues raised rather than fixed here.** #22 (replace the generated-SQF pipeline with `loadFile` + `fromJSON`; flagged ADR-0012-adjacent), #21 (exercise the client-to-gateway leg, blocked by #18).

**Status.** `retro` has now had two uses and has not needed amending; still `unproven` pending a use that tests it rather than exercising it.

## 2026-07-31 — retro: ad-hoc, after #27

Trigger: user-directed, after the per-side Observation projection landed.

**Findings.** The dominant one is a convention nobody followed, including the agent that wrote the convention's last amendment. `CLAUDE.md` says to interact with the project through `just` only, and the Phase-1 in-world tier has no recipe — `just spike` runs the phase-0 measurement mission. So both #15 and #27 were verified in-world by calling `./spike/run.sh --hold` directly with five environment variables. For #27 that invocation was reconstructed by reading the script and a *leftover* `.spike-out/mission/cti.Stratis/harness.sqf` staged by the previous session — a file the harness rewrites every run, so the reconstruction was luck. Second half of the same gap: #27's probe lived in the session scratchpad, so the issue carries its output and not its source, and re-verifying would mean rewriting it. Secondary: `git push origin main` from an agent worktree was rejected non-fast-forward, because it pushes the local `main` branch the worktree is not on; the correct sequence was undocumented. `cargo` is not on `PATH` in a default shell here, so every gate call this session carried a manual `export`, knowledge that travelled by handoff document rather than by the repo. And `/grill-me` was missing from the workflow backbone despite having driven both major design decisions to date.

**Applied** (all human-approved): `CLAUDE.md` gains `/grill-me` in the workflow backbone, a *Commits* bullet giving the worktree landing sequence, and a command-table row for `just probe` — added with the recipe rather than ahead of it, since a table listing recipes that do not exist is the defect the Phase 0 retro fixed. `.claude/skills/retro` goes `unproven` → `validated ×3`.

**Fixed rather than deferred** (#29, #30, both closed here): `just probe [file] [hold]` wraps the Phase-1 tier; `spike/probes/` holds probes in the repo, so the evidence a verification rests on outlives the session that wrote it; the justfile puts `~/.cargo/bin` on `PATH`. Building them turned up a false-green the retro had not: hold mode read its verdict when the hold window closed, so a probe still working at that moment reported `HOLD-COMPLETE` off a log it had not finished writing. `CTI_HARNESS_AWAIT` now waits for the probe's own completion line, with `FAIL` ending the wait too so an assertion is classified as one rather than timing out. Proven by running the #27 probe under a 60-second hold it outlives.

**Rejected / no change.** A rule about preserving probe source alongside its evidence: real, but it dissolves once `spike/probes/` exists, and a rule duplicating a tool is the over-prescription this skill warns against. A rule about editor diagnostics: `ty` in-editor contradicted the gate repeatedly this session (`EconomyTable has no attribute capture_seconds` and two others) while `just check` passed, and the gate was treated as authoritative without needing to be told — *"The gates above are this project's verification"* already covers it.

**Status.** `.claude/skills/retro`: `unproven` → `validated ×3`. Failure classes stay `validated ×2` — `assertion_failed` was wired into the #27 probe but never fired, so nothing new was earned. `playtest-brief` and `playtest-ingest` remain `unproven` and unused.

## 2026-08-01 — retro: scheduled, five issues (#28, #16, #17, #12, #22)

Trigger: five issues closed since the last retro. Also landed since: ADR-0013–0018, `docs/regression-tier.md`; #31 raised.

**Findings.**

- *Closure lagged landing, and the #12 audit earned itself once.* Three of the five sat closed-in-fact but open-in-record for a session, and #12 was nearly closed off commit titles. The read-only audit found criterion 8 unmet — but the evidence trail shows the pivot (callback-push → poll-and-ack) was *flagged* in #12's original delivery comment; what was missing was a decision record, which ADR-0018 then supplied. So the audit is not made a standing requirement. Instead the discipline that made #16/#17/#22/#28 auditable at a glance — close in the landing session, per-criterion evidence, supersessions recorded not narrated — is codified in `docs/agents/issue-tracker.md`, with the full audit required only when inheriting an issue whose work landed in an earlier session.
- *The ADR-number collision's fix worked first time.* #22 claimed ADR-0017 by issue comment while ADR-0018 landed concurrently; no second collision. The claim-by-comment rule *is* the structural fix — a reservation protocol over a shared namespace — so no deeper change; rule marked `validated ×1`.
- *The tier serialisation finding is a sequencing one.* The corpus grew 4 → 7 probes while #23 (the runner, lock, evidence dirs) stayed unbuilt, and three independent issues queued on one hand-driven tier. Recommended on #23: build it before further in-world feature work. Not made a CLAUDE.md rule — it is a scheduling call, and one instance.
- *Conventions designed but not applied, second instance of the pattern.* ADR-0016's `probe:`/`issues:`/`window:` header existed on no probe. Fixed here: all seven probes now carry the block, plus `// expect: assertion_failed` on `manifest-missing` — the red-by-design probe ADR-0016's "corpus gated green" definition did not anticipate; the design doc now specifies `expect:`. CLAUDE.md gains one working-style line: land a convention with its first applied instance. (The 420-vs-300 window discrepancy was already fixed on main before this retro.)
- *The six delegated ADRs are genuine decisions, not rationalisations* — each weighs rejected alternatives, and five of six state overturning evidence; they ratify built work and say so, which is what a sign-off on a flagged judgement call is. Two audit gaps fixed under ADR-0019: the marker set could not answer "which have I since reviewed" (field block gains `Reviewed-by-human: pending`, flipped only by the human; 0013–0018 retrofitted), and ADR-0015 stated no overturning evidence (retrofitted).
- *The probe corpus is load-bearing, not decorative.* This phase it found: three `targetsQuery` engine facts none of which a unit test could reach (#28), `Roster.reconcile` deleting Squads in flight, the desync loader gifting four Objectives (caught twice, two guards), travel scored in hops not metres, the harness's hold-mode false-green and its hard-coded 180 s cap, and it verified the `loadFile`-in-PBO fact ADR-0017 stands on.
- *`just check`/`just unit` caught one thing on record this phase*: `schema_stale` fired mid-session on #22 against a stale export. Every other defect lived at the engine boundary. The unit tier's job this phase was containment (283 tests holding determinism, no-overspend, isolation), not discovery — which is more weight on building #23, not less on the gates.

**Applied** (recorded as delegated decisions under ADR-0019, `Reviewed-by-human: pending`): closing-discipline section in `docs/agents/issue-tracker.md`; probe header blocks on all seven probes and the `expect:` extension in `docs/regression-tier.md`; ADR-0013 convention amended (fourth field line, overturning-evidence requirement); ADR-0014–0018 retrofitted with the field; ADR-0015 retrofitted with overturning evidence; CLAUDE.md marker bumps and the convention-lands-with-instance line.

**Proposed, not applied — for the human.** Two edits to `.claude/skills/retro` were blocked at the permission layer, treated as the gate speaking: (1) status `validated ×3` → `×4` (fourth use; whether it counts as "no amendment needed" depends on (2)); (2) a step-3 sentence saying that in an unattended session ADR-0013's standing authorisation may discharge the sign-off gate via a delegated ADR — this retro acted on that reading, and the skill should either say it or the reading should be repudiated.

**Rejected / no change.** A standing requirement to audit every issue at close (the discipline above makes it the exception, and ~70k tokens is a price to pay on inheritance, not on every close). A CLAUDE.md rule ordering capability work ahead of feature work (one instance, scheduling not process). Cutting `playtest-brief`/`playtest-ingest`: still `unproven` and unused after four phases, but for a structural reason — nothing a human can play exists yet (no map UI, no win conditions), so disuse is not evidence of failure. Tripwire instead: if the first play session happens without `playtest-brief` having authored its brief, cut both at the following retro.

**Status.** Failure classes `validated ×2` → `×3` (#22: `infra_unavailable` refused a mis-staged failure as a result; the properly reached guard classified `assertion_failed`). Probe-window rule `validated ×1` → `×2` (#17: the harness's own 180 s cap was fixed by honouring the subject-sized window, not by extending anything). ADR-claiming rule → `validated ×1`. `retro` `×3` → `×4` proposed above. `playtest-brief`/`playtest-ingest` stay `unproven`, tripwired.

## 2026-08-01 — retro: scheduled, five issues (#31, #23, #32, #34, #33)

Trigger: five issues closed since the same-day retro above. ADR-0020 and 0021 landed; #32–#35 raised, three closed. Amendments recorded under ADR-0022 (`Reviewed-by-human: pending`), claimed by comment on #35 while a #35 agent lands concurrently.

**Findings.**

- *A session limit killed two agents mid-flight, and the process had no words for it.* One died mid-Arma-run with uncommitted work, a dead server, and a half-written evidence directory. Everything designed held without being asked: the tier's `flock` freed itself on holder death — chosen for exactly this, first exercised the day it shipped — and both agents resumed cleanly from their worktrees' last commit plus `git status`. What was improvised was the orchestrator's reading of the leftovers, so that reading is now written: a dead run's evidence carries no `verdict.json` and is not a result; its leftover world is stale state to clear, never context to inherit (CLAUDE.md lock paragraph, ADR-0022). A worktree-resumption runbook was declined — both resumptions worked from what worktrees already afford, and a procedure would prescribe the default.
- *The regression tier changed behaviour within hours of landing, and full-corpus-per-issue is the right default at this size.* #32 was gated by `just regress` instead of a bespoke probe and the run closed a corpus gap in place (no probe asserted an Order carries a Place — the one failure mode of the rename no unit test could see); #33 ran 9/9. Cost is measured, not felt: ~1.5–2 min of wall per probe including bring-up (12 m 38 s / 13 m 10 s / 12 m 56 s at 8 probes, ~16 m at 9). The design already made selection a fable-session call on corpus growth; this session made it a number — first full green pass over 30 minutes builds `issues:`-header selection before the next probe lands. Tracked as #36; issue-class subsets declined again, for the design's own reason (the expensive failures are the unselected kind).
- *Two placeholders landed as sign-off-ready surfaces without a rule saying how.* #34 measured its `decapitation` 8.0 across 200 seeds and reported the behavioural window (7.27, 8.87) with the thinnest margin (0.11); #33 chose the simplest demolition mechanism and named the passed-over alternatives in the file header. Both are what a flagged judgement call should look like, and both happened unprompted — which is the evidence *against* requiring the pattern. "Measured window + thinnest margin" is also surface-specific: #33's fixed 90 s has no seed window to measure. Recorded here as exemplars; no rule.
- *Parallel dispatch with a ground partition worked, and stays uncodified.* #33 and #34 ran concurrently with do-not-touch lists; the predicted probe collision did not materialise; #34 yielded the tier by partition without ever contending the lock. One clean instance of a scheduling call, not process.
- *#31 closed as a decision ticket — ADR + CONTEXT terms + four implementation issues in dependency order, no code — and three implementing agents needed zero clarifying questions.* Named as a pattern in `docs/agents/issue-tracker.md` (ungated surface), with the flag that triggers one: a build ticket saying a decision is "not the scorer's to route around".
- *The lock's `infra_unavailable`-on-held fired only deliberately* (#23's demonstration, including bounded `--wait`); in anger, the holder-death case fired instead and produced the opposite of a stale stop — a free tier. No agent report reads a held lock as a result.
- *Building the runner found the harness had been mistyping every in-world failure* as `assertion_failed`, including ones the world had typed `timeout` or `oracle_disagreement` — latent in every hand run to date, and surfaced because the table's required-response column makes a wrong class a harness bug. That is the failure-class table's fourth earn.

**Applied** (under ADR-0022): the dead-holder sentence in CLAUDE.md's lock paragraph and the matching validation note in `docs/regression-tier.md`; the 30-minute selection tripwire in `docs/regression-tier.md` §Cost control (+ #36); the decision-ticket section in `docs/agents/issue-tracker.md`; marker bumps below.

**Rejected / no change.** Requiring the measured-window pattern for placeholders (over-prescription; both agents did it unprompted, and the pattern is surface-specific). A worktree-death recovery runbook (the tools already afford it). Codifying parallel-dispatch partitioning (one instance; scheduling, not process). Issue-class regress subsets (cost measured and cheap; the tripwire owns the future).

**Pending for the human, not retried.** The previous retro's two blocked `.claude/skills/retro/SKILL.md` edits — the status `×4` bump and the standing-authorisation sentence for step 3 — remain proposed and unanswered. This retro again acted on the standing-authorisation reading (ADR-0022); the skill still does not say it may.

**Status.** Failure classes `×3` → `×4` (#23: the mistyping fix — the harness now believes the line's own class). Probe-window rule `×2` → `×3` (#33: two effects on separate pump turns; fixed by waiting on the pair, window unmoved). ADR-claiming rule `×1` → `×2` (#23's 0020 → 0021 renumber on rebase, exactly as prescribed). Convention-lands-with-first-instance → `validated ×1` (#23 landed row, lock, and evidence conventions with the recipe). `retro` file marker stays `×3` with `×5` now the honest pending proposal (fifth use, no amendment needed by either of the last two). `playtest-brief`/`playtest-ingest` unchanged, still tripwired on the first play session.

## 2026-08-01 — retro: scheduled, five issues (#35, #37, #39, #40, #41)

Trigger: five issues closed since the morning retro (`ce1035c`). Also in scope: brief 0001 (the first `/playtest-brief` use), its verification pass, #18's ready-for-human half-close, and the human's permissions allowlist (`ccc7deb`), which ended classifier-denial friction on process surfaces — the two blocked retro-skill edits from two retros ago landed at `3c512f9` and this retro ran under the amended sign-off step. Amendments recorded under ADR-0026 (`Reviewed-by-human: pending`), claimed by comment on #21 while its agent works concurrently.

**Findings.**

- *The unverified-instruction pattern recurred at its first human-facing surface.* Brief 0001's boot line was authored blind (tier busy; flagged honestly), and verification found two wrong instructions — a lobby that does not exist, a build step the session never loads — plus the harness filing a joined Commander as `connected-but-never-entered-mission`. Third instance of "docs describing something nobody has done" (Phase 0 command table, ADR-0016 headers), first with the human as reader; honest flagging did not prevent it, only execution did. Fixed in the skill: the boot line is run before the brief is handed over; the tier being busy defers delivery, not verification. A general "verify every instruction to the human" rule declined — one surface, one instance; generalise on a second surface (ADR-0026).
- *`/playtest-brief` half-worked on first use and its author recorded four frictions.* All four amended: fixtures that assert nothing live in `spike/playtest/`, never `spike/probes/` — the corpus-membership rule now lives in `docs/regression-tier.md` rather than only in `6cac77e`'s commit message; the fresh-campaign rule gains its honest exception until staged-state fixtures exist (#42 raised); an empty perceptual checklist asks for candidates in the response instead of inventing entries; the bring-up path the skill assumed now exists (`session-hold.sqf` + verified boot line). Marker: `unproven` → `validated ×1`, amended on first use.
- *#40 fought the `just`-only rule deliberately and was right to.* It ran `spike/sp-run.sh` bare because a recipe means a sign-off-gated command-table row, for scaffolding that concluded "don't build it" and stayed off main. The rule now says what #40 read into it: it binds work that lands and its verification; exploration scaffolding off main may run bare, and landing it lands its recipe in the same commit. #27's contrasting case (bare invocation of a landed tier) stays a defect. Also the first *decline* to grow the corpus — the 165 s probe stayed on a branch — a decision class the tier doc's cost-control section had anticipated; no change there.
- *Two harness self-deceptions this cycle, both in bash orchestration, both found by use and neither by the corpus*: the joined-Commander-filed-as-absent wait (only `spike.Stratis` writes `client_report`) and #41's guard silently skipping itself on a bare `tasklist.exe`. Common shape: the harness reporting on its own environment fails open where verdicts fail closed. Carried to #5 as a design comment on the Phase-3 Python orchestrator, not a rule — two instances, and the fix pattern (#41's three-answer guard, `unavailable` ≠ `free`) is already on main to copy.
- *The process pivot CLAUDE.md was written for is live to step one.* brief → play → ingest exists as far as the brief; the tripwire from two retros ago ("cut both skills if the first session happens without a brief") is discharged in the skills' favour — the brief exists first. `playtest-ingest` stays `unproven`, now simply waiting on the human playing 0001.
- *Phase 1 status (#3), for the record, not a rule*: done — #12, #13, #14, #15, #16, #17, #22, #23, and #18 in substance (ready-for-human on the two eyes-only criteria, a clean half-close). Still owed: #19 (integration demo + no-path-outside-the-port audit), #21 (client-to-gateway leg, in flight), #24 (presence-sampling assertion), #25 (playable Squad-leader slot). Adjacent, unowned by Phase 1: #26, #36, #38, #42.

**Applied** (under ADR-0026): the boot-line-verification and three sibling amendments in `.claude/skills/playtest-brief/SKILL.md`; the exploration-scaffolding sentence on CLAUDE.md's command surface; the corpus-membership sentence in `docs/regression-tier.md`; marker bumps below; #42 raised; signal comment on #5.

**Rejected / no change.** A general verify-instructions-to-the-human rule (one surface; the skill amendment is the fix at the grain the evidence supports). A rule for the harness-environment fail-open shape (two instances, exemplar fix on main; #5 comment instead). `docs/mvp-scope.md` edits — the status above is scheduling, and the scope document is accurate as written. The recovery runbook (`docs/agents/recovery.md`): zero uses since writing, no change, no marker to move.

**Status.** Failure classes `×4` → `×5` (#41: a check that could not run classifies `infra_unavailable`, never passes). Probe-window rule `×3` → `×4` (#35: the teleported Squad met a garrison the real Campaign would have marched away; fix restored conditions, window unmoved). ADR-claiming `×2` → `×3` (#35's clean 0022→0023 renumber; #37's held claim). Elimination-context line → `validated ×1` (#35 inheriting #33's empty-Base measurement, named as such by the #35 agent itself). Convention-lands-with-instance stays `×1`. `playtest-brief` `unproven` → `validated ×1` (amended on first use). `playtest-ingest` stays `unproven`, tripwire discharged. `retro` `×5` → `×6` (sixth use, first fully under the amended unattended sign-off step, no self-amendment needed).

## 2026-08-01 — retro: scheduled, five issues (#38, #43, #21, #36, #44)

Trigger: five issues closed since `aeffc45`. Also landed: ADR-0027, ADR-0028; #46, #47 raised by the closing issues themselves. Amendments recorded under ADR-0029 (`Reviewed-by-human: pending`), claimed by comment on #45 while its agent runs concurrently. **Review queue at this retro: 16 ADRs pending (0013–0028), growing ~4 per cycle** — first report under the step-3 sentence this retro added.

**Findings.**

- *A new use of the harness audits the assumptions its old use shared with the code — second instance, so recorded.* #23's runner build found every in-world failure mistyped `assertion_failed`; #44's two-slot run found the shim reading `CTI_DAEMON_ADDR`, which the harness never set — latent in every serial run because the defaults agreed, surfaced as two runs silently merging and one slot reporting PASS on the other's world. One paragraph added to `docs/regression-tier.md` beside the #44 finding: budget for what a capability exploration flushes out, and report it as a finding, not a detour. The false PASS itself was weighed as a failure-class gap and declined — the classes type verdicts the harness emits, and a harness lying green cannot type its own lie; the defence is #47's named-consumer rule, already where the pool builder will read it.
- *A probe bets on the decision the code owns, never on a world-owned outcome.* #38 burned seven runs on a fighting garrison (a mass that won, a mass that lost, a run where the observer died before the sampler ticked and the decision was unobservable). The lesson is already codified at length in the tier doc by #38's own landing; CLAUDE.md's probe paragraph gains the one-line sibling rule beside the window rule, since that is where probe authors start.
- *The recovery runbook earned its marker and gained the case its second use exposed.* Two real uses, both 2026-08-01, both briefed to the document's three-part contract, both clean — #21's agent died mid-Arma-run and was resumed with the runbook's first real briefing, then rebased through three intervening landings without incident. The silent-stall case (agent stopped mid-turn, run still live, completion notification fired anyway) was outside "Recognising death", which distinguished failure from silence but not silence-with-live-work; amended — that is death with live work, and the live run's output is still not a result.
- *Three explorations concluded in three different right ways, unprompted*: #40 don't-build (scaffolding off main), #44 build-via-follow-up (#47, the port-allocation precondition explicitly the human's), #43 convert-in-place (landed, with the hard conversion declined for a measured reason and raised as #46). No doc change — the decision-ticket section and the tier doc's cost-control text already cover the shapes, and three correct conclusions are evidence the triage needs no more words.
- *The tripwire pattern fired on schedule, first time.* The tier doc set a measured 30-minute trigger two cycles ago; #36 landed the selector four minutes of arithmetic ahead of it, at ~26 m 40 s. Already recorded in the doc by #36's own landing; noted here as validation, no change. #36 also declined changed-files selection for the honest reason (no true file→probe map exists) and made zero-match refuse — the fail-closed spirit, though not a class-table earn.
- *Marker labels drifted from the log.* ADR-0026 recorded probe-window ×4 and ADR-claiming ×3; CLAUDE.md's labels stayed ×3/×2 — exemplars appended, counts unbumped. Corrected. One instance, caught by the log at the next retro, which is the mechanism working; no new mechanism.
- *The review queue was invisible except to whoever grepped.* Sixteen delegated ADRs pending, growing ~4 per cycle. The retro skill's step 3 now states the count in each report and journal entry; clearing it stays the human's cadence to choose.

**Applied** (under ADR-0029): the exploration-as-audit paragraph in `docs/regression-tier.md`; the assert-the-decision sentence in CLAUDE.md's probe paragraph; the recovery runbook's status marker and silence-with-live-work amendment; the retro skill's queue-depth sentence; the two label corrections; marker bumps below.

**Rejected / no change.** A failure class for a false green (nothing can emit it; structural defence lives in #47). A rule for how explorations conclude (three right endings under existing text). Anything about tripwires (the doc already records its own firing). A mechanism for marker counts (once, and the log caught it).

**Status.** Probe-window rule ×4 → ×5 (#43: `two-commanders` left unconverted because its soak carries #17's measured extremum — the rule's reasoning as a reason not to shorten). Recovery runbook: unmarked → `validated ×2`, amended on second use. Failure classes stay ×5 — nothing new earned; the false-green case is outside what the table can type, by design. ADR-claiming stays ×3 (label corrected; ADR-0027/0028 landing without collision is the rule operating, not a new earn). Elimination-context and convention-lands-with-instance stay ×1. `playtest-ingest` stays `unproven`, waiting on the human playing brief 0001. `retro` ×6 → ×7, self-amended (the queue-depth sentence) through its own procedure.

## 2026-08-02 — retro: scheduled, five issues (#45, #48, #26, #50, #46)

Trigger: five issues closed since `de32134`. Also landed: #49's implementation (multiplicative scorer, ADR-0031, its two flagged feel values human-approved in-session; the issue stays open for the tier-owed pass), ADR-0030/0031/0032, #51–#58 raised. Amendments recorded under ADR-0033 (`Reviewed-by-human: pending`; no concurrent agents, so claimed by fetch-and-scan alone). **Review queue at this retro: 20 ADRs pending (0013–0032), 21 once 0033 lands** — the human has flipped none, and ADR-0031 stays pending despite the in-session feel-value approval, because only the human flips the field.

**Findings.**

- *The orchestrator's own monitoring lied the way the harness once did, and the cost was noticing, not recovering.* `pgrep -f arma3server` matched the orchestrator's own command line and reported SERVER-LIVE for hours over a dead server while the #46 agent sat stalled with a *finished* pass nobody read — one of three stalls on that agent this cycle (mid-pass, post-pass, post-commit), each resumed cleanly by a runbook briefing, one after ~8 h unseen. Third instance of self-observation failing open (#41's `tasklist.exe`, #44's daemon-address defaults), which is the bar the last retro set for codifying it. `docs/agents/recovery.md` gains the orchestrator's side: fail-closed self-checks (`pgrep -x`, "could not observe" ≠ "still running"), check at the work's completion edges rather than on wall-clock, with the in-session watchdog-loop pattern (re-armed on server exit, grace-then-nudge) as the exemplar. That one section answers both "when to look" and the watchdog question — no separate cadence rule, which would be the clock-watching that just failed.
- *Nested subagents report to the spawning session, not their parent agent.* #48's strands delivered to the orchestrator, which relayed by files plus messages — worked, ~15k tokens of double-handling. One known-behaviour line in the same recovery.md section; transcript lore otherwise.
- *#26 re-measured before designing and the diagnosis inverted.* The issue's "Squad count binds, island size barely matters" was measured while the Observation carried both rosters; after #27/#28 the enemy term grows with the map, and a sixty-Objective island truncates empty. Not a new named rule: it is the elimination-context line applied to an inherited measurement, so that line widens by a few words and earns ×2.
- *#50's label deviation was the label's plain meaning, not drift.* #52 and #54 are mostly agent work gated by a human first step (a rebuild, a purchase); `ready-for-human` already means the human goes first. Adopted: the triage table's meaning column now says "requires human action first", citing both.
- *Research tickets are compounding.* #48's steal item 1 became #49 the same day with thirteen pinned behaviours; #50 closed as a decision-shaped research ticket into ADR-0032 plus four dependency-ordered issues. Evidence the existing shapes work; no change.
- *#45 declined multiplexing for the #44 lesson on a new axis* — mission cycling is <1 s but freshness needs a daemon restart, and the roster would have passed a dirty world. A false green still cannot type its own lie; the defence stays structural. No class earn; table stays ×5.
- *The four directed reviews (#55–#58) need nothing from the process docs*: findings-as-prioritised-issues flows through the tracker machinery unchanged (creation, triage labels, closing discipline, decision-ticket escalation), and nothing blocks it.

**Applied** (under ADR-0033): the noticing section and marker in `docs/agents/recovery.md`; the `ready-for-human` wording in `docs/agents/triage-labels.md`; CLAUDE.md's elimination-context widening and three marker moves; retro skill ×8.

**Rejected / no change.** A wall-clock polling cadence for stalled agents (the failure mode itself). A new "re-measure before designing" rule (four words on an existing line instead). A failure class for the orchestrator's false SERVER-LIVE (outside the verdict system). Any change for the reviews or the research-ticket pattern (working as built). No new issues: everything declined is already tracked (#49 holds its tier-owed pass, #47 the pool, #51–#54 the second machine).

**Status.** Recovery runbook ×2 → ×5, amended (three clean resumptions; the gap was noticing). Elimination-context ×1 → ×2 (#26). Probe-window ×5 → ×6 (#46: the floor kept a second time, absence claims made 2 s watchers — strictly stronger, window unmoved). ADR-claiming ×3 → ×4 (#49's 0030 → 0031 rebase renumber). Failure classes stay ×5. Convention-lands-with-instance stays ×1. `playtest-ingest` stays `unproven`, still waiting on the human playing brief 0001. `retro` ×7 → ×8, no self-amendment needed.

## 2026-08-02 — retro: scheduled (overdue), the review phase — thirteen issues

Trigger: thirteen issues closed since `9f37dfd` (trigger is five): reviews #55–#58 + #95, #49's tier-owed pass, and fixes #59, #60, #67, #68, #69, #81, #83 — all seven fixes landed same-day off the reviews' own priority ordering. ADR-0034 landed concurrently (#69's fix); #104 raised (recall-probe gap); #105 raised here. Amendments recorded under ADR-0035 (`Reviewed-by-human: pending`), claimed by comment on #105 while two fix agents run concurrently. **Review queue at this retro: 22 ADRs pending (0013–0034), 23 once 0035 lands** — the human has flipped none.

**Findings.**

- *The directed review pass worked, and its shape is now written where the next one will look.* Five tickets, one lens each, held independently to one deliverable contract (rubric vs stated target, full-sweep-then-filter, findings as severity-tagged issues, priority against the open backlog), and the concurrency disciplines emerged unprompted: #56/#57/#58 noted neighbouring-remit findings rather than re-filing, and #95 cited the in-flight #67/#69/#59 fixes as evidence rather than filing over them. ~45 findings flowed through the unmodified tracker machinery. Codified as a section in `docs/agents/issue-tracker.md` — descriptive of what ran, not new prescription; ADR-0033's "no process change needed" was pre-results and stands for the machinery, this is the ticket shape on top of it.
- *The score spread is the verification investment's own outline.* Game code 8.5–9, harness 7–8.2, play path ~5 — worst exactly where no automated tier looks (a human's multi-hour session), found only because the human extended #58's scope into #95. No standing phase-boundary production-lens gate: the designed standing lens is the playtest loop, still unexercised, and mandating a second instrument before the first has run once is premature. The residue is one scope line in the review section; a second boundary with the same spread reopens it.
- *Worktree infrastructure failed under agents a second time, and the response is now written.* The #56 review's isolation worktree vanished mid-sweep (14 config files unreviewed, #94), after #48's strands hit the same. Not covered by the stale-state rule — that governs a dead agent's leftovers, this is a live agent losing its ground. `docs/agents/recovery.md` gains the symptom and the twice-improvised response (finish read-only from the main checkout, never commit there, file the gap); #105 tracks the cause for the human, with the read-only-worktree-reaped-as-unchanged hypothesis stated.
- *The nested-agent relay recurred (#56's sweep slices) and confirms the existing recovery.md line*; naming the /tmp-file mechanics declined — a harness quirk to retire, not a pattern to specify.
- *Severity-first, ground-partitioned burn-down under the act-before-further-issues directive: working as designed.* Scheduling, not process — the same verdict parallel-dispatch partitioning got two retros ago.
- *The marker count lagged its exemplar list again — in the commit whose log entry corrected the first lag.* `9f37dfd` appended the #46 and #49 exemplars with probe-window and ADR-claiming labels left at ×5/×3 while its own entry recorded ×6/×4. Two consecutive retro commits, both under step 5, is a step failure: labels corrected, and the retro skill's step 5 now says the ×N and the exemplar move in the same edit.
- *#83 is the failure-class table's sixth earn, and a new kind*: the review found `run.sh`'s non-hold path retyping every FAIL as `assertion_failed` because the table makes a wrong class a harness bug, and the fix put classification under `just unit` — the first time the class machinery is testable without Arma.

**Applied** (under ADR-0035): the directed-review-pass section in `docs/agents/issue-tracker.md`; the worktree-vanish paragraph and status note in `docs/agents/recovery.md`; the step-5 same-edit clause in `.claude/skills/retro/SKILL.md`; CLAUDE.md's failure-class earn and the two label corrections; #105 raised.

**Rejected / no change.** A standing production-lens review at phase boundaries (playtest loop first; one scope line instead). Naming the /tmp relay pattern (ossification). Codifying burn-down sequencing (scheduling). A class earn for #81/#97's false greens (the table cannot type a lie told green; structural defence stands, per #44/#45).

**Status.** Failure classes ×5 → ×6 (#83). Probe-window label corrected to ×6, ADR-claiming to ×4 (no new earns — the reviews filed no ADRs and 0034 landed without collision, the rule operating). Recovery runbook stays ×5, amended (the vanish recoveries ran outside the document; nothing this cycle was briefed through it). Elimination-context stays ×2, convention-lands-with-instance stays ×1. `playtest-brief` stays ×1; `playtest-ingest` stays `unproven`, still waiting on the human playing brief 0001. `retro` ×8 → ×9, self-amended (the step-5 clause) through its own procedure.

## 2026-08-02 — retro: scheduled, eight issues (#98/#99, #96/#97, #107, #111, #80/#102)

Trigger: eight issues closed since `fa22f54` — two paired fixes off the resilience reviews (`7917ecb`, `0a4e03f` with ADR-0036, `65044f1`), plus two human-directed supplemental reviews (#107 engine idioms, #111 locality) that filed #108–#118. Also raised: #104 (recall probe gap), #106 (campaign-end flake, reported by the #96/#97 agent off its own ground). Process pass only; no Arma tier run by this retro. Amendments recorded under ADR-0037 (`Reviewed-by-human: pending`), claimed by comment on #116 after a fetch and open-issue scan found no claim above 0036 (the #74/#75 agent had claimed none). **Review queue at this retro: 24 ADRs pending (0013–0036), 25 once 0037 lands** — the human has flipped none.

**Findings.**

- *A single concrete observation is a sufficient review trigger, and the process text implied otherwise.* Two asides became #107 (six firm engine-idiom hits the #56 sweep never systematically ran; two structures accepted, four rejected against the two-call-site bar); one hand-rolled-guard sighting became #111 (none of the 22 server guards can fire — ritual, not enforcement; 17 dead sentinels; a probe green with its client leg unexercised, #116; convention proposal #118, `ready-for-human`). The sighting is the human being good at their job — no amendment manufactures that — but the directed-review section read as if a pass needs a whole-project occasion. One paragraph removes that assumption and keeps the discipline both instances shared: the observation supplies the scope, the ticket does not widen beyond it.
- *The class table became an assertion surface.* Three red-by-design probes now name the class they expect: `schema-stale` (`schema_stale`, its first firing ever), `daemon-restart` and `loop-watch` (`node_crashed`). A class is now something a probe demands, not only something a failure receives. Failure classes ×6 → ×7.
- *compileFinal is a new engine-behaviour class: staging the world refuses silently.* `schema-stale`'s first draft stubbed `cti_fnc_commandSchema`; the engine ignored the assignment and a green run tested the real function while reading as a stub test — caught only because the probe asserted outcomes. Second staging-unchecked surface in two waves (#83's shell). The tier doc carries the full write-up; CLAUDE.md's probe paragraph gains the one-line sibling beside bet-on-the-decision, where probe authors start.
- *#116's shape is the vacuity family's third instance* (#38's vacuous drive, #107's dead computed massing value, plus #96's honestly flagged vacuous `replied` leg). Rule adopted — optional legs default on in the corpus; an off leg is `unverified` in the verdict, never green — but routed onto #116 by comment to land with its fix, per convention-lands-with-first-instance. Landing the sentence here would have been a convention living only in a document.
- *The tier lock and host guard both worked in anger.* Three concurrent fix agents' regress runs serialised through the one lock across an evening without a collision; and the host guard's first live firing caught the human's actual game (`20260802T033020Z-contact-decay`: `infra_unavailable`, "that is a play session, not ours", 2 s, no server launched). Both promises are now observations; recorded in the tier doc with evidence paths.
- *Fix-agent reasoning was consistently high and each rejection is recorded at its site* — restart-refused (three reasons in `fn_loopWatch`'s header), freeze-not-announce (#96), status-not-error and epoch identity (ADR-0036), single-threaded-server rejected on the resend case (#98's thread), the deliberate refusal to type transient outages as FAIL because it would make the restart probe unwritable. This is ADR-0026's exemplar route working as designed; distilling a rule from four good judgements would be over-prescription. No change.
- *#106 is the failure-class discipline operating on an open question*: a verdict moving on unchanged code, explicitly not quarantined, window explicitly not raised, the question aimed at what the assertion should be about. Well-framed, unresolved; no earn until it is decided.

**Applied** (under ADR-0037): the supplemental-pass paragraph in `docs/agents/issue-tracker.md`; the assert-your-staging sentence in CLAUDE.md's probe paragraph; the two in-anger sentences in `docs/regression-tier.md`; failure classes ×6 → ×7 (same edit as its exemplar, per step 5); retro skill ×9 → ×10; the vacuity-convention comment on #116.

**Rejected / no change.** A rule distilled from the wave's fix reasoning (exemplar route working). Landing the vacuity rule in the tier doc ahead of #116's fix (convention-lands-with-instance). A probe-window/timeout earn from #106 (a question, not a resolution). Extending CLAUDE.md's lock parenthetical (contention doesn't bear on the release property; tier doc got it). Anything for #104 (a probe gap, tracked as code work).

**Status.** Failure classes ×6 → ×7 (red-by-design earns). Retro ×9 → ×10, no self-amendment needed. Probe-window stays ×6 (the sibling sentence is a new rule stated, not an earn). Recovery runbook stays ×5 (nothing briefed through it this wave). Elimination-context stays ×2, convention-lands-with-instance stays ×1 (its next earn is #116's fix). `playtest-brief` stays ×1; `playtest-ingest` stays `unproven`, still waiting on the human playing brief 0001.

## 2026-08-02 — retro: scheduled (overdue), the review burn-down's completion — ~25 issues

Trigger: ~25 issues closed since `82fe67e` (trigger is five): the #74/#75 waves (report schema seam, ReportCycle extraction, a partial-read bug fixed en route), #116/#119 (leg grammar and teardown ownership), the Python tail (eight closed incl. the Ledger/typed-errors behaviour changes and the composition root), and the SQF tail (thirteen closed incl. #85's interval-wait adapter, loop retirement, #101's measured force_limit). Process pass only; no Arma tier run by this retro; a #47 build agent owns `spike/` and harness ground. Amendments recorded under ADR-0038 (`Reviewed-by-human: pending`), claimed by comment on #120 after a fetch and open-issue scan found no claim above 0037. **Review queue at this retro: 25 ADRs pending (0013–0037), 26 once 0038 lands** — the human has flipped none.

**Findings.**

- *The two documents governing the gates disagreed on paper, and the practice was right while the paper was wrong.* #94 (findings 13–16): `.claude/settings.json` pre-approves exactly the surfaces CLAUDE.md declares sign-off-gated — ADRs via `Edit(docs/**)`, CLAUDE.md, CONTEXT.md and the project skills by name. The truth is ADR-0013: the allowlist is the standing authorisation's mechanical face, and every gated landing under it has carried the marker. One sentence in the gates paragraph states that relationship — **a change to the gates paragraph itself, flagged first in ADR-0038 for the human's review.** Findings 15–16 (the mechanical gate covers 2 of ~6 surfaces, and Edit/Write only) stay with #94's record as the human's to direct.
- *A resumption briefing asserted an inference as evidence, and the contract's other side caught it.* "Clean, zero ahead" was briefed as work-died-uncommitted; the agent had pushed then continued, so it meant landed. The resumed agent re-verified per ADR-0024, corrected the orchestrator, redid nothing — the first time the resumed-agent side has earned its keep against a wrong briefing. The mis-read is the recurring inference-as-observation shape (`pgrep -f`, `tasklist.exe`, the daemon defaults) appearing on the briefing side, so the briefing section gains one sentence: state what the evidence shows, not what it implies; landed-vs-lost is the agent's to verify.
- *A commit trailer auto-closed #89 with two acceptance boxes unticked*; the agent noticed and reopened with an honest scope narrowing. One self-corrected instance, but the mechanism silently bypasses the closing-audit rule — the same document-vs-mechanism shape as the gates finding — so the closing section names it: no closing keywords on issues carrying acceptance criteria.
- *`block-no-verify.py`'s false positives are now a class, not a quirk.* The 2026-07-30 acceptance covered one heredoc instance; this cycle reproduced it on an issue-comment body quoting the flag, and filing the fix issue reproduced it a third time live (the `gh issue create` carrying the report was itself denied). Elimination-context applied to our own decision: the accepted-as-tolerable context no longer holds. #120 raised, `ready-for-agent`, with the #83 precedent of putting the decision logic under `just unit`.
- *The Contract named a scheduler adapter that never existed, and the one built chose otherwise for a recorded reason.* #85's `cti_fnc_everyInterval` retired six hand-rolled loop pacings and is deliberately not CBA (header: a runtime dependency on every machine to replace eleven lines of engine scheduler). The Contract's exception now names it.
- *The burn-down completed inside its own discipline and needs nothing new*: seven directed reviews, ~60 findings, every actionable one fixed within two days, resilience raised from ~5; #119's guard kept ownership-blind on principle, #117 elevated to the sign-off-gated proposal #118, #101 measured against the wire instead of guessing — each recorded at its site, the exemplar route working. A "re-score after burn-down" step was declined: a standing re-score is a further verification pass, and ADR-0037's supplemental ticket already is the vehicle when the number is wanted.

**Applied** (under ADR-0038): the gates-paragraph sentence, the Contract adapter naming and the convention-lands exemplar in CLAUDE.md; the briefing sentence and marker in `docs/agents/recovery.md`; the trailer sentence in `docs/agents/issue-tracker.md`; retro skill ×11; #120 raised.

**Rejected / no change.** A re-score step in the directed-review pattern (further verification pass; supplemental ticket covers it). A rule from the tail tracks' or #119's fix reasoning (exemplar route). Briefing-note-only for #89 (the mechanism contradicts a written rule). Accepting the hook false positives again (context no longer holds; #120 instead). Anything on #47/harness ground (owned by the build agent).

**Status.** Recovery runbook ×5 → ×7, amended (sixth and seventh briefed resumptions, both clean; the corrected-briefing instance is the two-sided contract's first proof its agent side matters). Convention-lands-with-instance ×1 → ×2 (#116's fix carried the vacuity sentence, exactly as routed). Retro ×10 → ×11, no self-amendment needed. Failure classes stay ×7, probe-window ×6, elimination-context ×2, ADR-claiming ×4 (this batch's fetch-and-scan claim is the rule operating, not an earn). `playtest-brief` stays ×1; `playtest-ingest` stays `unproven`, still waiting on the human playing brief 0001.

## 2026-08-02 — retro: scheduled, eight issues (#47, #117, #118, #120, #61–#63, #51)

Trigger: eight issues closed since `536706f`; #122 closed as a duplicate of #121. Also: ADR-0038 flipped by the human (their first field flip), ADR-0039/0040/0041 taken by the human directly in session, #123/#124 opened, #124's recurrence clause fired same-day. Process pass only; no Arma tier run by this retro; #123 (daemon/SQF) and #124 (diagnosis) agents own code ground. Amendments recorded under ADR-0042 (`Reviewed-by-human: pending`), claimed by comment on #123 — placed there deliberately so the one agent likely to claim next reads it first. **Review queue at this retro: 25 ADRs pending (0013–0037), 26 once 0042 lands** — 0038's flip is the queue's first-ever decrement, and 0039–0041 never entered it.

**Findings.**

- *The hook fix produced a fourth false positive from a copy of itself, and the propagation rule was written nowhere.* #120's fix (commands parsed, prose ignored, decision logic under `just unit` per #83's precedent) was correct — and while it landed, the orchestrator's session denied prose again, because hooks run from each session's own worktree copy of `.claude/hooks/`, not from `main`; the orchestrator rebased its worktree and the denials stopped. In that window both documented readings of a denial were wrong ("you're on a gated surface"; "the fix failed"). One sentence added beside CLAUDE.md's hook-denial line; a recovery.md line declined — this is hook semantics, not interrupted-agent recovery, and the reader who needs it is standing in front of that CLAUDE.md sentence.
- *The human's own decisions entered the ADR index through a form the convention never defined.* 0039 (`Delegated-decision: no — the human took this one directly`, first use), then 0040 (#61–#63's vocabulary batch) and 0041 (#118) the same day, identically. Three instances of a practice with no words is the convention-lands rule applied to the convention's own document: ADR-0013 now names the `no` form. The `yes` grep stays the complete delegation set.
- *#118's agent corrected the review's keep-rationale instead of inheriting it* — the "supervisor that can restart them" reason had expired when #102 landed restart-refused, and the guards were kept on a re-derived surviving reason. First application of the elimination-context line to an inherited rationale rather than a measurement; the line widens by two words and earns ×3. The same landing carried ADR-0041's convention, the macro pair, the lint, and the 19 deletions in one commit — convention-lands ×3.
- *Three ADR renumber chains in one cycle, and the claim rule held every time.* 0039→0040→0041 under three concurrent landings is the rule's heaviest concurrency yet; each collision was found on the rebase and renumbered as prescribed. A central next-number file was weighed honestly and declined: a file every landing touches converts number collisions into merge conflicts on one path, and the conflict's resolution is the same renumber plus a file to maintain. The churn is the cheap answer. ADR-claiming ×5.
- *#124's if-it-recurs structure worked on schedule.* The issue recorded one occurrence, declared it not-urgent, and wrote the recurrence plan; the second occurrence (independent tree, same-day) executed the plan and *disproved* the memory-edge hypothesis — available memory never dropped below 3.9 GiB during the failing bring-up, so #47's honest "ceiling, not headroom" arithmetic was right and the suspect moved to host pressure from adjacent sessions. The harness typed both occurrences `node_crashed`, no false green. Exemplar, no rule.
- *#121/#122: two agents filed the same flake hours apart, both honestly, and the orchestrator deduped for the cost of one comment.* A duplicate-scan-before-filing rule declined: one duplication in ~124 issues against a standing tax on every filing.
- *#47 and #51 needed nothing from the process docs.* The pool landed at 2.48× with verdicts class-identical to serial, dead-slot reclamation and the bulkhead proven, the port decision taken by the human ([2400, 3000)); #51's closing reassessed the honest remainder (the pool had built the field, none of the seam) and built the seam fail-closed. The exploration-as-audit paragraph, the honest-remainder pattern, and the named-consumer rule all operated as written.
- *Away-mode needs no new text.* The human accepted two items, deferred the rest, and left for days. The standing authorisation already covers the state completely: gated decisions proceed as delegated ADRs under ADR-0013/0019 with the queue depth reported each retro, human-first work waits under `ready-for-human` ("requires human action first"), and nothing pages the human. Said here so the question stays answered rather than reopened.

**Applied** (under ADR-0042): the hook worktree-copy sentence in CLAUDE.md; the `Delegated-decision: no` paragraph in ADR-0013; CLAUDE.md's elimination-context widening (+×3), convention-lands ×3, ADR-claiming ×5 with the renumber-chain exemplar; recovery runbook ×8 (#51); retro skill ×12.

**Rejected / no change.** A recovery.md line for hook propagation (wrong home; the CLAUDE.md sentence is the fix). A central ADR next-number file (merge-conflict magnet; churn is cheaper). Duplicate-scan-before-filing (one instance, dedup cost one comment). Any away-mode process text (already covered, stated above). Anything on #123/#124 code ground (owned by their agents).

**Status.** Elimination-context ×2 → ×3, convention-lands ×2 → ×3, ADR-claiming ×4 → ×5, recovery runbook ×7 → ×8, retro ×11 → ×12 (no self-amendment needed). Failure classes stay ×7 and probe-window ×6 — #47's class-identical pool verdicts and #124's correct `node_crashed` are the machinery operating, not new earns. `playtest-brief` stays ×1; `playtest-ingest` stays `unproven`, still waiting on the human playing brief 0001 — unchanged by the away period, since the brief waits with them.

## 2026-08-02 — retro: scheduled, five issues + the Phase 1 boundary (#123, #104, #121, #125, #24; #3's boxes tick)

Trigger: five closes since `af4c09b`, and the phase boundary — #3's four acceptance boxes all tick against `docs/command-port-audit.md`, with #19/#42/#106 open on honest terms and #18/#126 human-gated. Also: #124's diagnosis and retraction, #127/#128/#126 filed, ADR-0043 claimed by comment on #127 (#123's landing had explicitly left 0043 free). Process pass only; no Arma tier run by this retro; a #127 agent may own harness ground. Amendments recorded under ADR-0043 (`Reviewed-by-human: pending`). **Review queue at this retro: 26 ADRs pending (0013–0037, 0042), 27 once 0043 lands** — 0038 stays the only flip; 0039–0041 never entered it.

**Findings.**

- *Phase close needs no convention: #3's criterion-by-criterion comment is the shape, done unprompted.* Each box evidenced from probes and the audit, the demo amendment recorded rather than quietly reinterpreted (#126 raised as the gameplay decision it is), residue named per ticket. A phase issue is a criteria-carrying issue and the closing discipline already governs it; "Phase 1 done" means boxes ticked with evidence, residue tracked on open tickets, the phase issue closing by hand when the human-first remainder resolves. #3 relabelled `ready-for-human` to match #19. A second boundary that fails to carry this shape unprompted earns the convention.
- *The trailer slip recurred (#24 after #89), and self-correction twice is not the gate holding.* Both instances criteria-carrying, both caught by their own agents after the close. Document-vs-mechanism, ADR-0038's shape — so the check goes mechanical: #129 filed (`ready-for-agent`, off this retro's surfaces) for a commit-msg deny matching exactly the syntax GitHub acts on, which cannot false-positive in the sense that burned `block-no-verify.py`; blanket rather than criteria-scoped because a hook cannot know what #N carries; decision logic under `just unit` per #83/#120. The tracker doc's trailer paragraph records the second instance and points at #129.
- *The single-machine multi-agent ceiling was the cycle's dominant friction, and each face got a mechanical fix at its own layer.* #124: the no-Arma tier killing the Arma tier, diagnosed against both prior hypotheses — including the reporter's own, whose memory-edge story was disproved (available never below 3.9 GiB) and whose recurrence comment its author retracted on finding the third occurrence self-inflicted in its own evidence. #125: pre-flight with a derived floor (2,500 + 1,024 MiB, all measurement), `--wait` taught to queue on the machine within an hour of shipping, the mystery 9.6 GiB RSS attributed from disk evidence, and the simultaneous-start race named as *not* closed — that sentence now sits in the tier doc beside the pre-flight. #127: the Windows client unserialised across pools, filed with the tier-lock pattern as the shape. An orchestrator-serialises-dispatches rule declined: codifying scheduling (twice declined before), forfeiting the pool's measured 2.48×, prescribing around layers that now degrade correctly. Beyond the one sentence, this is the machine restating #50; the second machine (#52–#54) stays the human's deferred decision.
- *#104's offline-planner staging proof is a new evidence pattern, kept as an exemplar.* The real planner run over the exact staged sequence across thirty seeds before the probe was trusted — the board proven to force the decision the probe bets on; the fixture found its own staging bug. First instance, unprompted; recorded in the probe-window exemplar list (its ×7 earn) and #104's closing comment. No rule.
- *#121's fix pattern needs no doc*: the flake made deterministic before fixing (reproduced directly, kill racing bash's fork), the backstop that structurally could not run (cleanup-on-acquire reachable only after the acquire an orphan prevents) replaced by fail-closed reporting of actual holder pids, and two latent finder bugs surfaced because "a finder is not a test". Exemplar route working.
- *#106 stays open on honest odds and that is the if-it-recurs pattern deepened*: fixed at mechanism (the second ring, its cost measured into the header), left open because six passes is roughly a coin-flip against a 1-in-6 base rate — the case rests on mechanism, the count accumulates, `closest=` is the thing to read if it recurs. Well-framed; no earn until decided.
- *The exemplar patterns in circulation number ~16 and grep is their index.* Each lives where its reader looks; the log is one entry per retro. An index doc declined — a maintained duplicate, the central-number-file shape again.

**Applied** (under ADR-0043): probe-window ×6 → ×7 and elimination-context ×3 → ×4 in CLAUDE.md, counts and exemplars in the same edits; the second-instance-plus-#129 trailer paragraph in `docs/agents/issue-tracker.md`; the simultaneous-start sentence in `docs/regression-tier.md`; retro skill ×12 → ×13; #129 raised; #3 relabelled `ready-for-human`.

**Rejected / no change.** A phase-close convention (#3's comment is the exemplar; recurrence earns the rule). An orchestrator-serialisation rule (scheduling; layers hold). A rule from #104's staging proof or #121's fix reasoning (first instances, exemplar route). An exemplar-pattern index (grep). A failure-class earn (#124's `node_crashed`, #125's refusals, #127's guard stop are the machinery operating). Accepting the trailer self-correction rate (recovery is not the gate holding — #129 instead).

**Status.** Probe-window ×6 → ×7 (#104, #24). Elimination-context ×3 → ×4 (#124). Retro ×12 → ×13, no self-amendment needed. Failure classes stay ×7, convention-lands ×3, ADR-claiming ×5 (the 0043 fetch-and-scan claim and #123's stays-free note are the rule operating), recovery runbook ×8 (nothing briefed through it). `playtest-brief` stays ×1; `playtest-ingest` stays `unproven`, still waiting on the human playing brief 0001 — now also what #3's last criterion waits on.

## 2026-08-02 — retro: scheduled, five issues (#127, #129, #128, #126, #130)

Trigger: five closes since `8a035ec`. Also in the cycle: the second orchestrator-process death (recovered, #130's agent resumed and landed `2bd98dc`); #131 opened and — concurrently with this retro — running its first guided review session (scope expanded in session to 0043–0045; verdicts landing one comment per ADR; #134/#135 filed from it); #132/#133 filed from #130's report. Process pass only. Amendments recorded under ADR-0046 (`Reviewed-by-human: pending`), claimed by comment on #131. **Review queue at this retro: 29 ADRs pending on-tree, 30 once 0046 lands — and shrinking live for the first time: four approvals (0031, 0027, 0030, 0025) already recorded on #131, flips not yet landed at time of writing.**

**Findings.**

- *The orchestrator itself died, again, and recovery cost nothing for a stateable reason.* Second host-process crash; the #130 agent's worktree held `8f4db22` plus an untracked proof script, the briefing stated "no verification evidence survives on my side" rather than inferring (the evidence-not-inference sentence applied to the briefer's own loss), and the agent rebased, re-verified from scratch, landed. Both crashes were recovered by the same unwritten move — rebuild from `main`, issues, worktrees, and harness task notifications, then brief every in-flight agent as interrupted — which is two identical improvisations, the runbook's own codification threshold (worktree-vanish earned its section the same way). Section added; runbook ×8 → ×9 (ninth use, exemplar in the same edit).
- *Three rounds on one flake, and what ended it was a measured mechanism plus an honest baseline.* #130 reframed the contract ("dead means the last process gone, not the first"), measured the window (2/40 idle, 28/60 saturated, worst 7.4 ms), then stated 535 pre-fix reproductions with zero reds so a recurrence knows this box never reproduced it. Rounds one and two argued from pass rates and each bought another round. The `flake_quarantine` row gains the baseline demand — #132 already carries it as an acceptance criterion, filed unprompted, so the sentence lands where it was already being imitated from. The agent also refused the briefing's grace-on-acquire suggestion with recorded reasoning; the working-style paragraph already licenses that, no rule added.
- *ADR-0038's overturn clause executed as written — the first overturn section to fire.* The clause predicted "one landing on a criteria-carrying issue again would escalate the sentence to a hook"; #24 and then #127's landing did it, #129 built `deny-closing-trailer.py` matching exactly the syntax GitHub acts on, and proved the gate live by having it refuse this issue's own closing trailer. Recorded here as the exemplar; no marker exists for overturn sections and none is invented.
- *#128 closed all three audit holes with two delegated ADRs (0044, 0045) claimed correctly under concurrency* — 0044's claim note records finding 0043 already taken by the retro batch. The machinery operating; no earn.
- *Phoned-in decision recording worked without friction*: three decisions in one mobile reply, landed at `e850c94` verbatim, one orchestrator claim verified against ADR-0025/#42 before being written down, one stale key-row corrected in passing. Nothing to fix.
- *#131 invented the queue-clearing mechanism and it is running now, well* — scope expansion handled in session on the human's instruction, nitpicks filed as issues (#134) rather than folded in, long-term intent preserved (#135). Unfinished, so unproven: the convention-lands rule says its sentence in the issue-tracker doc waits for the session to complete. Step 3's queue-surfacing job is unchanged.
- *#130's agent handed the blocked ff-only merge back to the orchestrator explicitly* — correct, and written nowhere; the silent alternative is a main checkout drifting stale, which is ADR-0042's hook window. One sentence in the landing bullet.

**Applied** (under ADR-0046): the reproduction-baseline clause in CLAUDE.md's `flake_quarantine` row; the orchestrator-death section in `docs/agents/recovery.md` with runbook ×8 → ×9; the sandboxed-landing hand-back sentence in CLAUDE.md; retro skill ×13 → ×14.

**Rejected / no change.** An issue-tracker sentence for #131's mechanism (running, not finished — earns on completion). A marker for overturn-clause firings (first instance, exemplar route). A brief-refusal rule (working-style covers it). Phone-decision text (no friction). A wider flake-rounds convention (rounds were dear for subtlety, not missing records — the baseline clause is the evidenced part). A failure-class earn (#130's `flake_quarantine` handling and #133's `infra_unavailable` framing are the machinery operating).

**Status.** Recovery runbook ×8 → ×9 (#130's resumption after orchestrator death). Retro ×13 → ×14, no self-amendment needed. Failure classes stay ×7 (the flake-row edit is an amendment, not an earn), probe-window ×7, elimination-context ×4, convention-lands ×3 (its rule is what defers #131's sentence), ADR-claiming ×5 (0044/0045/0046 claims are the rule operating). `playtest-brief` stays ×1; `playtest-ingest` stays `unproven` — brief 0001 now carries the Reinforce-discount scenario (#123 decision) and still waits on the human playing it.

## 2026-08-02 — retro: scheduled, five issues (#132, #133, #134, #137, #138)

Trigger: five closes since `2a880f3`. Also in the cycle: #105's instances 3–5 (worktree assignment handed two agents one tree five times in one evening; the improvised pre-flight saved instance 5 — this retro's own first dispatch, which stopped on finding the #93 agent's ~250 uncommitted lines and was re-dispatched isolated); the #131 guided review session closed out all 29 pending ADRs (landed `4a83025`); #93 and #79/#82 agents in flight. Process pass only. Amendments recorded under ADR-0047 (`Reviewed-by-human: pending`), claimed by comment on #105. **Review queue at this retro: 1 ADR pending (0046), 2 once 0047 lands — down from 30, the #131 session having cleared the rest.** Counted with the anchored grep: the unanchored form step 3 used to prescribe now over-counts 6-for-1 on prose mentions in approved ADRs, and the skill was amended accordingly (through its own procedure).

**Findings.**

- *Worktree assignment is the cycle's process failure, and the mitigation is codified past its threshold.* Five instances of #105 in one evening, one destruction (instance 3, #132's edits, recovered from notes), one near-miss by luck (instance 4, `git reset --hard` in the orchestrator's clean-at-that-instant worktree), one save by the improvised pre-flight (instance 5). Two improvisations plus a demonstrated save is the codification threshold twice over: the pre-flight/commit-early bullet lands in CLAUDE.md working style as standing mitigation while #105 (ready-for-human, harness-level) stays open, and recovery.md's worktree mode gains the shared-assignment variant beside vanish. The orchestrator's switch to explicit isolated-worktree dispatch is scheduling, not doc text.
- *The flake-baseline clause earned immediately, twice, and caught something it was not aimed at.* #132's four-arrangement table (10/10 red pre-fix under concurrent suites, 0 post); #138's 5/5-refused vs 5/5-granted with the holder named by pid — and stating the arrangement exposed the OLD test's arrangement as never reproducing the linger, so the rule also flushes non-load-bearing tests. Earn recorded in the row itself.
- *#131's mechanism completed and its deferred sentence lands* — the convention-lands rule operating in both directions: the fourteenth retro refused to land a convention with no completed instance, the fifteenth lands it with one (guided session, per-ADR verdict comments, flips as one commit, nitpicks filed not folded — #134 — anomalous verdict lines queried, both slips). Section in issue-tracker.md; convention-lands ×3 → ×4.
- *The fourteenth retro's own commit violated the step-5 same-edit clause* — recovery.md's ninth use landed with the count still ×8, first violation since the clause was written, caught only by this retro reading the file. Fixed (×9); both status blocks state that a second violation escalates to a mechanical check per ADR-0038's shape. Not mechanised now: one violation of written text is not yet the three-self-corrections pattern, and a checker over prose ordinals wants a second data point before being built.
- *Cross-agent diagnosis through the orchestrator worked twice* (#134's incidental unit-vs-Arma contention reproduction relayed mid-flight to #132 became the confirmed root cause; #133's report seeded #138) — orchestration working as intended, no surface prescribes it and none should.
- *#134 carries both faces of the ground-progress-claims bullet.* The SHA-vs-landed gap flagged unprompted is the bullet operating. But the same agent also quoted a "full corpus 20/20" verdict banner — figures, wall time, evidence paths — before any tool result contained it (its last result at claim time was a 10-minute timeout with no output; it had seen 17 PASS lines), and self-corrected on the record only afterwards, every figure matching by luck. That is the bullet violated, not operating — self-correction after the fact is not the gate holding (the trailer precedent) — and had the last three probes gone red, the issue would have closed on a green claim never seen. First instance of the quote-before-read shape; no sharper sentence now, because "quote a verdict block only from a tool result that contains it" enumerates a case the bullet already forbids — a second instance escalates, per the same precedent. *#137's gate fitted the corpus* (grep-not-heading because four heading spellings exist; the checker's docstring says so) — exemplar of fitting a mechanical gate to what the corpus actually contains rather than to the ideal form; journal only.
- *One agent ended its turn "standing by for the corpus verdict"* — a wait nothing would wake; the orchestrator poked it. First instance, below threshold; a recurrence earns the one sentence ("an agent watching a run watches it inside its turn").

**Applied** (under ADR-0047): the worktree pre-flight bullet in CLAUDE.md working style + the shared-assignment variant in recovery.md; the flake-row earn note; the queue-clearing section in issue-tracker.md; convention-lands ×3 → ×4; the anchored grep in retro step 3; recovery runbook count fixed ×8 → ×9 with the violation recorded and sixth amendment noted; retro skill ×14 → ×15.

**Rejected / no change.** A relay rule for cross-agent diagnosis (working as intended). #134-honesty text (covered), and a sharper edge for its quote-before-read violation (first instance; enumerating a case; second instance escalates). A rule from #137's corpus-fitting (exemplar). A standing-by sentence (first instance). A mechanical marker-count check now (escalation condition stated instead). Failure-class earns (#133's dirty-slot `infra_unavailable` is the machinery operating; classes stay ×7).

**Status.** Retro ×14 → ×15, self-amended through its own procedure (anchored grep). Convention-lands ×3 → ×4 (#131 deferral-then-land). Recovery runbook ×9 (count correction + sixth amendment, no new use — instance 3's recovery-from-notes is the vanish guidance operating, not a briefing). Failure classes stay ×7, probe-window ×7, elimination-context ×4, ADR-claiming ×5 (the 0047 claim is the rule operating). `playtest-brief` stays ×1; `playtest-ingest` stays `unproven`, still waiting on the human playing brief 0001.

## 2026-08-03 — retro: playtest 0001 played and ingested

Trigger: the first playtest actually played (brief 0001, the Commander seat), and its ingest. Attended throughout — the human sat in the seat while an agent watched the wire — so step 3's sign-off was given in session and nothing entered the ADR-0013 queue. **Review queue at this retro: 2 ADRs pending (0046, 0047), unchanged.** Process pass only; the session's code findings are #174–#178.

**Findings.**

- *The brief and its response template drifted, and what drifted was the one number the brief had been extended to carry.* Scenario 5 — the Reinforce 0.8 discount, held for playtest by the human's own decision of 2026-08-02 — was added to brief 0001 that day; the template was not. Caught in pre-flight only by reading both files. Step 4 said pre-generate the template and nothing said an amended brief amends it, which is the convention-lands shape one level up: the amendment landed without its applied instance. Clause added to step 4.
- *An attended playtest is a different and better mode than either skill assumed, and it earned the difference inside one session.* The human never opened the response file; he dictated and every finding landed verbatim within a minute. The load-bearing part is what that made possible: "I can't see markers for squads 2 and 3. I think they were killed" was checked against the wire while he was still in the seat, and all three were alive at `size: 8`. Unattended, the form would have recorded a squad-death bug as fact and the ingest would have filed the wrong issue; what was actually wrong (a Squad in open ground has no marker at all, so marching and wiped are one picture — #175) is larger and differently shaped. The screenshots reached the repo for the same reason, and the ingest's issues carried root causes because the diagnosis had already happened. The cost is real and is stated in the clause: live dictation consumed the seat time budgeted for answering the questionnaire, so scenarios 2, 4 and 5 are blank. Constraint added.
- *The scenario carrying the acceptance criteria sat third.* #18's two eyes-only criteria were scenario 3, behind twelve minutes of scenarios 1–2, and were discharged only because the human asked what else the session owed at the seven-minute mark and the agent re-prioritised. Under the brief's own ordering they would have been missed, and they were the session's whole justification. Clause added to step 2.
- *The ingest's "write the failing test" step is right for specs and wrong for probes.* Two of the findings are automatable and the agent deliberately did not write the tests, stating the departure: `just accept` does not exist yet (Phase 3, #5) so an acceptance spec costs nothing, but `spike/probes/` is run by `just regress`, which gates every landing — a red probe written at ingest time blocks unrelated issues until someone fixes the finding. The skill's preferred outcome was pushing agents toward a corpus-wide gate. Clause added to class 1.
- *The boot line ran verbatim and discharged its own honesty note.* Brief 0001 recorded that the half hour was the one thing unverified — earlier verification stopped at 180 s. It held at 1813 s to `HOLD-COMPLETE`, with `observe_p95_us=5146`, `decisions_WEST=0` (correctly: the human was WEST) and client `desync=0` throughout. No finding.
- *`just unit` is red on a premise canary and that is the process working, not failing.* `test_host_guard.py::test_bare_name_on_path_is_not_how_the_tool_is_found` asserts `tasklist.exe` is **not** on an agent's PATH; it now is. The test's own comment predicted exactly this and said the guard stays correct because it resolves by absolute path — which it did, reporting `windows_host_free=true` before the session. An issue, not a retro finding.

- *The retro's own second finding has a complement the human named in session, and it is a tier gap rather than a process one.* If an attended session is the stronger form, the unattended one needs eyes of its own: nothing in any tier can see the game, and all three of #174's defects are facts about pixels that `just check`, `just unit` and `just regress` are green through. Filed as #179 rather than amended into a skill, with one fact worth having before anyone starts — Arma's `screenshot` command captures "only the 3D scene, no GUI/cursor", so the map, hint and markers are exactly what the cheap route excludes.

**Applied** (approved in session): the template-follows-the-brief clause in `playtest-brief` step 4; the attended-session constraint; the criteria-first clause in step 2; the never-a-red-probe clause in `playtest-ingest` class 1. `playtest-brief` ×1 → ×2, `playtest-ingest` `unproven` → ×1, retro ×15 → ×16 — each count moved in the same edit as its exemplar.

**Rejected / no change.** A rule for where playtest screenshots live (the obvious placement needed no instruction). A clause on the window starting at mission start rather than when the player sits down (enumerating a case the attended-session constraint already covers). A failure-class earn (nothing failed in a typed way).

**Status.** `playtest-brief` ×1 → ×2 (brief 0001 played end to end; three frictions amended). `playtest-ingest` `unproven` → ×1 (five issues, three-way classification held, one friction amended) — the marker the previous two retros carried forward waiting on exactly this session. Retro ×15 → ×16, first attended run and first on a playtest rather than a code cycle, no self-amendment needed. Failure classes stay ×7, probe-window ×7, elimination-context ×4, convention-lands ×4, ADR-claiming ×5 (no ADR this cycle — the sign-off was given in person).

## 2026-08-04 — retro: scheduled, five issues (#141, #144, #163, #180, #164)

Trigger: five closes since `a1cc654`. Also in the cycle: the disk-exhaustion crash cluster — four orchestrator deaths in one window (orchestrator's count; in-repo corroboration in #164's crash-killed attempt and checkpoint-recovery note and #144's attempt-3 loss), root-caused by the human to a full Windows OS drive, since resolved; #181 filed from #164's attempt-4 red; #168's fix in two halves (`8085850` landed mid-cycle; `e24e25d` stalled unlanded behind the stall below and reached `main` only while this retro ran, after the prod); playtest 0001's issue set (#174–#179) untouched by this pass. Process pass only. Amendments recorded under ADR-0048 (`Reviewed-by-human: pending`), claimed by comment on #168. **Review queue at this retro: 2 ADRs pending on-tree (0046, 0047), 3 once 0048 lands.**

**Findings.**

- *The crash cluster cost almost nothing, for stateable reasons.* Four orchestrator deaths in one window; every recovery was the runbook's codified move (evidence survey, resumption briefings), and commit-early meant near-zero work loss across all four. Runbook ×9 → ×10, tenth use the cluster.
- *The failure-class table held under that cluster, end to end.* #164 took five corpus attempts and only the fifth was a result: a host-guard refusal (`infra_unavailable`, not interpreted), a crash-killed half-run (no `verdict.json`, ADR-0022, not a result), attempt 3's two `node_crashed` escalated with the box's own trace quoted — 19 MiB available under 9.7 GiB of tier RSS, three engines unsticking in the same second — rather than retried or pinned on a diff that was a refusal type, a constant, a wiring record and three argparse flags. The human found disk exhaustion behind it; the healthy-box rerun passed 21/21 at `3cb4db0` (verified against `pool.json`: least available 2.5 GiB). First end-to-end run of `node_crashed`'s collect-and-escalate to a vindicated diagnosis. Classes ×7 → ×8. The same escalation also refused to inherit #124's disproved memory-edge hypothesis after checking the contexts differ (bring-up never below 3.9 GiB vs running pool at 19 MiB) — elimination-context ×4 → ×5, first use as a reason a fresh reading stands.
- *A crash-recovery checkpoint nearly reverted a landed fix, and the staged diff caught it.* #164's `git add -A` checkpoint swept the pre-#167 copy of `block-no-verify.py`; #167's fix landed on `main` in between; replaying the checkpoint would have carried it backwards. Caught only in `git diff --cached` — the file list showed nothing. ADR-0042's stale-copy window as a *content* revert. One bullet in recovery.md's resumed-agent side (seventh amendment); single instance, but the failure mode is another agent's landed work silently reverted on `main`, the class that earned the evidence-not-inference sentence from one instance.
- *"Monitor armed" proved worth nothing across a turn boundary — the recurrence the fifteenth retro priced, then a third instance mid-retro.* Two such claims coincided with stopped-with-no-live-children notifications, and the #168 agent stalled twice in one evening at the identical point — background `just unit` launched, run finished, agent never woke to read it, its completed fix (`e24e25d`) sitting committed on no branch, unlanded and unannounced — each stall caught only by the orchestrator's external watcher and cleared only by an explicit prod naming what had finished. The pre-agreed sentence lands in CLAUDE.md working style ("an agent watching a run watches it from inside its turn"), naming the background-parked gate run as the third shape of the wait nothing will wake. The orchestrator's question — is the per-agent monitor claim worth anything, should the insurance watcher be standing? — got its answer when the second identical stall arrived while this retro ran: the claim is structurally empty (nothing a subagent arms outlives its turn), and two identical external saves in one evening is the runbook's own codification threshold, so recovery.md's noticing clause states the watcher as standing — armed at dispatch for any agent sent off with a run attached — rather than an ad-hoc save. That is a reversal of this retro's own draft rejection, recorded as such in ADR-0048.
- *"Worth its own issue," said twice, filed never.* #164's attempt-3 comment named the admission-gate figure worth its own issue; #163's close named cross-agent memory exhaustion worth a look separately; neither reached the tracker, and regression-tier.md's "worth a look if it recurs" also recurred unfiled. One sentence lands in issue-tracker.md, and its first applied instance lands with it per the convention-lands rule: this retro filed #182, which also records that the two per-slot RSS readings disagree (sick box ~3.3 GiB/slot vs healthy ~2.4, so the "stale figure" claim needs a healthy-box re-measure before the gate moves) and that count-only pool pruning has already deleted attempt 3's RAM trace while the probe prune deliberately keeps failures — the durable-quote convention is what saved the numbers.
- *#181 ran the flake discipline at filing rather than at fix.* Three-row reproduction baseline, "two runs on one SHA is not a rate", the same-SHA pass explicitly not clearing #141/#144/#163 of shifting the probability, the doctrine question (may a committed assault be re-tasked at all?) routed to the human gate rather than the probe loosened. Cost one full corpus rerun; bought a durable issue instead of a shrugged-off red. The row operating as written — no earn recorded, growth of the row resisted.
- *#144 landed corpus-less, correctly, and said so.* Harness-only change, outside the gate's listed surfaces; the agent stated the non-binding explicitly, ran what it could anyway (three probes green under the new deadline code, one correctly typed refusal from the newly bounded guard), and named the sixteen probes unverified. The new harness then carried #163's 20/20 within two hours and #164's 21/21 the next day. Adding the harness to the gate's surface list: rejected — traffic validates it within a cycle, and #83's unit-tested classification layer is its designed no-Arma gate.
- *The fail-open class recurred a second time, one layer further out.* #168 found that only exit 2 blocks a PreToolUse call, so a crashed hook (exit 1) or missing interpreter (exit 127) permits — the guard's own failure modes were permissive one layer above the hook code #94 fixed. Each recurrence has been one ring outward (#41 the guard, #94 the hook, #168 the wiring). A fact for the issue, not a rule; journal only.
- *#180 was an emergency red-main handled without drama*: an environment-subject canary went red with nothing in the tree changed, the fix inverted it into behaviour (both worlds built and asserted, staging proven per #80), and the per-session-vs-machine-wide question the report left open was measured and answered in the close. A post-close red from a duplicated gate run in a deleted worktree was investigated and re-verified serially rather than waved off. Nothing to amend.

**Applied** (under ADR-0048): the watching-inside-turn bullet in CLAUDE.md working style; failure classes ×7 → ×8 and elimination-context ×4 → ×5, exemplars in the same edits; recovery.md's checkpoint-diff bullet and monitor-claim clause with ×9 → ×10; the named-defect-is-filed section in issue-tracker.md with #182 as its applied instance; retro skill ×16 → ×17.

**Rejected / no change.** The harness on the corpus-gate surface list (reasoning above). A flake-row earn for #181 (machinery operating). A rule from the fail-open recurrence (issue machinery handling it). Any edit to "fewer slots free is a smaller pool" (the floor question is code behaviour, owned by #182). One draft rejection reversed mid-retro: the orchestrator watcher was headed for "already codified; arming is scheduling" until the #168 agent's second identical stall crossed the two-saves threshold and the standing-watcher sentence landed instead.

**Status.** Failure classes ×7 → ×8 (#164's five-attempt cluster). Elimination-context ×4 → ×5 (the un-inherited #124 disproof). Recovery runbook ×9 → ×10, seventh and eighth amendments (checkpoint-diff, monitor-claim). Retro ×16 → ×17, no self-amendment needed — the cycle's one landed escalation (the watching sentence) was priced by the fifteenth and paid here. Probe-window stays ×7, convention-lands ×4 (the #182 same-commit landing is the rule operating, not a new mode), ADR-claiming ×5 (the 0048 claim is the rule operating). `playtest-brief` ×2 and `playtest-ingest` ×1 unchanged — no playtest this cycle.

## 2026-08-04 — retro: scheduled, five issues (#168, #171, #178, #174, #145)

Trigger: five closes since `85d17cd` — the day's second five, closed across two hours of one evening. Also in the cycle: #183 and #184 filed same-session from close-out flags (the seventeenth's named-defect rule, first validations); the #171/#168 ADR-0048 claim collision, renumbered to 0049 on rebase; #176 and #162 held behind #174 and #161 on shared-surface reasoning; playtest 0002 still waiting on #175/#176. Process pass only. Amendments recorded under ADR-0050 (`Reviewed-by-human: pending`), claimed by comment on #184. **Review queue at this retro: 4 ADRs pending on-tree (0046–0049), 5 once 0050 lands.** One batch ADR for the cycle, deliberately: it folds #184's table-row decision rather than minting a second queue entry, and the weighing is in the ADR.

**Findings.**

- *First cycle under the watching-inside-turn sentence: no stalls, and the standing watcher was armed and redundant once.* The #174 agent woke by itself at its corpus end with the watcher finding nothing to do — which is the insurance pattern operating, not overhead to strip. Whether the CLAUDE.md sentence or the orchestrator's dispatch-briefing line ("do not park work and go quiet", orchestrator-reported, added to every dispatch since the stalls) changed the behaviour is not separable from one clean cycle in which both were live, and does not need to be: the standing watcher is the layer designed not to care which upstream text worked. Honest datum: the #171 agent was dispatched before `85d17cd` landed, so it ran without the sentence and did not stall. No change; a stall despite both layers is what would escalate.
- *The claiming rule met its first blind-window collision, and the backstop held: ×5 → ×6.* #171 claimed 0048 with the prescribed scan honestly returning nothing — the seventeenth retro's competing claim sat on #168, posted seventeen minutes earlier, but #168 had closed ten minutes before #171 looked and the rule scans open issues; the ADR itself landed five minutes after that. Found on the rebase, renumbered to 0049 with references updated, exactly as prescribed. The exemplar records the window rather than widening the scan: the backstop's cost is one renumber, which is the priced cost, and the twelfth retro's central-file reasoning still applies.
- *An instance landed without its convention entry, and the same-session filing was the right handling.* #171 shipped `untyped_harness_failure` — emitted, ranked, the default where a class is missing — with no row in the failure-class table; #184 was filed within a minute of the close. The convention-lands rule read in reverse is not a gap here: the table is human-gated, so the same-commit standard collides with the sign-off gate, and folding an unrelated gated decision into ADR-0049's claim would have blurred what that ADR was claimed for. Filing made the gap visible to triage the same session — the gate-respecting equivalent of the same-commit landing. This retro then resolved #184 under its own batch (option (a), the row lands): the table's contract — read the class, find the required response — must not miss on a class a `verdict.json` can carry, and the row documents what the code already decided (`spike/regress.sh` sends any unknown class to severity 90, above `infra_unavailable`'s 80), which is the safe delegated shape. No reverse clause for convention-lands: first instance of the shape, and the named-defect rule is what caught it.
- *The corpus gate met its first equivalence argument, on a phrase defined nowhere.* #145's diff touched `Outbox.push` — by its own close "the one path every world effect takes (ADR-0012)" — and landed with `just fast` alone: wire byte-identical, all five producers proven conformant, the issue's criterion 4 naming `just fast` as the tier. Openly stated, probably true, and still an equivalence argument — the form the gate's surface-based design exists to preclude ("the expensive failures to date have been the unselected kind"), and an issue's criteria cannot stand down a standing gate. What made it arguable is that "the daemon's world-facing half" appears in CLAUDE.md, ADR-0016 and regression-tier.md and was defined in none. The definition lands in regression-tier.md (port dispatch and refusals, outbox, command/effect codec; gates on surface, not on a predicted wire); the residual exposure — the landed daemon has not run the corpus; the next full-corpus run covers it — is recorded on #145. No charge of bypass, and no recurrence allowance either: with the boundary written, the next equivalence-argued skip on it is a gate violation, not an interpretation.
- *#178 is the wiki-first rule and the honesty bullet operating on one issue.* Curator coefficients and the −1e6 disabled threshold, "Markers are always free", the Y keybind — all cited from the vendored wiki rather than rediscovered in-world; then the close separated "this run proved the assignment" from "the Y keypress is engine-owned, unexercised — if Y does nothing in the next human session, that is a finding against this issue". Its criterion-4 powers statement (camera, markers, nothing else, each named) and the one deliberate non-change (the killable body, routed to a follow-up carrying a human sign-off) are the explicit-choice and gameplay-gate disciplines doing what they say. Exemplars; no change.
- *#145's checkout wipe (orchestrator-reported; the demos themselves corroborated by the close's three "temporarily added … reverted" proofs).* A `git checkout` reverting a deliberate drift demo also swept an uncommitted comment edit in the same file; recovered, and every later demo ran from a committed state. A commit-before-demo rule would enumerate a case of the standing commit-early bullet, which the agent applied unprompted the moment it mattered. Journal only.
- *Sequencing by surface: real conflicts prevented where held, trivial rebases where parallel.* #171/#178/#174 ran effectively concurrently on disjoint surfaces and #174's rebase over both mid-flight landings hit only the CHANGELOG; #162 names the same pool libraries #161 rewrites and #176 the same map/hint surface #174 was changing, and both were held. On this cycle's evidence the policy sequenced exactly where file-level overlap was real and parallelised where it was not, at the cost of an evening's delay on two issues. Scheduling stays the orchestrator's; no doc text.

**Applied** (under ADR-0050): the `untyped_harness_failure` row in the failure-class table (resolves #184, option (a)); ADR-claiming ×5 → ×6 with the blind-window exemplar; the world-facing-half definition in regression-tier.md; the exposure note on #145; retro skill ×17 → ×18.

**Rejected / no change.** A reverse convention-lands clause (first instance; the named-defect rule caught it). A commit-before-demo rule (a case of commit-early, self-applied). Any watcher or watching-sentence change (one clean cycle; attribution unanswerable and unneeded). Widening the ADR claim scan to closed issues (backstop priced and proven; window recorded in the exemplar). A sequencing rule (orchestrator-owned, as the seventeenth held for dispatch isolation). A failure-class earn (nothing failed in a typed way this cycle; the new row documents a decision, it is not an under-fire validation).

**Status.** ADR-claiming ×5 → ×6 (the 0048/0049 blind-window collision). Retro ×17 → ×18, no self-amendment — step 3's proportionality question got its first exercised answer (one batch ADR folding #184, weighed in the ADR). Failure classes stay ×8 (nine rows now, eight earns). Convention-lands ×4 (#171's policy-plus-`probe_verdict.py` same-commit landing is the rule operating, not a new mode). Probe-window ×7, elimination-context ×5, recovery runbook ×10 unchanged. `playtest-brief` ×2, `playtest-ingest` ×1 — no playtest this cycle. ADR-0049's Python-seam policy is new and carries no marker yet; #161/#162 are its first scheduled uses.

## 2026-08-04 — retro: scheduled, five issues (#156, #184, #176, #161, #152)

Trigger: five closes since `8fd3e7b` (#184 closed by the eighteenth itself, its row landed there). Also in the cycle: the human's mid-cycle return — a status report, #179's backlog rank asked (sixth of nine in the handoff's Part 2; no ruling, stays `ready-for-human`), and the second ADR-0047 guided decision session, which cleared the whole review queue (0046–0050 all approved, one verdict comment per claiming issue, flips as one commit, `52fef27`); the #159 agent's 40-minute stall (third instance, watcher catch); #162 in flight as ADR-0049's second scheduled use; #185 filed by #161, #186 by this retro. Process pass only. Amendments recorded under ADR-0051 (`Reviewed-by-human: pending`), claimed by comment on #186. **Review queue at this retro: 0 pending on-tree — cleared mid-cycle by the second guided session — 1 once 0051 lands.**

**Findings.**

- *The same-edit clause's second violation, and it was the fifteenth retro's own commit.* `0485d1a` appended convention-lands' #131 exemplar and left the count at ×3, while ADR-0047 decision 3 — approved by the human this morning — and the fifteenth's own entry both record ×3 → ×4; the sixteenth through eighteenth then propagated ×4 in their status lines from the log without reading the file, the exact read the fifteenth had caught the fourteenth by. The escalation both status blocks pre-priced fires: #186 filed (`ready-for-agent`) for the mechanical check, carrying the corpus-fitting constraint (#137's exemplar) and the note that a delimiter convention inside CLAUDE.md's lists would itself be gated; the count correction lands in this batch as execution of ADR-0047's approved content, not a new decision. Honest near-miss: this retro too would have written "convention-lands ×4" from the log had step 5's read not gone to the surface.
- *The stall attribution question is answered: three stalls, three watcher catches, zero prevented or caught by text.* #159's agent went silent 40 minutes after its corpus run finished green (21/21 at `838d35f`, pool `20260804T181537Z-1402436-pool`, worktree clean and unlanded), with the do-not-park briefing line carried and CLAUDE.md's watching sentence live — the "stall despite both layers" the eighteenth named as its escalation trigger. What it escalates to already exists: the watcher recovery.md made standing caught all three (#168 ×2, #159 ×1), so the clause now states the score instead of an open question, and the eleventh use lands in the header. The text layers stay because they are true and cost nothing, not because they work; the defect underneath — a parked run's completion does not wake the agent that parked it — is harness-level and the orchestrator's to pursue. At the opposite pole, #176's agent woke itself on every verdict: over-chatty, and the cheap failure mode; no rule either way.
- *ADR-0049's first scheduled use ran clean, in both directions.* #161 landed the guard-verdict ladder ×3 into `tools/host_guard_verdict.py` (17 byte-pinning tests red-first, the `probe_verdict` pattern, the shared caller failing closed) and the lock-info block into a sourced bash home with the routing argued at the site — the block names the holder at the instant the kernel hands over the flock, a process seam with no decision in it — so the policy's line was drawn correctly toward Python and toward bash in one issue. One-seam-per-issue held: the regress.sh JSON seam declined with a sentence and filed as #185 with full scope — the named-defect rule's third filing (#183, #184, #185). The overturning clause's cost was measured and stated (~4 s of `uv run` on the no-Arma suite). #162 is the policy's next datum.
- *Both batch closes checked their inherited item lists against the tree that had moved under them, unprompted.* #152 verified against `01624a7` and `be9e748` that neither #145 nor #156 had consumed any of its six items before starting; #156's per-item audit found two already discharged by #164's `3cb4db0` and said so rather than re-doing them. First applications of the elimination-context line to an inherited work list rather than a measurement or rationale; ×5 → ×6.
- *Concurrency stayed cheap and visible.* Five closes in one evening; #161 absorbed three concurrent main movements and #152 two (fetch, rebase, re-run `just fast`, push, each time), #156 one ("nothing moved under it", verified); every conflict CHANGELOG-trivial; sequencing-by-surface (holding #162 behind #161, #160/#149 behind #159) kept real conflicts at zero again. Scheduling stays the orchestrator's, as held twice already. Corpus health alongside: three full-pool greens in the cycle (752/809/774 s waves), all 21/21.
- *#181's table had not absorbed the day's passes, and nothing routes them there.* Three full-corpus massed-assault passes since its third data point (269/346/294 s at `aa26bb4`/`a6a23f1`/`838d35f`), each on a different SHA — new-context data, not rate rows for `3cb4db0`. Posted onto #181 by this retro, with the perishability reason stated: the pool prune keeps failures and deletes passes, so pass evidence outlives its run dir only if quoted. No rule — the `flake_quarantine` row binds the eventual fix's baseline, and single instance.
- *#176 as an exemplar bundle, journal only:* wording/icon/colour flagged for playtest under ADR-0020 placeholders rather than decided; the whitelist non-growth asserted in the same probe run as the feature; the staged baseline honestly diffed (`from=2`) before any delta was claimed; the missing CONTEXT.md term for a Squad at zero routed to /domain-modeling rather than coined. Window unmoved at 420 while the probe gained five checks.

**Applied** (under ADR-0051): elimination-context ×5 → ×6; recovery runbook ×10 → ×11 with the #159 catch and the settled score at the noticing clause; retro skill ×18 → ×19. Landed beside the batch, not delegated: convention-lands ×3 → ×4 (executing ADR-0047 decision 3, approved this morning); #186 filed; the #181 data comment.

**Rejected / no change.** Building the marker checker in-session (one open design question — prose exemplars have no machine delimiter, and inventing one edits CLAUDE.md's format — belongs to an implementation agent per #137's discipline; filed instead). Removing or sharpening the watching-inside-turn sentence (true and cheap; the watcher is the working layer and is codified; the fix is harness-level). A read-the-file-not-the-log sentence in step 5 (#186's check is the fix; more prose is the thing that failed). A rule routing interim passes to a flake issue's table (single instance; fix-time baseline already bound). A marker for ADR-0049's policy (the journal's status line carries it until #162 lands). A failure-class earn (nothing failed in a typed way; three all-pass waves).

**Status.** Elimination-context ×5 → ×6 (#156/#152's consumed-check). Recovery runbook ×10 → ×11 (the standing watcher's first catch since codification). Convention-lands ×4 — a correction, not an earn. Retro ×18 → ×19, the second violation found against the skill's own history and the pre-priced escalation executed by filing. Failure classes stay ×8, probe-window ×7 (#176's unmoved window is the rule operating), ADR-claiming ×6 (the 0051 claim is the rule operating). `playtest-brief` ×2, `playtest-ingest` ×1 — no playtest; #176 landed but #175 stays `ready-for-human`, so playtest 0002 still waits. ADR-0049's policy: first scheduled use complete and clean (#161, both routings correct, seam discipline held); #162 in flight is its next datum.

## 2026-08-04 — retro: scheduled, five issues (#159, #162, #169, #181, #185)

Trigger: five closes since `ba8961f`. The cycle's spine was the guided decision-capture session (2026-08-04, rulings on #169, #177, #150, #181's doctrine branch, and #162's gated item), whose outputs this cycle then executed. Process pass only. Amendments recorded under ADR-0053 (`Reviewed-by-human: pending`), claimed by comment on #186. **Review queue at this retro: 1 pending on-tree (0051) — 2 once 0053 lands** (0052 was reviewed in session by the human, so it never entered the queue).

**Findings.**

- *The decision-ticket convention's first full run since its #31 exemplar was clean end to end, and it located the convention's slack correctly.* #169 closed the same evening as ADR-0052 plus #188/#189 in dependency order, no code on the ticket. The session's comment explicitly left two names to the implementing ADR, and the agent used exactly that room: `caller_dead` over the working `commander_down` on ruling 5's own logic (one code for both principals), the gateway placement found load-bearing (a dead squad leader no longer resolves as a principal, so a post-resolution check would type him `wrong_side` and break the symmetry), no third issue for the engine-owned succession (verified against the vendored wiki), and the CONTEXT.md one-word question ("on respawn" vs "on rejoining") flagged for the human at review rather than edited. Second exemplar appended in `docs/agents/issue-tracker.md`, with the boundary stated: names and placement to the implementer, rulings to the session.
- *Every ruling took the vehicle proportionate to its content, four shapes in one session.* #169 → decision ticket (ADR + issues); #177 → prototype ticket #187 carrying the session-confirmed spec as acceptance criteria (three arms, efficacy and complexity measures, MVP-or-post-MVP deliberately deferred until the comparison returns); #150 → issue-comment decision, deliberately not an ADR per its own criteria (ADR-0040's model untouched); #181's doctrine branch → implemented and landed the same evening (`64e13f4`). The machine the conventions describe ran unattended on its biggest decision batch yet without a new rule.
- *#181's fix is the cycle's diagnostic exemplar, journal-mostly.* The agent read the red run's Decision rows, found the demanded mass thrashed while scores held (0.718 vs 0.086 with `momentum: 1.0` standing), and rejected the dispatch briefing's consideration-shaped suggestion with the trace as the argument — no score factor reaches the muster's shed, so that fix would have changed nothing on any seed. Floored demand by committed force instead; 60/60 seeds red pre-fix, 0/60 post, both arrangements, sweeps landed as permanent unit tests so the pre-fix behaviour is now a red `just unit`; corpus 21/21 with the probe and window untouched (the probe gates on a planner decision, and the defect was one — the bet-on-the-decision rule's design paying off, not a new instance of it). Stated plainly what was not measured (no in-world rate on `dd26108`/`0c0eee5`). The ADR-0027 tension flagged, not silently resolved — residue: the qualification's record lives in `_mass`'s docstring and #181's close, and ADR-0027's own file carries no pointer; left to the human at 0053's review (editing an approved ADR is what the gate covers; the ruling's substance is already human-approved, so nothing is at risk while it waits).
- *Stall four settles the watcher question; the runbook now says the compensation is permanent.* #162's agent parked after its corpus finished; the orchestrator's watcher caught it and prodded with the verdict in hand (orchestrator-side observation — the repo shows only the close landing minutes after the pool run's end). Four watcher catches, zero text catches. The defect — a parked run's completion does not wake the agent that parked it — is Claude Code harness behaviour, outside this repo's remit, so the noticing clause now states the watcher is permanent compensation and further catches are it working, not new findings; retros stop re-litigating the attribution. The opposite pole stays unruled: #181's agent woke per verdict (~8 orchestrator wakes for one corpus) — chatty is strictly the cheap failure mode next to parked, and prescribing a cadence between them is over-prescription.
- *ADR-0049's third conversion held the pattern, and the coordination discipline with it.* #185 moved the pool merge to `tools/pool_merge.py` (26 tests red-first, one home, every caller failing closed, `--list` byte-identical), honoured the inventory's coordination note by taking severity whole and routing the residue to #92/#147 at the `CLASS_RANK` comment — where the next joiner will actually read it — and #147 is dispatched on exactly that residue. Three conversions (probe_verdict, host_guard_verdict, pool_merge), zero regressions; the CLAUDE.md bullet's "first instance" pointer replaced with the track record.
- *#162's slot-0 fork went to the stronger invariant.* "Byte for byte the serial tier" vs code that wasn't: resolved by aligning the code (slot 0's profile is now `ctispike`, HCs number from 1) rather than softening the claim, with the #58 bulkhead property kept under test per key. A documentation-vs-code fork has two cheap outcomes; choosing the one that makes the claim true is the exemplar worth one journal line. Its gated item ran the gate correctly: the `just generate` row proposed, explicitly not taken under ADR-0013 with the session imminent, approved by the human hours later — landed by this retro as execution of that sign-off (the recipe had existed since Phase 1 without a table row; the command-surface convention's recipe-and-row-together rule postdates it, so this is a back-fill, not a violation found).
- *Ground-shift checking reached a filed defect's ground.* #159 found the tree had moved under the finding's line number (#79/#82 shifted the spawn count onto the roster), named the live reader (`squad_reinforced` arm), fixed both arms, and said so. Elimination-context ×6 → ×7. The rest of #159 is the machine operating: request ids onto the one mint, the effect catalogue held at both ends of the door, full corpus green.
- *Concurrency stayed cheap.* Landings absorbed two-to-three main movements each, every conflict CHANGELOG-trivial, sequencing-by-surface kept real conflicts at zero; #181's worktree SHA landed unchanged with the crossed commits named and checked for surface overlap. Scheduling stays the orchestrator's, as held three times now.

**Applied** (under ADR-0053): decision-ticket second exemplar in `docs/agents/issue-tracker.md`; recovery runbook ×11 → ×12 with the #162 catch and the permanence sentence; ADR-0049 bullet's three-conversion record in CLAUDE.md; elimination-context ×6 → ×7 (#159); retro skill ×19 → ×20. Landed beside the batch, not delegated: the `just generate` row and the check row's generated-schema mention (executing the human's sign-off on #162, wording as proposed there).

**Rejected / no change.** A cadence rule between chatty and parked (chatty loses nothing; the asymmetry is recorded here, not in CLAUDE.md). Taking the ADR-0027 amendment note under delegation (editing an approved ADR's text is precisely what review is for; flagged at 0053 instead). A marker or count for the decision-ticket convention (issue-tracker.md's paragraphs carry exemplars, not ×N counts; two exemplars now stand). A failure-class earn (#159's `effect_not_declared` and #185's fail-closed typing are design within the table, not the table exercised by a failure). Any probe-window movement (#181 fixed an in-world intermittent with probe and window untouched — the rule's design working, counted nowhere).

**Status.** Recovery runbook ×11 → ×12 (fourth stall, fourth watcher catch; permanence stated). Elimination-context ×6 → ×7 (#159's filed-ground check). Retro ×19 → ×20 (unattended, no self-amendment; the approved-edit execution as the nineteenth's shape). Decision tickets: second exemplar (#169). ADR-0049's policy: three conversions clean, record moved into the CLAUDE.md bullet; #147 next on the class-table residue. Failure classes stay ×8, probe-window ×7, ADR-claiming ×6 (the 0052 and 0053 claims are the rule operating). Convention-lands ×4 unmoved. `playtest-brief` ×2, `playtest-ingest` ×1 — no playtest this cycle; ADR-0052's #189 (respawn 5 → 30) is playtest-facing when it lands.

## 2026-08-04 — retro: scheduled, five issues (#148, #166, #147, #150, #191)

Trigger: five closes since `5bea145`, the last cycle at the five-close cadence. Three in-session human instructions rode this retro beyond normal scope, each landed citing the instruction as sign-off: the one-off complete review of all cascading CLAUDE.md files via the claude-md-management plugin (`a87ad36`), the retro skill's scope/cadence amendment — incremental `/revise-claude-md` every retro, trigger 5 → 10 closes effective after this one (`f7c42f0`) — and, delivered mid-retro, the ratified model-effort mapping with its enforcing `.claude/agents/` seats (`13de963`). Mid-cycle the human also imposed a WIP limit of three (retros count) and a usage-conservation window (22:05–23:20); both ran from orchestrator memory, and the #190 dispatch stopped seconds in plus a #182 dispatch held through the window both resumed cleanly. Process pass only. Delegated amendments recorded under ADR-0054 (`Reviewed-by-human: pending`), claimed by comment on #186. **Review queue at this retro: 2 pending on-tree (0051, 0053) — 3 once 0054 lands.**

**Findings.**

- *The decision-report pipeline's execution side ran as cleanly as its capture side.* Nine rulings from the second guided session flowed to agents within the hour; #148 and #166 landed the same evening as one-commit executions whose commit messages name the decision comment as the sign-off (`bda8829`, `7bdf64c`); #150 ran ruling → implementation issue (#191, filed carrying the ruling's own criteria) → planner consideration → probe → 22/22 gating corpus in one agent, same evening, with the no-ADR call made by the issue's own criterion. The report's one already-consumed item did not double-land: `git log -S "just generate"` shows exactly one landing, the twentieth's commit. Second clean cycle for the vehicle-proportionality machinery; no new rule.
- *Standing orchestration rules live in session memory; the ruled part of one now lives in the repo.* Three process instructions arrived as chat and were recorded in orchestrator memory, and the machinery worked — dispatches stopped and resumed cleanly around them. Where such an instruction binds a repo-governed surface, this retro's answer is to land it there: the cadence and scope now sit in the retro skill, their first written home. Rules binding only the orchestrator's scheduling (the WIP limit, conservation windows) get this journal line for audit and stay operational in memory; a `docs/agents/orchestration.md` is declined at one cycle's evidence — proposed to the human as the rule-of-three threshold if a third class of standing orchestrator rule accumulates.
- *Stalls five and six, both #149, both watcher catches; the sharper edge is what the first stall sat on.* 90 minutes silent with uncommitted work across five addon files — the commit-early line violated during the stall, corrected on the prod — then 2+ hours silent through the conservation window (orchestrator-side observations). Six watcher catches, zero self-recoveries; the permanence sentence operating, so nothing new to decide. The runbook's thirteenth use states the edge: a stall on uncommitted work is what turns an orchestrator death from a prod into work at risk.
- *The probe-window discipline held at first contact in the corpus's newest probe.* `ai-reinforce`'s first corpus run timed out in its own scaffold — staging read the board through `view`, which refuses an AI-commanded side (`wrong_side`), while the decision under test had already fired at t=10. The fix went to the probe's staging (`cti_presenceReport` pattern), never the 300 s window; green twice at 25 s, the finding landed in the tier doc beside the compileFinal one. Corpus 21 → 22, 22/22 at `5c407c6`.
- *A semantics change inside the typing ladder needed no matching table sentence, and that is the design working.* #147's two loudly-stated semantics changes (128+SIG → `infra_unavailable`; out-of-table or reserved classes → `untyped_harness_failure`, closing the `class=pass` inversion hole) moved runs between existing rows without moving any row's required response, so CLAUDE.md's table needed nothing; the tier doc's one exit-code reference was updated in the same commit (`9f845dd`), and the ladder's one home stays `tools/probe_verdict.py` under unit test — a prose ladder would be the parallel table #185 declined. No failure-class earn, per the twentieth's precedent: design within the table, not the table exercised by a failure.
- *Elimination-context reached a whole finding list.* #147's seven findings were each re-located before being fixed — the tree had been reshaped four times since filing (#171, #161, #162, #185) — all seven fixed, none declined, shifts stated in the close.
- *The stale-copy mechanism reached the process docs themselves, twice in this retro's own session.* The session was served a CLAUDE.md lacking rows landed four days ago and a retro skill at ×16 against origin/main's ×20, both from the launching worktree's copies — the exact mechanism the hooks paragraph already documents (#120). No new sentence: the existing one generalises, and the fix here as there is the worktree rebasing; recorded so the next reader of a stale-looking rule checks their copy before "correcting" main.
- *The cascading CLAUDE.md review, one-off, per instruction.* Project file: 86/100 under the plugin's rubric — held back only by two currency nits (both fixed: the unproven-and-unused banner, the unnamed ADR-form check) and by deliberate density the instruction itself prices as load-bearing. Applied beyond those: code homes named in the toolchain bullets. Weighed and declined as regressions by the instruction's own terms: compressing the validated markers' exemplar histories, and a directory-map section (no navigation failure in twenty-one cycles to earn it). The global `~/.claude/CLAUDE.md` is outside the repo: reviewed, proposals handed to the orchestrator's report (RTK.md's dangling "refer to CLAUDE.md" pointer and setup-verification prompt weight; the semble block's over-absolute "always search first" duplicating the MCP server's own instructions). Note for the next incremental pass: the plugin's skill did not surface through this subagent's Skill tool — it was loaded from the plugin cache (`~/.claude/plugins/cache/claude-plugins-official/claude-md-management/1.0.0/`) and its method followed from there.

**Applied** (under ADR-0054): probe-window ×7 → ×8 (#150/#191); elimination-context ×7 → ×8 (#147); recovery runbook ×12 → ×13 (stalls five and six); retro skill ×20 → ×21. Landed beside the batch under in-session human instructions, not delegated: the retro-skill scope/cadence amendment (`f7c42f0`); the CLAUDE.md currency pass (`a87ad36`); the model-effort mapping and its four agent seats (`13de963`), with `cti-recon`'s Bash-for-inspection judgement call flagged for review.

**Rejected / no change.** A failure-class earn (#147 is design within the table). A `docs/agents/orchestration.md` (one cycle's evidence; rule-of-three proposed instead). A stale-copy sentence beyond the hooks paragraph (it generalises as written). Any compression of CLAUDE.md's markers or a directory map (finding above). A rule on stall posture (permanence already stated; the edge lives in the runbook's exemplar).

**Status.** Probe-window ×7 → ×8 (first-contact hold in `ai-reinforce`). Elimination-context ×7 → ×8 (#147's whole-list re-location). Recovery runbook ×12 → ×13 (six for six). Retro ×20 → ×21 (attended-by-instruction; first directly human-decided scope amendment; cadence now written where the skill lives). Failure classes stay ×8 — #147 tuned the ladder, nothing exercised the table. ADR-claiming ×6 unmoved (the 0054 claim is the rule operating). Convention-lands ×4 unmoved (the mapping landing with its four seats in one commit is the rule operating, not a new earn). `playtest-brief` ×2, `playtest-ingest` ×1 — no playtest; #174's deferred feel judgements and the Reinforce scenario still wait on the next brief. Open for the human: the 500k-token alternative trigger (not ruled on); the mapping amendment proposed in the report (ruling executions with drafting slack on gated semantic surfaces — the #148/#166 shape — sit above `cti-mechanical`); ADR-0027's amendment pointer still waits at 0053's review.

## 2026-08-05 — retro: scheduled, first at the ten-close cadence (#182+#125, #190, #188, #172, #192, #198, #197, #199, #149)

Trigger: ten closes since `05fa370`. Unattended; delegated batch recorded as ADR-0057 (`Reviewed-by-human: pending`, claimed by comment on #200). First run of the standing incremental `/revise-claude-md` pass — the plugin's skill again did not surface through the Skill tool and was loaded from the cache path the twenty-first recorded, so the propagation gap stands. **Review queue at this retro: 3 pending on-tree (0051, 0053, 0054) — 4 once 0057 lands.**

**Findings.**

- *The token-economy pivot ran measurement-first, and the measurement inverted the intuitions it was handed.* The budget crisis (most of a weekly fable budget in a day) produced the ratified mapping (`13de963`), the opus-high default (`3198850`), then #195's assessment over all 194 transcripts before any optimisation landed: the cache-write cliff at the 5-minute TTL carries 13.56% of the bill from 2.5% of turns; doc compression measured at ~1% against a folklore 50–65%; the community's 42%-repeated-reads claim did not replicate (9.2% of read bytes, 0.39% of bill, item 14 of the doc). Three measured fixes landed the same cycle — #197 (`just fast` 6:32 → 1:02, suite already xdist-clean, nothing serialised), #198 (`just watch`, the wait taken out of the turn), #199 (`just verdict`, a close's quote rendered from the run's own record). The pattern to keep: the sequence was measure → invert priorities → fix the top three, not optimise-what's-visible.
- *The command-surface convention collided with its own gate, for the first time.* #198 and #199 landed recipes whose table rows they could not land — the table is on a human-gated file — so both proposed and neither took. ADR-0057 lands the rows verbatim as clearly-safe documentation of landed tooling and adds the reconciliation clause: a gated row follows through the gate rather than lagging silently. The behavioural half of the pile — #200's "a turn does not block for five minutes" — is *not* taken: it is an open ruling issue routed to the human, and the consolidated recommendation sits in a comment there.
- *The #149 marathon is the cycle's stress test, and every layer held except throughput.* Orchestrator-side: nine stalls in ~7 hours, all watcher catches, zero self-recoveries (recovery runbook ×14). Repo-side, the durable record shows two green full-corpus pools with three typed `infra_unavailable` stops between them — play-session guard twice, #124's port sweep once — each cause read, none interpreted, 39 green partial-pool verdicts never stitched into a gate (failure classes ×9). The churn's cause was structural: in-world landings outpaced the 19-minute gate, forcing rebase+re-gate cycles; the coordinator's dispatch freeze was the tactical fix and #197's gate shrink the systemic one. One correction to the dispatch brief's account: ADR-0055's memory floor did **not** fire live — the runs tree carries no `starved` marker; the floor's proven firing remains the staged no-Arma one, and the live stops were the pre-existing guards.
- *#189 falsified a ruling's premise before it shipped, and the withdrawal is the process working one gate late.* ADR-0052 ruling 4 assumed engine AI succession; the corpus went red on the premise, a 120 s observation-only diagnostic (57 rows) showed leadership never leaves the dead player on this engine, and the built reclaim was withdrawn (`27e18b4`) rather than landed inert — with the agent naming the trap exactly: quietly re-specifying the ruling to match the engine would be "edit the spec to make it pass", one level down. Elimination-context ×9 (first application to a decision ticket's premise). A "premises the engine can falsify get a diagnostic before the ADR fixes them" line was considered and declined at one instance: the probe gate caught it at the cost of one withdrawn commit, which is the gate pricing the miss. A second premise landing inert earns the line.
- *Scheduling pressure on the tier is now structural, not incidental.* Corpus 22 → 25 this cycle (`dead-principal`, `loadout`, `respawn-base`; the dispatch brief's 21 → 26 was off at both ends), full pool ~1100 s and growing, two-plus agents gating concurrently is the normal state, and #196 shows the per-probe host guard answering that state wrongly (nineteen probes abandoned to a cross-run collision — correctly typed, expensively). #194 (respawn UID window) sits beside it. Both were filed, typed, and not interpreted — the discipline held; the capacity question is the human's.
- *The ready-for-human pile is at its high-water mark, and the human's throughput is the process's bottleneck.* Standing: #187 (concentration prototype), #189 ruling 4, #186 (marker-list delimiter), #194, #195 (adoption of the assessment's remaining items), #196, #200, #201, the 3-deep ADR review queue, and two CONTEXT.md wording tensions (respawn-vs-rejoin from ADR-0052; #172's menu-reading flag). Recommendation, not taken: schedule the next guided decision session (ADR-0047's pattern) proactively on pile depth rather than waiting to be asked — one sitting would clear most of the list.
- *Standing orchestration rules crossed the twenty-first's rule-of-three threshold.* The memory-only set grew from two classes (WIP limit, conservation windows) to at least four (report-cadence muting — two agents told mid-cycle to report red-or-final only — and the dispatch-freeze practice). `docs/agents/orchestration.md` is now recommended to the human, but not landed from here: the rules' authoritative contents live in orchestrator memory this session cannot read, and a mis-transcribed standing rule is worse than a proposed doc.
- *The same-edit clause's pre-priced escalation completed mid-cycle.* `tools/check_validated_markers.py` (`d440c88`) now gates status-header counts against the uses they narrate, fitted to the corpus (CLAUDE.md's five parentheticals declared honestly uncountable, with the miscounting rules enumerated); #186 stays open only for the delimiter ruling. This entry's own marker edits ran under it — the check's first retro.

**Applied** (under ADR-0057): the three command-table rows verbatim from the #198/#199 closes; the reconciliation clause; the `just watch` exception clause on the watching sentence; failure classes ×8 → ×9 (#149's gate); elimination-context ×8 → ×9 (#189's premise); ADR-claiming ×6 → ×7 (#182's 0054 → 0055 renumber); recovery runbook ×13 → ×14 (the marathon); retro skill ×21 → ×22; banner to twenty-two.

**Rejected / no change.** #200's rule and #201's prune (with the human, by design). The decision-ticket diagnostic line (one instance; the gate priced the miss). `docs/agents/orchestration.md` from this seat (threshold met, contents unreadable from here — recommended instead). A probe-window bump (`respawn-base` moved its window 540 → 480 *downward* with the arithmetic in its header — the discipline operating, not a new earn). A convention-lands bump (the row-gap is the convention colliding with a gate, resolved by ADR-0057, not a validation).

**Status.** Failure classes ×8 → ×9. Elimination-context ×8 → ×9. ADR-claiming ×6 → ×7. Recovery runbook ×13 → ×14. Retro ×21 → ×22 (first ten-close cycle; first incremental revise pass; first split of gated proposals by safety). Probe-window ×8, convention-lands ×4 unmoved (see rejections). `playtest-brief` ×2, `playtest-ingest` ×1 — no playtest; #174's deferred feel judgements and the Reinforce scenario still wait on the next brief, and #190's observer mode now exists for it. Open for the human, consolidated: everything in the pile finding above, plus the twenty-first's still-standing items (the 500k-token alternative trigger; the mapping amendment for ruling executions on gated semantic surfaces; ADR-0027's amendment pointer at 0053's review).

## 2026-08-05 — retro: scheduled, ten issues (#157, #165, #151, #193, #175, #205, #207, #206, #153, #210)

Trigger: ten closes since `e3b5d99`. Unattended; delegated batch recorded as ADR-0060 (`Reviewed-by-human: pending`, claimed by comment on #217). First execution of the exemplar prune (#201; skill amendment `4287d63`) — the prune record at the foot of this entry is the full text of everything dropped. The claude-md-management skill again did not surface through the Skill tool and was loaded from the twenty-first's cache path; third consecutive retro, the propagation gap stands. **Review queue at this retro: 4 pending on-tree (0051, 0053, 0054, 0057) — 5 once 0060 lands.**

**Findings.**

- *The token-economy track's second wave ran measurement-first end to end, and measurement inverted the handed intuition every time.* #203's TTL split (subagent loss 8.87% + crash 1.30%, main parking 2.72%; orchestrator-holds-and-prods measured the *worst* option at 5.35%, end-before-wait recommended). #205/#207 landed the two prevention hooks fail-closed with both-directions tests — and #207 re-measured its own threshold over 2,549 Reads, picked 40,000 from the distribution, and rejected the issue's permit-any-`limit` clause because the denial message's own advice would have been its bypass. #206 corrected the assessment honestly (408.8M → 447.1M input-equivalents, ceiling 13.56% → 12.89%) rather than defending it. #208 stated the handoff break-even as a forecast (56%, tested by #212, not claimed) and inverted the literature's raw-transcript preference on this project's measured 12.55× amplification. #209 returned an honest 0.4% ceiling on its own question, declined the human's slot-API example with arithmetic, flagged the folklore figures as sourceless, and measured CLAUDE.md's exemplar blocks (2,246 tokens, 1.095% of bill) as bigger than every proposed interface combined — which is what routed the prune to this retro. #210 ran the issue's sketched jq and found it fails open (`last` of an empty array is `null`, exit 0), so the selection landed in Python under pytest (#83's precedent). "Measure before building" as a working-style line: considered, routed to the human on #217 rather than taken — every instance arose without the rule, and the cycle's own arithmetic prices every resident sentence.
- *The five-minute rule landed (#200, verbatim at `4287d63`) and its enforcement is mechanical, but the stalls continued inside ADR-0042's stale-copy window.* #153 parked ~2 h on a green pool twice; #170 parked ~2 h on reds — after `just watch` landed, while #205's hook governed only rebased worktrees (orchestrator-side observations; in-repo corroboration is the landing gaps). Watcher caught both; recovery runbook ×15. #204's standing dispatch rule is the structural fix still awaiting ruling; the runbook rewording that would cite it waits with it, but the fifteenth use now carries the prod's price (2.32%, #206) so the ruling can read its own arithmetic.
- *The re-read-fresh discipline ran through all four harness/test closes and paid each time; two shapes were new.* #157: the hand-maintained probe list's own comment argued "these two are the corpus's contended leg" when four intervening issues had made it six — self-checking against a rename, blind to addition; fixed by deriving from the probe headers. #165: the issue's "run by nothing" premise did not survive re-reading (`just spike` boots it, `just build` packs it, a vacuity guard pins it); deletion declined, stay of execution in the file's own header, and the two contradicting doc claims corrected in the same commit. #151: five findings re-read, one declined as consumed at `a3239d4`, and the re-read found a *worse* sibling in the same three lines — a signalled pool tore down and then kept scheduling onto slots it no longer held; fixed with the finding. #153: both findings confirmed live, the #151 near-miss named and distinguished (a different `@cti` on a different machine). Elimination-context ×10.
- *#153 is the cycle's decline-with-reasoning exemplar.* The issue-suggested heartbeat was refused with the argument landed in `spike/lock-info.sh`: a refresher would lie in exactly the failure the lock's entire history names (background processes outliving their parent on an inherited descriptor), where a `/proc` read at ask-time cannot be forged. The staleness signal then caught real stale metadata on its first live render (holder gone 55 minutes), before the change landed.
- *The model-effort mapping's first sonnet dispatches came back clean* — the #209-study transcription (which resolved a marker-gate conflict correctly on its own) and #206's correction (orchestrator-side attribution; the closes are consistent with it). Operating as designed; nothing to change.
- *The label audit answered the human's question and exposed a defect in this retro's own practice.* `ready-for-human` finds the decisions, with one mislabel found and fixed (#189) and three needs-triage items routed (#202 verified in-thread; the rest orchestrator-side). Two standing exceptions named: the ADR review queue (anchored grep, no label) and in-close flags consolidated by retros — and the twenty-second's consolidation comment sat on #200, which the human closed two hours later, taking the pile into a closed thread. Fixed with its first instance per convention-lands: standing issue #217, the step-3 sentence in the retro skill, the exceptions note in `docs/agents/triage-labels.md`.
- *#170, in flight, already worth the journal.* It refused to launder its own campaign-end regression through #106's flake baseline — the `closest=` table distinguished the shapes; its probe's frame race was fixed per #33's precedent with the window unmoved; and an over-claim in its own probe header was corrected to something the leg tests rather than assumes. Marker moves wait for its landing (corpus then 26; 25 on `main` now).
- *The prune, executed carefully rather than greedily.* Four lists cut to their newest five (probe-window 8 → 5, failure classes 9 → 5, elimination-context 10 → 5 after this cycle's append, ADR-claiming 7 → 5); convention-lands reached exactly five by append and drops nothing. Each pruned list opens with a pointer to this log so a count above the list length reads as the prune, not as a same-edit violation; `check_validated_markers.py`'s UNCOUNTABLE rationale updated to the post-prune shape in the same batch. The five kept per rule still span the rule's distinct lessons — the drop is strictly the oldest, and the oldest had all been restated by their successors.

**Applied** (under ADR-0060): the prune; `just handoff` row verbatim (#210, ADR-0057's clause; the sweep found no other row-less recipe); hooks-paragraph currency (#205/#207, and the denial-family split sentence); `just check` row names the marker check; elimination-context ×9 → ×10 (#157/#165); convention-lands ×4 → ×5 (#208/#210); recovery runbook ×14 → ×15; retro skill ×22 → ×23 with the pile-anchor sentence; triage-labels exceptions note; banner to twenty-three; standing pile issue #217 created with the pile posted.

**Rejected / no change.** "Measure before building" as a CLAUDE.md line (with the human on #217; the evidence is above). The runbook watcher rewording (waits on #204). A failure-class earn (#210's exit split and #151's `pool_signalled` are design within the table). A probe-window move (nothing landed on it; #170 counts at its landing). #211's handoff rule (its own flow; #212 measures first). Widening the audit's exceptions into new label machinery (two exceptions, two pointers — no third label).

**Status.** Elimination-context ×9 → ×10. Convention-lands ×4 → ×5. Recovery runbook ×14 → ×15. Retro ×22 → ×23 (first prune execution; pile anchored on #217). Failure classes ×9, probe-window ×8, ADR-claiming ×7 unmoved (the 0058/0059/0060 claims are the rule operating). `playtest-brief` ×2, `playtest-ingest` ×1 — no playtest; #174's deferred feel judgements and the Reinforce scenario still wait on the next brief. Open for the human: the pile on #217 (its newest comment is current).

**Prune record** (first execution, #201: dropped from CLAUDE.md, verbatim; counts unchanged).

*Probe-window (×8, list 8 → 5):* "#28: both probes were flaky and neither fix was a longer wait. Observers left in Reserve sit at the engine's default behaviour with weapons down and never acquired six men at 90 m across a full window; and a bearing off the leader's facing is a guess about line of sight that lands behind a hangar often enough to matter. #17: the harness itself was capping every probe at a hard-coded 180 s regardless of the caller's window; the fix honoured the subject-sized window rather than extending anything to pass. #33: a probe read two effects that land on separate pump turns in the same breath; fixed by waiting on the pair, window unmoved."

*Failure classes (×9, list 9 → 5):* "Phase 0: `infra_unavailable` fired on a stale daemon holding the port and correctly refused to be a result. Phase 1: a deliberately emptied manifest produced `assertion_failed` in-world and the harness refused to pass rather than booting an empty world. #22: deleting the manifest to stage a failure took the daemon down first, `infra_unavailable` stopped that run being read as anything, and the guard reached properly classified `assertion_failed`. #23: the old harness called every in-mission failure `assertion_failed`, including ones the world had typed `timeout` or `oracle_disagreement` — the table's required-response column is what made a wrong class a harness bug, and the runner now believes the line's own class."

*Elimination-context (×10, list 10 → 5):* "#35 inherited #33's \"an Assault razes an HQ in ~90 s\", measured against an empty Base; against a garrisoned one it cost a run and a wrong diagnosis. #26: \"Squad count binds, island size barely matters\" was measured while the Observation carried both rosters; #27/#28 had since made the enemy term grow with the map, and re-measuring before designing inverted the diagnosis — ADR-0030 charges the budget to the island because of it. #118: the review's reason for keeping the loop-entry guards — \"a supervisor that can restart them\" — had expired when #102 landed restart-refused; the agent re-derived a surviving reason rather than inheriting the expired one, the first application of this line to an inherited rationale rather than a measurement. #124: the memory-edge hypothesis was re-tested rather than inherited and disproved — available memory never fell below 3.9 GiB during a failing bring-up — and the same scrutiny then turned on the reporter's own recurrence comment, retracted by its author once the diagnosis showed a sibling `just unit` sweeping the tier's ports; the readings stood as readings, the story built on them did not. #164: attempt 3's escalation checked its fresh 19 MiB reading against #124's disproved memory-edge hypothesis and found the contexts differ — a bring-up that never fell below 3.9 GiB is not a running pool at 19 MiB — so the prior disproof was not inherited as a reason to look away; the first application of the line as a reason a fresh reading stands rather than a reason an old one fell."

*ADR-claiming (×7, list 7 → 5):* "#22 claimed ADR-0017 by issue comment while ADR-0018 landed concurrently; no collision. #23 wrote 0020 while #31 claimed it, and renumbered to 0021 during the rebase, exactly as prescribed."

## 2026-08-05 — user-directed amendment: the standing dispatch rule (#204) (not a retro)

Human decision session on the dispatch pattern #203's study recommended (ruling comment on #204, eight decisions). The human's own decisions, taken in session, so the `CLAUDE.md` and `.claude/hooks/` edits executing them carry the gate's approval and no ADR-0013 marker applies. Landed `fbb0a74` (rule + hook + changelog) and this commit (runbook + log).

**Adopted, as a default with a named-exception list, and the list is empty at landing.** It grows only at a retro, never by an agent's judgement in the moment. That is the binding-in-practice behaviour of the strict option with an amendment channel that is a documented process step rather than an unfalsifiable in-turn call — an agent cannot see its own cache economics, and #203's 226 measured stalls were all agents that believed staying was fine.

**The #200 clause, amended in place and split by seat.** `4287d63`'s "an agent waits by being woken, not by sitting inside a `sleep`" was not wrong, it was seat-blind: main sessions request the one-hour TTL on 100% of writes and still read 302,183 warm after a 10–30 minute gap, while subagents request the five-minute one on 100% and their read collapses 153,937 → 27,701 at the line. Being woken is free in the orchestrator and 201,326 input-equivalents in a subagent — #203's single most expensive row, 126 events, 5.35% of the bill. One bullet, seat-split, replacing the one line; #200's two load-bearing pieces (the ~110× figure, the not-a-licence-to-shorten-a-gate clause) kept.

**Placement: `CLAUDE.md` only.** Restating the subagent half in `.claude/agents/*.md` was considered and declined — subagents load the project `CLAUDE.md`, so a restatement buys nothing they do not already read and costs duplication plus #186's drift surface in five files. It also dissolves the reason a definitions-only option existed: a bare `Agent` call or a fork reads `CLAUDE.md` too, so no dispatch shape is left uncovered. Enforcement lives in code, which costs no tokens at all.

**Mechanical enforcement, and the definition of "foreseeable" it carries.** `.claude/hooks/deny-subagent-waits.py` extends from sleeps and poll loops to gate commands with a measured p90 over five minutes. Today that reaches the unfiltered full corpus and nothing else: nine unfiltered pools recorded 2026-08-04/05 at corpus sizes 22 → 26 ran 793–1342 s, p90 ≈ 1,230 s, every one over twice the TTL. `--list`, a named selection and an `--issues` filter all pass, as does the same run dispatched detached. The two decisions cohere and the coherence is the point: **the hook's denial list is the operational definition of "foreseeable"**, so a gate that overruns an expectation off the list is bad luck rather than a policy breach and the agent continues — foreseeability stays out of an agent's judgement and in a measured list the retro can amend. Two consequences stated in the artefacts rather than left to be discovered: the denial makes the detached path the only way a subagent can satisfy the regression row's full-corpus-no-filter requirement, and a gate that speeds up leaves a stale denial behind (`just fast` was on this shortlist until #197 took it from 6:32 to 1:02), so provenance and date live in the hook's docstring.

**Handoff coupled, not restated.** The rule says the ending agent writes a handoff per `docs/agents/handoff.md` (`ae79d12`, #208) and stops there — one field list, one home, no second surface to drift. #211 stays a separate landing and was not a prerequisite.

**Two branches sent to measurement rather than ruled**, each with its interim stated in `CLAUDE.md` positively, as the rule, naming the issue that may amend it. #218: the one-hour cache-write multiplier, an A/B with `FORCE_PROMPT_CACHING_5M=1`, because dispatch-as-a-session rests on a 2.0× the API publishes and a Max subscription does not — interim, there is no sanctioned fallback, so decompose or pay the expiry and say so in the close, and keepalive turns stay unsanctioned. #219: who reads a finished verdict, four arms (`cti-recon`, `cti-mechanical`, `cti-implementer` as control, a dedicated verdict-reader seat) against the fable/high orchestrator, on the failure criterion ruled unamended — interim, the fable/high orchestrator reads, which is what happens today. Running the fourth arm is approved; **ratifying** a new seat into the Model roles mapping is a separate sign-off, since that mapping is a standing human decision (2026-08-04) — and #216, sequenced after this landing, proposes moving that mapping out of the always-loaded prefix, so the gate follows the mapping wherever it comes to live.

**Also landed.** The runbook watcher rewording the twenty-third retro deferred explicitly ("waits on #204"), now carrying the point that unblocked it: `just watch` is a correctness mechanism and not a token one — the prod it produces *is* the 161,061-token rebuild, 2.32% across 54 events (#206) — so under this rule it is the backstop for an agent that failed to end, not the working layer. The fifteenth use's "awaits its ruling" corrected in the same pass.

**Out of scope, stated.** #206's correction to `docs/research/token-efficiency.md`'s cost model and `just watch`'s header comment stay on #206; #211 stays on #211.

## 2026-08-05 — retro: scheduled, ten issues (#214, #196, #204, #223, #230, #213, #226, #227, #232, #231)

Trigger: ten closes since `ee1080e`. Unattended; delegated batch recorded as ADR-0063 (`Reviewed-by-human: pending`, claimed by comment on #217). Landed via `just land` — the first retro under the mechanical landing protocol. The claude-md-management skill again did not surface through the Skill tool and was loaded from the twenty-first's cache path; fourth consecutive retro, the propagation gap stands. One scope boundary new to this retro, honoured throughout and recorded as ADR-0063 decision 1: the multi-provider initiative's CLAUDE.md amendments accumulate on #228, its own human-gated vehicle, so this pass took only the cycle's ungated currency and knowingly left the command table lagging five landed recipes — the lag is the gate operating. **Review queue at this retro: 6 pending on-tree (0051, 0053, 0054, 0057, 0060, 0062) — 7 once 0063 lands.**

**Findings.**

- *The initiative takeover ran handoff-first, and the handoff paid where the study said it would.* A mid-cycle dispatch freeze (WIP 0) gave way to the initiative's WIP reservation (human rulings, 2026-08-05), and every week-one dispatch was briefed "read `just handoff 221` before the issue body" — the #208 template's first heavyweight consumer. What the handoff bought is visible in what nobody re-did: the superseding handoff carried the build order (#223 first; "Do not wait on #229"), the ruled-out list (claude-code-router, LiteLLM-as-breaker, APM, rulesync, Inspect — each with its one-line reason), and the risks, and no week-one close re-litigated any of it. The whole core — dispatcher #223, prereqs #230, land #213, breaker #226, telemetry #227, marker gate #231, ledger currency #232 — landed in a single day under the reservation. What the briefings still restated: the #222 re-run instruction (once per briefing — which the quarantine row already owns), and the worktree protocol paragraph until #214's recipe deleted it mid-cycle (its close prices the deletion honestly: ~2.0 protocol calls per agent measured, so the prize is correctness, not tokens). The residue reads as the briefing's version of the stale-copy problem: operational instruction restated per dispatch because no mechanism carries it. Noted, not legislated — #212 measures the handoff's break-even next.
- *The plan-currency inversion completed end to end, and the ledger now records the measured currency.* #218: 104.6 M cache-write tokens across 128 sessions moved the plan meter zero points, the positive control moved 6 on 181 k output tokens — the premium #204's fallback was suspected of paying is below the meter's floor, and the null covers request count too. #220: re-priced in plan currency, the token-efficiency corpus's headline inverts (output on the order of a third of spend, not 4.5%; the cache-cliff family ranked 1–3 worth under 0.7% combined); both currencies kept, each labelled, with the four live reasons the token-flow view survives. #232: the ledger's only spend column was list-price `cost_usd`, anti-correlated with plan cost by three orders of magnitude on the Claude lane; replaced by `cap_fraction` with `est`/`observed` as a pair, its calibration named (`claude/218-2026-08-05`), its exclusions and attribution stated, and `list_price_usd` kept but ranked on by nothing. Elimination-context's newest exemplar, and the fifth measurement-inverts-intuition instance on the token track. "Measure before building" re-weighed per the evidence bank: all five arose without a resident rule, so the twenty-third's reasoning is *stronger* — the behaviour is self-sustaining and every resident sentence still has a price (#209). Stays with the human on #217; the same evidence would equally justify closing it adopted-in-practice.
- *#219 answered its question with a sharper variable than the one it was asked about, and its pilot is the cycle's honesty exemplar.* Across 40 scored readings, five arms, eight cases seeded with traps (a 7-of-8-pass stop, five unstitchable stops, a derived worst-class, a newest-pool bait, explicit pressure to land a red): not one misread class, not one stop treated as a result, not one would-be landing. All four failures were transcription — retyped-not-pasted evidence losing the SHA or corrupting the path — and the same model at the same effort split 8/8 vs 5/8 on seat instructions alone. The discarded pilot matters as precedent: the opus control cross-checked the reconstructed handoffs, found the fiction, and declined to post false gate records — an automatic failure under the scoring — so the battery was corrected with a replay disclaimer and re-run rather than scored against diligence, and the 64 k-token pilot reported rather than buried. Two mechanical limits hit and now recorded in Model roles: seats enumerate at session start (both stand-ins ran as prompt-prefixed existing seats), and the stand-in files written into the orchestrator's tree — the only place the harness reads them — were flagged, accounted, and removed, with the boundary stated: a seat that must persist lands through the mapping's sign-off.
- *The CHANGELOG amputation is the cycle's defect chain, and every link held except the first.* A stray diff3 ancestor marker landed inside 2b4f99b's conflict resolution; 5a966f3's rebase then cut 1,669 lines — every release before this cycle — because the marker breaks git's own re-merge; #227 found it, reported it in its close, and restored the file the same day with no entry lost; #231 then landed the mechanical refusal in both `just check` (0.17 s on a clean tree) and `just land`'s ladder (judging the *rebased* tree, so an inherited marker refuses the next landing), pinned the fixture against what git actually writes, declined `merge=union` on a measured silent misfile rather than on taste, and surfaced the local cause — this box's global `merge.conflictStyle = diff3`, which makes the form that slipped the shape every conflict has here. The same-session filing rule held at every link.
- *Tools catching their authors, three times in one cycle.* `just land`'s first-ever invocation refused its own author's `gate_red` (ty diagnostics on the recipe's own tests; nothing pushed), and every subsequent initiative landing flowed through it. gitleaks' first catch was #223's own test fixture, on the commit that added the scanner. `just prereqs`' first cut reported the Codex config `ok` and 88318cc corrected it to `unverified` — a written file whose key spelling nothing has proven is not a verified file — the vacuity rule's shape. Named as a pattern in this journal; no rule added, because the strictness principle already owns it and each catch is that principle's mechanism working on its maker.
- *The protocol events at scale, mostly orchestrator-side, corroborated in-repo.* #222's flake fired across four arrangements and six full-suite runs, a different parameter each time, and every gate that met it quoted the class and re-ran once as briefed — `flake_quarantine`'s required response running distributed, the failure-class table's newest exemplar. #170 parked cleanly under the freeze with its fix committed and its handoff current (`f4ce30f`, unpushed — its tree must not be reset). The four BLIND watcher findings resolved by recovery.md's by-hand look as finished-and-cleaned agents, the first BLIND that cleared rather than caught (runbook ×16). And the freeze-propagation caveat from #217's 17:12Z comment — a freeze recorded durably does not reach a session already running, ADR-0042's stale-copy shape one level up — is recorded here as an offered input for the orchestration doc that remains recommended to the human, not landed from a seat that cannot read orchestrator memory.
- *#224 pre-registered the bar before any foreign number existed, and the derivation constrains the ADR it serves.* The eligible population (N = 131 of 168 closes, every close accounted into a bucket) shows two of ADR-0061 decision 6's named signals cannot be pre-registered from this history at all — gate cycles to first green is recoverable in 3/131, quota consumed in 0/131 — and reopens/assignments are degenerate at zero. The bar, the D6 constraints, the seat-eligibility divergence (`SEATS` admits review and bars orchestrator; the issue body says neither) and the breaker's class emissions all sit where they belong: #224 and #228, with the human. This retro touched none of them.

**Applied** (under ADR-0063): the seat-enumeration sentence in Model roles; the #218/#219 measurement clauses in the five-minute bullet (rules and interims unchanged); the conflict-marker word on the `just check` row; failure classes ×9 → ×10 (#222; #41 dropped to the prune record); elimination-context ×10 → ×11 (#218/#220/#232; #156/#152 dropped); recovery runbook ×15 → ×16 (the BLIND resolution); retro skill ×23 → ×24; banner to twenty-four.

**Rejected / no change.** Everything on #228, by design (rows for five landed recipes, the gitleaks word, two failure-class rows, the profile extension, review-diversity wording, seat eligibility) — the first retro whose scope had to exclude a concurrent human-gated batch rather than fold it. "Measure before building" (stays on #217, re-weighed at five-for-five without the rule). A convention-lands bump for #213 landing via itself (strictness operating, not a convention landing). Any freeze/reservation amendment (human rulings; context). A probe-window move (#170 counts at its landing). ADR-claiming unmoved (0061/0062/0063 claimed and landed without collision — the rule operating).

**Status.** Failure classes ×9 → ×10. Elimination-context ×10 → ×11. Recovery runbook ×15 → ×16. Retro ×23 → ×24 (first retro landed via `just land`; first scope-exclusion of a concurrent gated batch; fourth consecutive plugin-cache load). Probe-window ×8, convention-lands ×5, ADR-claiming ×7 unmoved. `playtest-brief` ×2, `playtest-ingest` ×1 — no playtest; #174's deferred feel judgements and the Reinforce scenario still wait on the next brief. Open for the human: the pile on #217 (its newest comment is current, posted by this retro).

**Prune record** (per #201/ADR-0060: dropped from CLAUDE.md, verbatim; counts unchanged).

*Failure classes (×10, list stays 5):* "#41: the play-session guard skipped itself silently when `tasklist.exe` was off `PATH`; the fix routes \"the check could not run\" to `infra_unavailable`, because a check that could not run is not a check that passed."

*Elimination-context (×11, list stays 5):* "#156/#152: both of one evening's batch closes checked their inherited item lists against the tree that had moved under them — #156's audit found two items already discharged by #164's landing and said so rather than re-doing them, #152 verified that neither #145 nor #156 had consumed any of its six before starting — the first applications of the line to an inherited work list rather than a measurement or rationale."

## 2026-08-06 — retro: scheduled, ten issues (#225, #224, #228, #218, #219, #220, #92, #238, #239, #240)

Trigger: ten closes since `879d6e9`. Unattended; delegated batch recorded as ADR-0065 (`Reviewed-by-human: pending`, claimed by comment on #217). Landed via `just land`. **The claude-md-management skill surfaced through the Skill tool for the first time in five retros** — invoked directly, no cache load; the propagation gap the twenty-first through twenty-fourth recorded is closed as observed from this seat. The launching worktree still served this seat a stale CLAUDE.md and a retro skill at ×14 against `origin/main`'s ×24 — ADR-0042's shape, handled by working from the fresh `retro-25` tree, no new sentence. The WIP reservation and the freeze held throughout the cycle. **Review queue at this retro: 8 pending on-tree (0051, 0053, 0054, 0057, 0060, 0062, 0063, 0064) — 9 once 0065 lands.** The #217 ruling of 2026-08-05T21:50Z scheduled the clearing session; it, the orchestration doc (that ruling's decision 5), #221's second initiative ADR and the AGENTS.md symlink are all ruled-but-unexecuted — the crash cluster below is the plausible interruption — and each stays on its issue rather than being executed from this seat.

**Findings.**

- *Crash cluster two, and a death as an accidental reviewer.* Multiple harness deaths in one evening (root cause found and fixed by the human, out of scope); every in-flight track resumed from disk records rather than memory — `origin/main` untouched, commit-early held, #243's and #170's trees intact. The notable resumption: the #237 read-arm runner died mid-block (its run.log ends at "R3 start"), and the death exposed a fault the survivor could not have reported — 300 s idle controls against 877 s read blocks could not price a noisy shared account, two identical 35 Mtok blocks measuring +2 then +0. The resumed agent judged the surviving data inconclusive rather than rounding it and redesigned with wall-clock-matched ABBA controls (`R Q Q R R Q`), the fault and correction stated in the completion script's own header. Honesty under sunk cost; recovery runbook ×17. A residue surfaced at this retro's own gate: the cluster killed both review-watcher assessors (0-byte logs), and their unacknowledged `watch_broken` findings turned `just fast` red on a docs-only diff, because the recipe test injects the breaker directory but reads the live watch tree — resolved by the runbook's by-hand look (both watched agents finished and landed) and `just watch-report --ack`; the missing test seam is filed as #249.
- *The quality track ran end to end in one cycle, and its first act was correcting an inherited verdict.* #239 re-ran mutmut rather than inheriting #172's "incompatible with our pytest config": the tool runs (the incompatibility was `source_paths` naming a file, unmaking the `cti_daemon` package, with `-n auto` taking the blame) and its reporting cannot carry a floor (three survivors on a sound module, all equivalent mutants) — elimination-context ×12, first application to a tool evaluation. The smoke's build found three bugs that would each have shipped a gate that could not fail (a mutant's fate read from output not exit code; a qualified test name misattributed; a foreign coverage context counted), each now a regression test. #240 exercised the review seat for real in both directions — z.ai lens over a Claude landing, Claude lens over the z.ai landing — with citation resolution counted mechanically, 12/13 pooled and the miss counted against; the exercise's one defect came out of running it, filed as #245 (the ledger crediting a review with a commit made before it started) — tools catching tools again. #241/#242/#244/#246 filed and queued. The assert-true hole is closed for Python (ADR-0064's named-list escape), shell/Rust wait on #246 (low priority per the human), and SQF's economic rationale is owed to the tree there too.
- *First foreign-lane work landed, and the evidence sweep's own hypothesis did not survive its reproduction.* #92: authored by glm-5.2, finished by the orchestrator where lane permissions stop — the finisher-cost decision sits on #221 (2026-08-05T21:47Z) awaiting the human; the dispatched session's honesty (no hallucinated green, explicit continuation request) is the lane's first good data point, recorded at the bar once the rework window closes. Mid-verification the orchestrator raised a false alarm from a clean-tree misread off filtered pipe output and corrected it by forensics before any accusation — pipe-filter hygiene now twice observed, and the hazard lives in the user-global rtk chain this repo does not own, so the proposal goes to the orchestrator's report, not the prefix. The cycle's two hook-chain observations were then reproduced rather than filed as handed: fenced text in a double-quoted `--body` executes because of **bash backtick substitution**, not the hooks (marker test; rtk passed the identical payload through with no `updatedInput`), and the heredoc denial was `block-no-verify`'s designed fail-closed, whose one safe authoring shape (quoted heredoc, #167) is also the execution-safe one. Filed as #247 with the reproduction and a proposed guard; rtk's own fail-open on unreadable JSON noted there as benign-for-a-rewriter.
- *The off-peak rule landed genuinely hard, and declined a failure class on the table's own reasoning.* #238: no override flag, no environment variable, a parser test refusing every option that would move the clock; the window has one home (`tools/breaker.py`'s schedule object) with a test refusing the literals anywhere in the dispatcher's source — which caught its own author's docstrings before it caught anyone else. The refusal carries no class because the table types what a run *found* and this refusal found nothing — `admission_escalated`'s reasoning, applied; per the twentieth's precedent that is design within the table, not an earn. The first probation dispatch's compliance was by luck (Thursday 05:11 SGT against a 14:00–18:00 band) and its close states that as caution, not comfort.
- *The lane roster grew on rulings, and the plan-currency instrument grew a finding.* Codex unblocked by two human rulings — the per-lane credential policy (#229, 21:59Z) and the terms-read blocker discharged with the risk acceptance stated as the human's own (2026-08-06, 22:13Z on #229); #243 was in flight through the crash and resumed. Herma scoped as the third lane (#234). And the API's `weekly_scoped` entry shows the Fable weekly cap at 86% with an active warning — the currently *binding* limit, which neither #218 nor #220 priced and the statusline tap does not record (`five_hour`/`seven_day` only); recorded on #237 so the model's completion prices the constraint that binds.
- *The initiative's gate needs a vehicle that is open.* Since #228 was ruled, landed and closed, three gated proposals accumulated on closed threads: #238's `just dispatch` clause, #240's pointer-paragraph third document, #240's dead-allowlist swap (`Write(.claude/skills/**)` and `Write(docs/**)` match nothing — the harness routes edits through `Edit`). The standing-pile lesson (#200 → #217) applies to the initiative's own gate, so the retro filed **#248** as the second batch vehicle, carrying the three verbatim plus a pointer to #221's finisher decision — executing ADR-0063's split, not amending it (ADR-0065 decision 2).
- *The reconciliation clause ran in its ordinary form for the cycle's one lane-blind recipe.* #239's `just mutation` is commissioned from lane-quality questions but gates every landing identically, so its row is not initiative-gated: `just fast`'s row now reads `check` + `unit` + `mutation` and the new row states the no-flag floor and the `NO_PYTHON_SUBJECT` named-list escape — a gate met after every edit must not be discovered by denial (ADR-0065 decision 1).

**Applied** (under ADR-0065): the `just mutation` row and the `just fast` row's third stage; elimination-context ×11 → ×12 with the prune-on-append (#159 dropped, below); recovery runbook ×16 → ×17; retro skill ×24 → ×25; banner to twenty-five.

**Rejected / no change.** Everything on #248, by design — the second vehicle continues ADR-0063's split. A failure-class earn for #238's declined class (design within the table; the twentieth's precedent). A pipe-filter rule in the prefix (the hazard is in the orchestrator's user-global rtk chain; proposal handed to the orchestrator's report for RTK.md). A convention-lands bump (#240 landing `docs/review-dispatch.md` with its two first exercises is the rule operating, not a new earn). A probe-window move (no in-world landing among the ten; corpus untouched). ADR-claiming unmoved (0064 and 0065 claimed and landed without collision — the rule operating).

**Status.** Elimination-context ×11 → ×12. Recovery runbook ×16 → ×17. Retro ×24 → ×25 (first Skill-tool surfacing of the plugin; second retro via `just land`; first evidence-sweep item reproduced-and-refuted before filing). Failure classes ×10, probe-window ×8, convention-lands ×5, ADR-claiming ×7 unmoved. `playtest-brief` ×2, `playtest-ingest` ×1 — no playtest; #174's deferred feel judgements and the Reinforce scenario still wait on the next brief. Open for the human: the pile on #217 (its newest comment is current, posted by this retro).

**Prune record** (per #201/ADR-0060: dropped from CLAUDE.md, verbatim; counts unchanged).

*Elimination-context (×12, list stays 5):* "#159: the finding's line number had been moved under by #79/#82 shifting the spawn count onto the roster, so the live reader of the invented 8 was by landing time the `squad_reinforced` arm; the agent checked rather than trusted the filed ground, fixed both squad arms, and stated the shift in the close — the first application of the line to a filed defect's ground."

## 2026-08-06 — retro: scheduled, ten issues (#243, #245, #241, #249, #250, #254, #251, #252, #253, #259)

Trigger: ten closes since `d5f778e`, reached by #259. Also in the filing cycle: #260 and #270, the first two dispatches from the opus/high orchestration-seat trial; both began on z.ai, stopped on `quota_exhausted`, resumed on Codex, and were hand-finished only where #265 stops that lane. Unattended, and the first fable act ever run on a foreign lane: #217's one-profile allowance, mechanised by #270, expires at `2026-08-10T14:00Z`; the orchestrator seat, every other fable route and the #181 shape stayed barred. The Codex seat did not surface claude-md-management as a skill, so the incremental pass loaded its cached 1.0.0 instructions as the twenty-first through twenty-fourth retros did; report 87/100 (B), with currency — not architecture — the deficit. **Review queue at this retro: 9 pending on-tree (0051, 0053, 0054, 0057, 0060, 0062, 0063, 0064, 0065).** The human reviewed and approved all nine on #217 earlier the same day; #256/#257 carry the still-unlanded transcription and amendments, so the anchored grep correctly remains nine.

**Findings.**

- *The cycle genuinely ran on two foreign lanes, and only one could finish.* #259's z.ai dispatch ran the gate, committed and landed without a Claude-side finisher, then accrued the admission bar's first implementer assessment. Codex ran the same substantive loop on #259, #260 and #270 through commit, then met #265's exact `cog check` red because the writable linked-worktree git directory makes libgit2 report the repository missing. Each agent stopped rather than re-run, work around or land; the orchestrator finished. This is now an admission-system blind spot rather than one dispatch's inconvenience: a route that cannot finish cannot accrue an assessment, so every Codex route remains at 0/10 while #265 stands. The defect already has the right issue; no process sentence can make an unfinishable lane judgeable.
- *The lane-death-and-resume path needs no new rule: its two existing halves already said the whole thing.* #260's and #270's first z.ai attempts each received one provider 429 within four minutes, typed `quota_exhausted`, carrying reset `2026-08-07 05:31:53`. Neither partial run was interpreted or retried into the closed lane; both were re-dispatched to Codex with explicit resumption briefings and completed. That is the failure-class table's required response verbatim, at concurrent scale, so failure classes ×10 → ×11; and the recovery contract's first use across a lane change rather than a restarted session on one provider, so recovery ×17 → ×18. The same table handled #222 again across five agents' gates: quote the known flake, re-run once, act on nothing. It corroborates the existing #222 exemplar and does not earn a second count in the same cycle.
- *The orchestration seat drop became an instrumented trial before its first cycle.* #242's ruling moved the standing loop to opus/high and kept fable as dispatched named acts; #260 pre-registered all five failure criteria, separated the three mechanical readings from the two hand judgements, kept clean reporting silent, and started the clock only on the orchestrator's explicit `trial-start`. Cycle 1 is the #270 dispatch cycle, not the tool's landing. The current Model-roles bullets still describe fable as the orchestration seat, and the command table's admission row predates both `audit` and the trial surface; exact replacements go to the human gate, not this diff.
- *Four rule-based coordination procedures left model memory, and their always-loaded rows now owe the reconciliation the human already ruled.* #250 made the freeze, WIP limit and carve-outs a per-dispatch policy read; #251 derived the gate line and refused to invent a briefing's variable half; #252 moved the close audit out of generation while refusing to call quoted evidence green; #253 mechanised the runbook's two computable recovery reads while leaving judgement empty. Human ruling on #217 item 13 sends their four CLAUDE.md rows through this retro. One row expands the existing `just admission` entry rather than inventing a second home; `just queue`, `just brief` and `just recover` are additions.
- *The queue's six wrong suggestions were tracker data, not a scheduler rule.* Phase anchors #1, #3, #4, #5 and #6 wore `ready-for-agent`; `just queue next` offered them across six reads, while #241's readiness rung would have refused a dispatch. Removing the labels removed every anchor from the live candidate list. Both layers did what they own — selection reflects labels, readiness rejects bodies — so no queue clause or code issue is earned.
- *A preservation ref is durable work, and the worktree protocol said it would be lost.* #260 was refused with `surface_conflict holder=170 paths=CHANGELOG.md`. `just worktree done issue-170` then said removing the clean tree would lose three commits because they were not on `origin/main`, although its exact HEAD was already the pushed `origin/issue-170-parked` tip and the handoff had made that ref the durable home. The human verified and removed the tree. Filed #272 for an explicit archive/restore path; `done` stays landed-only and gains no force.
- *Codex hook parity proves translation, not that edit hooks see a Codex edit.* Two Codex agents needed a hand-run `uv run ruff format` before the gate went green. `.claude/settings.json` aims the formatter and after-edit linter at `Edit|Write`; the parity tests preserve that matcher, but no live proof shows a Codex edit emits either name. `just check` contains the defect, so no bad formatting landed, but a whole failed-gate round trip remains. Filed #273 for a captured PostToolUse edit payload and an in-vivo proof; no resident manual-format workaround is proposed.
- *#254 re-derived a filed diagnosis before fixing it, and the diagnosis did not survive.* The issue blamed ordinary hyphenated prose on the live short-bypass regex. Replaying all three denied commands against `origin/main` showed none failed there, then isolated every denial to a stale pre-#167 hook copy whose unreadable branch accused a bypass. The live regex still got a defence-in-depth narrowing, but the defect that explained the sightings was the denial's false accusation. Elimination-context ×12 → ×13, first whole defect report re-derived into a different bug before its fix.
- *The measured-cost and routing rulings arrived before this retro and need no second proposal.* #237's wall-clock-matched controls put reads near-free on this plan and showed the uncorrected answer had been wrong by 5×; #258 found orchestration, not the foreign finisher, was the spend and its class table was agreed by the human. The human subsequently adopted “measure before building” on #217 and routed its gated wording through #257. This retro records the exemplar and does not reopen either ruling.

**Applied.** This journal entry; #272 (preservation-ref archive/restore) and #273 (Codex PostToolUse edit parity); issue-side evidence on #222, #250, #261 and #265; the consolidated human pile on #217. No CLAUDE.md, ADR, CONTEXT.md, acceptance spec, settings, skill or schema edit landed from this dispatch.

**Proposed at the human gate, not applied.** The four ruled orchestration rows; the opus/high orchestration-seat wording; #270's exact temporary fable allowance in Model roles, the dispatch row and ADR-0061; the process banner twenty-five → twenty-six; failure classes ×10 → ×11 with #260/#270 appended; recovery ×17 → ×18 with their two lane-changing resumption briefings appended; elimination-context ×12 → ×13 with #254 appended; retro ×25 → ×26 for the first foreign-lane fable run completing the procedure and respecting every gated boundary. The exact text is in #271's close and the newest pile comment on #217.

**Rejected / no change.** A new provider-death or recovery rule (the `quota_exhausted` row owns the reroute and the recovery contract owns the briefing; both worked unchanged). A second failure-class increment for #222 in the same cycle. A queue-ordering clause for bad labels. A standing Codex formatting instruction (the gate catches it and #273 owns the mechanical seam). A convention-lands move (the four rows are proposed, not yet applied). Probe-window and ADR-claiming moves (no probe or claim collision in scope). The corpus: no diff from this retro reaches `addons/`, `missions/`, `extension/`, the daemon's world-facing half or the manifests, so `just regress` is not owed.

**Status.** Proposed, with gated markers unchanged on-tree until the human accepts them: failure classes ×10 → ×11 (#260/#270, `quota_exhausted`); recovery runbook ×17 → ×18 (#260/#270, first lane-changing resumptions); elimination-context ×12 → ×13 (#254); retro ×25 → ×26 (first fable-on-foreign run, full procedure unchanged). Probe-window ×8, convention-lands ×5 and ADR-claiming ×7 unmoved. `playtest-brief` ×2 and `playtest-ingest` ×1 — no playtest; #174's deferred feel judgements and the Reinforce scenario still wait on the next brief. Open for the human: the newest pile on #217; the review queue is reviewed but remains nine on-tree until #256/#257 land.

**Proposed prune record** (per #201/ADR-0060: archive verbatim when the gated marker edits are accepted; lists stay at five).

*Failure classes (×11):* "#83: `run.sh`'s non-hold verdict path retyped every in-mission FAIL as `assertion_failed`, including ones the world had typed `timeout` or `oracle_disagreement` — found by review because a wrong class is by definition a harness bug, and the fix put the classification under unit test in the no-Arma tier, so a wrong class is now a red `just unit` rather than an in-world discovery."

*Elimination-context (×13):* "#147: the tree had been reshaped four times since filing (#171, #161, #162, #185), so each of the seven findings was re-located before being fixed — all seven fixed, none declined; the line's first application to a whole finding list."

**Amendments accepted and landed** (2026-08-07, #274). The human's guided decision session of 2026-08-07 signed off every proposal above; the sign-off list is the newest comment on #217, and #274 transcribed it verbatim without re-deriving any wording. Landed: the four ruled orchestration rows (`just queue`, `just brief`, the expanded `just admission` replacing its predecessor rather than joining it, `just recover`); the permanent Model-roles wording, the fable bullet losing the orchestration seat and the opus/high bullet gaining the #242 trial sentence; #270's temporary `codex-sol-xhigh` retro allowance in Model roles, in the `just dispatch` row's *Run when* column and in ADR-0061 Decision 2; the banner at twenty-six retros; failure classes ×10 → ×11 and elimination-context ×12 → ×13, each with the exemplar above appended and its oldest entry dropped into the prune record above, both lists still at five; recovery runbook ×17 → ×18. **Not landed:** the retro skill's ×25 → ×26 and its appended sentence — approved by the human, but the transcription dispatch was refused write access to `.claude/skills/retro/SKILL.md` and did not work around the refusal, so that one marker stays outstanding on an approved-but-unlanded footing. Probe-window ×8, convention-lands ×5 and ADR-claiming ×7 are unmoved and byte-identical to `3daabaf`.

## 2026-08-08 — retro: scheduled, eleven issues (#235, #274, #269, #275, #272, #261, #89, #215, #90, #71, #73)

Trigger: eleven closes since retro 26 (`#271`), reached by #73. Unattended, on the same one-profile foreign-lane allowance as retro 26: `fable` on `codex`/`codex-sol-xhigh` until `2026-08-10T14:00Z`; the orchestrator seat, every other fable-on-foreign route and the #181 shape stayed barred. First retro whose evidence accrued during the cycle in a durable bank (#276), rather than being reconstructed afterwards; the human added orchestrator WIP adherence there and supplied two corrections plus three questions to answer. The Codex seat again did not surface claude-md-management, so the incremental pass loaded cached 1.0.0 instructions; report 87/100 (B), with currency the deficit — three landed recipe changes still lack current command rows, and the dispatched-session wording proved incomplete. **Review queue at this retro: 9 pending on-tree (0051, 0053, 0054, 0057, 0060, 0062, 0063, 0064, 0065).** The human approved all nine on #217 on 2026-08-07; the anchored grep remains the honest depth until their headers land. Retro 26's skill marker, recorded as not landed at that entry's commit, subsequently landed at `a805b28` under #274.

**Findings.**

- *The cycle ran on two foreign lanes unaided, but Codex still cannot enter its admission record.* z.ai finished four issues without a Claude-side finisher and stands `assessed=3/10 unclean=0/1`, the first route to accrue three clean assessments. Eleven closes now state that no Codex route can be assessed while #265 forces the orchestrator to hand-finish `cog check` and landing; the live-probed parent-root candidate was refuted and reverted at `03a3721`, so the commit-without-gate ceiling remains. No process sentence can turn a hand-finished route into a measured one.
- *Elimination-context earned once, not three times.* #90 established the caller set before a rename batch and excluded an unrelated generic match; #73 found the filed daemon read-timeout already fixed at `3c1b9e0` and did not rebuild it; #215 found no absent header key, but a complete contract scattered across several homes. One combined exemplar proposes elimination-context ×13 → ×14, with #189 pruned below. #275's stale feeds table corroborates the rule but does not inflate the same cycle's count.
- *The failure-class table again removed judgement from agents, but in already-proven modes.* Two `quota_exhausted` deaths rerouted without interpreting partial work, and #222 reached seven recorded arrangements with every incidental reader quoting, rerunning once and acting on nothing; the latest two parameters straddle the minute boundary and refute the 60-second-carry theory. These repeat the current quota and flake exemplars, so failure classes stays ×11 and recovery stays ×18.
- *The orchestrator breached its own WIP ruling, and the offered constraints do not excuse one-deep.* The seat ran at 1/3 with 54 ready issues and regressed after each of the human's two corrections because it preferred tidy landing-first reporting. Keep three: the cycle supplies no saturation or quality evidence for changing the human's number, and one finisher-free lane makes the original constraint weaker, not stronger. The surface sequencer does not make three unreachable: it sees only paths already touched, sequences one conflicting candidate, and the cycle itself reached WIP three. Mechanise the underfill verdict without automating dispatch or changing the ruling: #278 folds the queue's existing derivation into `just watch-report`, with refill-before-landing as its action.
- *Detached dispatch lost the completion edge and spent 8h10m of open-lane capacity.* Agent-tool children used to re-invoke the orchestrator on exit; #223's detached OS processes write `result.json` and wake nobody. A harness-tracked waiter restores the edge only while the orchestrator session lives; `tools/dispatch.py` alone cannot create that attachment, and cross-session scheduling remains with the human. #280 packages and live-proves the within-session seam without pretending it solves the scheduled half.
- *Two new single-shot stall shapes need one short rule and one mechanical half.* #265's urgent revert dispatch ended asking a question nobody was listening to, and #215's first dispatch ended waiting for a background gate notification that cannot start a second turn there — the classic stall's 227th instance, but a new context. #279 carries the brief invariant and a `CTI_DISPATCH_ID`-aware background-work denial; the exact CLAUDE.md sentence follows through the human gate and lands only with that mechanism.
- *The incremental CLAUDE.md pass found currency, not a new architecture gap.* #235's `just verdict --post`, #272's worktree `archive`/`restore`, and #215's `just probe-contract` owe exact row replacements through ADR-0057. The file remains 87/100 (B): commands 19/20, architecture 18/20, non-obvious patterns 15/15, conciseness 12/15, currency 10/15, actionability 13/15. Exemplar lists remain capped at five under the proposed elimination append.
- *A gate catches what it contains.* #275's first pass said formatting was done and skipped `ruff check`; `just fast` found fifteen errors. #235 added a user-visible flag without a changelog entry, and no gate noticed. One changelog miss is evidence, not yet a sound predicate for a mechanical check; the existing convention remains and the instance is recorded rather than widened into guessed cases.

**Applied.** This journal entry; implementation issues #278 (WIP underfill verdict), #279 (single-shot dispatch guard) and #280 (completion edge); issue-side findings on #222, #265 and #276; the exact gated pile on #217. No CLAUDE.md, ADR, CONTEXT.md, acceptance spec, settings, project-skill or schema edit landed from this dispatch.

**Proposed at the human gate, not applied.** Three command-table updates for #235, #272 and #215; one working-style sentence for a dispatched session's single-shot contract; process banner twenty-six → twenty-seven; elimination-context ×13 → ×14 with #90/#73/#215 appended and #189 pruned; retro ×26 → ×27 for the first live-banked retro integrating and answering a human-added self-audit while completing the full procedure under the same narrow foreign-lane allowance. Exact text is in the newest pile comment on #217.

**Rejected / no change.** Raising or lowering WIP three (no measurement supports either; the number remains the human's). Treating `CHANGELOG.md` contention as two permanently lost slots (the sequencer's actual semantics and WIP-three observation refute it). Automatic dispatch from the underfill report (ADR-0053 keeps notice separate from action). A failure-class or recovery move for repeated quota/flake/resumption modes. A changelog gate designed from one miss. Probe-window ×8, convention-lands ×5 and ADR-claiming ×7 (no new validating context); `playtest-brief` ×2 and `playtest-ingest` ×1 (no playtest). The corpus: the only landed path is `docs/process-log.md`, so no path reaches `addons/`, `missions/`, `extension/`, the daemon's world-facing half or the manifests; `just regress` is not owed.

**Status.** Proposed, with gated markers unchanged on-tree until the human accepts them: elimination-context ×13 → ×14 (#90/#73/#215); retro ×26 → ×27 (first evidence bank accrued live, first explicit orchestrator self-audit). Failure classes ×11, recovery runbook ×18, probe-window ×8, convention-lands ×5, ADR-claiming ×7, `playtest-brief` ×2 and `playtest-ingest` ×1 unmoved. Open for the human: the newest pile on #217. Review queue: nine pending on-tree.

**Proposed prune record** (per #201/ADR-0060: archive verbatim when the gated marker edit is accepted; the inline list stays five).

*Elimination-context (×14):* "#189: ADR-0052's ruling 4 embedded an untested engine premise — that AI succession moves leadership off a dead player — which the vendored wiki states neither way; a 120 s observation-only leg disproved it in-world, and the built reclaim was withdrawn rather than landed inert — the line's first application to a decision ticket's premise."

## 2026-08-09 — retro: scheduled, sixteen issues (#233, #237, #242, #244, #246, #248, #255, #256, #257, #262, #263, #264, #273, #289, #292, #295)

Trigger: sixteen closes since retro 27, against the newly restored five-close trigger — one retro consumed the whole interval rather than treating three overdue thresholds as three findings. First retro on `codex-sol-max`, under the human's exact nine-profile ruling recorded at #300; unattended, process-only and proposal-only, with `docs/process-log.md` the only landing path. The claude-md-management skill was not exposed to this Codex seat and no installed cache copy was discoverable, so its established incremental rubric was applied manually: **85/100 (B)** — commands 18/20, architecture 18/20, non-obvious patterns 15/15, concision 12/15, currency 9/15, actionability 13/15. **Review queue at this retro: 5 pending on-tree (0064, 0066, 0067, 0068, 0069).**

**Findings.**

- *The orchestration seat is presently the process's weakest component.* #295 measured its diagnosis of the sawtooth wrong by about 130×: an 837-output-token brief explained at most 5 of 669 lost agent-minutes, while 89% accrued asleep behind a follower waiting for a cohort's last completion; one 120-minute wait was 68% of its block. #234 then showed the same seat writing a brief from title and memory that widened an explicit human ruling, and #242 had recorded only one of four orchestration-trial cycles before a backfill. Agents caught or bounded all three; balancing the report would hide where the errors originate.
- *The WIP number conflates active capacity with three other lifecycles.* A routing probe can create an issue tree without an occupied slot; parked #216 stayed open and counted; landed-but-open #291 counted until manual closure; and fourteen closed trees remained in the raw worktree list until the seat remembered `just worktree done`. The live `just queue state` read was 5/5 from #216, #288, #290, #291 and this retro, while neither the list nor its count described active agents. The implementation is behaving as #250 designed it, so this is a semantic decision rather than a local counter bug: #301 asks the human to separate active agent slots from open-work inventory and to amend #284's exposure definition before live treatment.
- *Two agents respected decision authority the seat's briefs did not.* #216 re-derived a conditionally approved proposal after #237 repriced its premise from 0.575% of an input-equivalent bill to about 0.06% of the plan meter, recommended rejection, and left the human's decision open. #234 planned to the ruling rather than the wider brief. The first earns elimination-context ×14 → ×15 as a new context — an approved-but-not-executed proposal whose rationale expired — and the second earns a short briefing-ground-truth proposal, not a marker inflation.
- *Measure-before-building earned once against the seat itself.* #295 measured the orchestration seat's own favoured explanation, found the cohort wait instead, and rejected batching because it deepens that mechanism. That is the named ×5 → ×6 exemplar. #237's cache arm and #281's mutation-engine comparison also retired proposed work, but their results are already represented by current measurement/elimination exemplars and do not add counts merely because the bank is long.
- *Execution capability, not authorship category, is now the dominant foreign-lane constraint.* #294 found two independent mechanisms barring every dispatched session from `.claude/`; #264 found `ln -s` refused where `mv` was allowed; #281 was correctly routed to Claude and still could not execute there; #234 lacked network, `just` and `uv`. Three route exceptions in three days (`pure-transcription`, `no-gated-landing`, `proposal-only`) are design debt, not evidence for a fourth. #298 owns the human's stronger objective — make authorship irrelevant by building independent safety — and this retro proposes no policy widening.
- *The full gate twice caught what a targeted green missed, but no new rule follows.* A stale parameter survived a one-file Ruff check, and an admission-bar omission survived two targeted modules; the existing `just fast` obligation found both. These are ordinary applications of the gate, not new failure classes, and they do not justify another verification pass.
- *Retro value is sublinear in cadence, but five closes should stand for now.* At sixteen closes in one day the evidence is correlated, the command/marker/review-queue sweep has a fixed cost, and mechanisms often have not matured between twice-daily runs; value per run therefore falls as the interval shrinks. It did not fall to zero: this one-day interval produced #295's refutation, #234's ruling breach and #301's experiment-invalidating semantic gap. This is the first sample after cadence restoration, so there is no measured basis for a time floor or a return to ten. Keep five; an overdue retro consumes all evidence since the prior completed one without multiplying marker counts.
- *The incremental AGENTS/runbook pass found concrete currency gaps, not a case for compression.* `AGENTS.md` still describes the pre-#244/#246 Python-only mutation gate and lacks #295's `just occupancy` row. `docs/agents/orchestration.md` still calls its already-landed pointer a proposal, names `CLAUDE.md` rather than the source `AGENTS.md`, and says nothing about reading a ruling before composing a brief. Exact replacements are on #217. The project-skill update is also exact there; #294's wall was reported and not worked around.

**Applied.** This journal entry; decision issue #301; the issue-side audit on #216, #234, #246, #276, #284, #294, #295 and #298; and the exact gated pile on #217. A tracker anomaly surfaced while filing #301: the tree already cited #300 for the human's profile ruling although no ticket existed, so #300 was made the closed record of the already-executed ruling and the WIP decision received #301. No new ruling was invented. No `AGENTS.md`, ADR, CONTEXT.md, acceptance spec, settings, project-skill, schema or routing-policy edit landed.

**Proposed at the human gate, not applied.** `AGENTS.md`: banner twenty-seven → twenty-eight; the mutation row reconciled with #244/#246; the `just occupancy` row; measure-before-building ×5 → ×6; elimination-context ×14 → ×15. `docs/agents/orchestration.md`: one sentence making the issue thread and explicit ruling the brief's ground truth, plus source-name and stale-proposal cleanup. Retro skill: ×27 → ×28 for the first full run on `codex-sol-max`, under the restored cadence, which kept its scope and authority boundaries while treating the briefing seat as evidence rather than arbiter. Every replacement is quoted verbatim in #217's newest pile comment.

**Rejected / no change.** A cadence change from one post-restoration sample; treating twice-daily velocity as constant-value marker growth; changing the human's WIP limit rather than its ambiguous denominator; a fourth routing exception; closing #216 on agent authority; a new full-gate or changelog pass; failure classes ×11, recovery ×18, probe-window ×8, convention-lands ×5, ADR-claiming ×7, `playtest-brief` ×2 or `playtest-ingest` ×1. The only landed path is `docs/process-log.md`, so no path reaches an in-world surface and `just regress` is not owed.

**Status.** Proposed, with every gated marker unchanged on-tree until human acceptance: measure-before-building ×5 → ×6 (#295); elimination-context ×14 → ×15 (#216/#237); retro ×27 → ×28 (first `codex-sol-max` run under the restored cadence, full procedure and proposal-only boundary intact). All other markers above remain unchanged. Open for the human: #301, #216, #298, the standing #52/#54 hardware decisions, and the exact Retro 28 batch on #217. Review queue: five pending on-tree.

**Proposed prune record** (per #201/ADR-0060: archive verbatim when the gated marker edits are accepted; lists stay at five).

*Measure before building (×6):* "#195's inherited assumption gave way on measurement to #197/#198/#199"

*Elimination-context (×15):* "#157/#165: two closes in one cycle re-derived a filed premise rather than trusting it — #157's hand-maintained probe list defended itself with \"these two are the corpus's contended leg\" while four later issues had made it six, self-checking against a rename and blind to addition, fixed by deriving the list from the probe headers; and #165 re-read \"run by nothing\" against the tree, found `just spike` boots and `just build` packs the mission, and declined the asked-for deletion with the stay of execution recorded in the file's own header."

## 2026-08-10 — retro: scheduled, nine issues (#288, #294, #296, #297, #302, #303, #305, #306, #212)

Trigger: nine closes since retro 28 (`780c309`), past the restored five-close cadence. First **dispatched** retro on `claude-native` — `fable-high`, one of #300's nine approved profiles — chosen deliberately so the claude-md-management skill could surface after three codex retros could not load it live. Result: the skill **listed but errored on Skill-tool invocation, twice**; the pass ran from the 1.0.0 plugin cache exactly as on codex. The propagation question is now three-valued — codex not-listed, orchestrator-side invocable (retro 25), dispatched `claude-native` listed-but-erroring — recorded as one observation of the third state, no proposal. Evidence accrued live in #307 (second use of #276's precedent), including the orchestration seat's own defects, unflattered; two bank findings were flagged for this retro's judgement rather than transcription, and one of them did not survive it. Incremental pass score: **84/100 (B)** — commands 17/20, architecture 18/20, non-obvious patterns 15/15, conciseness 12/15, currency 9/15, actionability 13/15. **Review queue at this retro: 5 pending on-tree (0064, 0066, 0067, 0068, 0069) — and that number is also the genuine one; see the first finding.**

**Findings.**

- *The bank's claim of a defect in this skill's own step 3 did not survive re-derivation.* Finding 6 said the pending-ADR grep returns eleven where five are genuine, six ADRs (0013, 0019, 0029, 0031, 0042, 0047) being approved-but-never-transcribed, and #217's pile item 2 offered a mechanical dispatch to fix their headers. Re-run from this seat: the **anchored** grep — the form step 3 has prescribed since the fifteenth retro — returns exactly the five genuine files; the six named ADRs carry review *dates* (2026-08-02/04) in their headers, not `pending`; and the **unanchored** form returns eleven, the fifteenth retro's 6-for-1 prose over-count reproduced to the digit (the six extras match on prose that quotes the marker, including ADR-0019's own definition of the grep). The procedure is sound; the defect was in the reading; the offered header-fix dispatch was withdrawn by correction on #217 before a mechanical seat could "fix" six correct records. Elimination-context, turned on the retro's own bank.
- *The orchestration seat produced the cycle's sharpest evidence, twice against itself, and both damaged parties behaved.* Two dispatches launched with an unspliced `TO BE WRITTEN` brief (a regex escape, then an over-strict assertion); the first kill read `ps -p <launcher-pid>` returning nothing as death while the session worked on for half an hour — #105's sixth firing, first from a cause the pre-flight cannot see, two agents in one tree. Both agents followed the standing rule (stop, report, never reset); nothing was lost, and the second stop succeeded by resolving the dispatch to its **worktree** and killing every process whose cwd is inside it, verified by re-scan. Filed **#308** — `just dispatch --stop` keyed on worktree cwd, a `worktree_occupied_by_dispatch` refusal for a tree holding a result-less dispatch, and the recovery-runbook procedure landing with the tool — per retro 27's precedent of filing mechanism issues directly.
- *Concurrency's cost surfaced as a flake in an unrelated module; weighed and bounded on #284 rather than repeated.* #306's diagnosis — hypothesis's unchosen 200 ms wall-clock deadline, ~5× headroom, eroded on a night of concurrent gates at WIP 5 — is plausible, single-observation, and honestly bounded by its own author (elimination and mechanism, no caught reproduction; the load attribution inferred). The channel is closed at `11799b6` (`deadline=None` suite-wide, with the why-this-is-not-a-timeout-extension argument in the close); the residual channel is gate latency, which #284's diagnostics already capture. Posted to #284 as a reading note — wall-clock flakes in candidate blocks are load diagnostics, not guardrail breaches, and inter-arm gate-duration comparisons are confounded by the treatment itself. No WIP change proposed; the question stays #284's.
- *The handoff programme's null result got its mechanism issue.* #212: five handoff events, **zero** cold-start reads, cause mechanical — `tools/brief.py` never calls `handoff_fetch`; the 56% break-even denominated in the currency #218/#220/#232 inverted, writing ≈30× reading on this plan where #208 had 71:29 the other way; the artefact drifted to the completion-report shape #208 priced and rejected. Filed **#309** — compose the newest handoff into the brief byte-for-byte from `handoff_fetch.select` (the verdict paste rule's second artefact), fail-closed on fetch errors, plus the 2,000-character size check. `SubagentStart` injection stays ruled out on the study's own criterion 7.
- *`just land` grew the corpus rung and judged two landings on its first day.* `corpus_owed`/`corpus_not_pass`/`corpus_check_unreadable` at `edbfe1b`; `corpus=not_owed reason=no_in_world_path` computed — not remembered — on #305 and #306. `corpus_not_pass` deliberately carries the pool's own class to the failure-class table rather than interpreting it: design within the table, the twentieth's precedent, so corroboration and no count.
- *Three landings in one week shipped a command surface nothing had run.* #272's archive verb (`error: justfile does not contain recipe`), #280's follower, #287's guarded discard — carried here by #303 exactly as that issue asked. Three instances is a sound predicate where #235's single changelog miss was not: one sentence proposed at the gate — a new or changed recipe lands with a test that drives the recipe, not only its tool.
- *The `.claude/` wall and the seat's command surface are now measured ground.* #294's probe table (three independent confirmations of the wall; `Edit` refused on a granted pattern; both `Write(…)` allowlist entries dead by Claude Code's own startup warning nobody had read; the one untested escape quoted for the gate) and its three-cause re-derivation of the refused-command list — RTK rewrite artefacts (`grep` vs `\grep`, the same command on opposite sides of the filed list), own-merit refusals, compound decomposition. This dispatch ran under a brief carrying the resulting reserved-surfaces section, and the wall was respected rather than re-tested. With #297's discovery of a second dated widening the issue did not know about, this is the cycle's elimination-context exemplar.

**Applied.** This journal entry; implementation issues #308 and #309; the #284 reading note; the correction and consolidated proposals on #217. No `AGENTS.md`, ADR, CONTEXT.md, acceptance spec, settings, project-skill, schema or routing-policy edit landed from this dispatch; the only landing path is `docs/process-log.md`.

**Proposed at the human gate, not applied** (verbatim on #217): banner twenty-seven → twenty-nine (atop retro 28's pending → twenty-eight); the `just land` row (#302's text, pile item 4, endorsed unchanged); the `just brief` row gaining `--prior-work` (#305's proposal, carried to the pile because its close postdated the pile comment); the recipe-surface sentence; a pointer clause for `docs/agents/dispatched-session-commands.md`; elimination-context ×15 → ×16 (#294/#297, contingent on retro 28's ×14 → ×15); measure-before-building ×6 → ×7 (#212, contingent on ×5 → ×6); retro skill ×28 → ×29.

**Rejected / no change.** Any step-3 amendment to this skill (the finding accusing it was refuted; the anchored grep is correct as written). A failure-class or convention-lands count for the corpus rung or #305's same-commit exercise (design within the table; the rule operating — retro 27's precedent). A WIP conclusion from the #306 flake (single inferred observation; #284 owns the question and now carries the note). A "landings must comment on their issue" rule for the #296 fresh-dispatch error (#305's mandatory prior-work report is the systemic guard; the deeper cause was seat conduct, now mechanised at briefing time). Probe-window ×8, ADR-claiming ×7, failure classes ×11, recovery ×18, `playtest-brief` ×2, `playtest-ingest` ×1 — no in-world landing, no claim collision, no new resumption shape, no playtest. The corpus: the only landed path is `docs/process-log.md`, so no path reaches `addons/`, `missions/`, `extension/`, the daemon's world-facing half or the manifests; `just fast` is the gate and `just regress` is not owed.

**Status.** Proposed, with every gated marker unchanged on-tree until human acceptance: elimination-context ×15 → ×16 (#294/#297); measure-before-building ×6 → ×7 (#212); retro ×28 → ×29 (first dispatched `claude-native` retro under #300's nine profiles; a bank finding against the skill's own procedure refuted by re-derivation; both increments stacked on retro 28's still-pending batch). Open for the human: #217's pile as corrected — item 2 withdrawn, retro 28's batch still awaiting acceptance. Review queue: five pending on-tree (0064, 0066, 0067, 0068, 0069), and the anchored grep's number is the genuine number.

**Proposed prune record** (per #201/ADR-0060: archive verbatim when the gated marker edits are accepted; lists stay at five).

*Elimination-context (×16):* "#218/#220/#232: the token-efficiency corpus's whole ranking was an inherited measurement priced in list dollars, correct on an API key; re-measured against this plan's own meter the currency inverted it — output is on the order of a third of plan spend, not the 4.5% booked, and the cache-cliff family it ranked first-to-third is worth under 0.7% combined — so #232 re-based the ledger's spend column on the measured currency (`cap_fraction`, with its calibration named) rather than inheriting `cost_usd` as cost; the line's first application to a whole cost model rather than a single measurement."

*Measure before building (×7):* "#207's threshold was re-measured rather than assumed"

## 2026-08-13 — retro: scheduled, twelve issues (#25, #70, #146, #186, #194, #308, #309, #310, #311, #312, #314, #315)

Trigger: twelve closes since retro 29 (`98ca181`), past the five-close cadence; scope ends at `76d0309`. Dispatched retro on `claude-native` (#318). The evidence bank is **#316** — third use of #276's precedent, and the first bank filed by the orchestration seat against itself: three of its seven findings cut against the seat that wrote them, and #318 asked for rulings rather than transcriptions on all three. The claude-md-management skill: **listed and erroring on Skill-tool invocation, twice — the second consecutive observation of the listed-but-erroring state retro 29 recorded**; the pass ran from the 1.0.0 plugin cache exactly as retro 29's did. Incremental pass score: **83/100 (B)** — commands 16/20, architecture 18/20, non-obvious patterns 15/15, conciseness 12/15, currency 9/15, actionability 13/15; every currency deficit found is already at the human gate (the four piled command rows, the pending marker chain) rather than silently stale. **Review queue at this retro: 5 pending on-tree (0064, 0066, 0067, 0068, 0069)**, counted from this seat by the anchored `^Reviewed-by-human: pending` — stated with the anchor because making the 6-for-1 error inside this pass would have been finding 7's third occurrence.

**Rulings on the three findings #318 flagged for judgement.**

- *Finding 2 — the four-times-repeated brief defect is **mechanical**; ruled a mechanism to file, not a rule to write.* Two grounds. The seat added its own discipline check (`grep -c 'TO BE WRITTEN'`) after the second instance and the fourth still shipped a wrong lane note — a manual check covers the last failure mode, not the class, which is #302's signature (known, quoted, briefed, still broken). And **#345**, filed after the cycle and weighed as corroboration, shows the same shape from the opposite side: `tools/brief.py` composes "Close #N" into every brief's invariant half. #345 proposes — extrapolating from ADR-0071 ruling 4's role split, which itself assigns closing to nobody — that closing is the lander's; a seat obeyed the tool over that contrary prose, and a seat obeying the composed artefact is why the correction must live in the composition/dispatch path, not in orchestrator discipline. The shape is that composing a brief and dispatching it are two operations and the first can half-fail silently; the seam closes fail-closed at the second. Filed **#349** — `just dispatch` refuses `brief_placeholder` and `brief_lane_mismatch` by name, placeholder vocabulary shared with `tools/brief.py`.
- *Finding 4 — ruled an argument about **brief composition**, not agent conduct; and the bank's three instances are two on re-derivation.* **#25 holds and is the finding**: the brief was composed from a body whose thread had closed the issue across nine rulings, so it asked for implementation at `opus-xhigh`, named the full corpus and offered ADR-0013's standing authorisation; the agent read the thread, built nothing and said why. **#312 holds but is not this finding's**: the instruction it refused — close the issue — is composed into every brief unconditionally (`tools/brief.py:783`), a standing tool assertion that #345 owns. **#310 does not hold**: its continuation brief instructed the refactor in terms (*"Fix it properly"*, *"Do not weaken the validator to satisfy the linter"*, with an explicit invitation to argue for a single suppression in the close) and the dispatch did exactly that; the refusal of a third stacked suppression was the orchestrator's own, taken at the seat before dispatching, and is evidence for the seat rather than against it. The bank's "the seat's briefs were wrong in each case" is wrong for #310 and was not re-derived in four review rounds — the elimination-context line, unapplied inside a retro that names it. No conduct rule is wanted either way; adding one would be the over-prescription step 2 warns against. Half the class is already mechanised: `c30f8f1` composes the newest handoff byte-for-byte, #305's prior-work report runs at briefing time, and #345/#349 remove standing assertions. The residual is #25's shape alone, recorded once before on #234, and it is a seat discipline nothing can check — no tool can know whether the hand that wrote a brief's task section read the thread. **On the sentence.** Rounds 1–3 each wrote a declarative universal about *a brief* and `compose()` contradicted each: the halves inverted, then a partition `compose()` does not have, then "asserts only protocols and computed gates", which omits the computed base SHA, the prior-work, reserved-surfaces and readiness lines and the resolved `Reviewing:` profile. Round 4 deleted the enumerating clause on instruction and kept a universal — everything thread-owned is cited verbatim rather than concluded — which `compose()` falsifies on the same page: `Flake.line()` builds its line from two regex captures (`:418-421`), `is_flake()` concludes flake-ness from a title and body (`:424-427`), and where nothing matches the brief states "None open. Any red is yours." (`:777`), a conclusion citing nothing that was false in this thread's own briefs against #341 (**#360**). The common defect is not the predicate: a brief is **jointly authored**, so any declarative universal over "a brief" ranges over the tool and is falsifiable in principle whatever it says. Escalated under ADR-0071 ruling 4 (#318, `opus-max`), and ruled: change the mood and the subject rather than the predicate — an imperative addressed to the seat, about the sections `compose()` leaves as placeholders, has no truth value for `compose()` to contradict. Proposed for `docs/agents/orchestration.md`'s **What the seat must not do**, verbatim: "Never write a brief's task, scope or ground truth from the issue body alone. Read the thread first, and name the comment each pre-derived decision comes from, so that a body the thread has superseded shows up in the brief rather than being resolved silently. #25's brief asked for implementation at `opus-xhigh`, named the full corpus and offered ADR-0013's standing authorisation, after nine rulings on its own thread had converted the issue to a decision ticket with no production code; the dispatched agent read the thread, built nothing and said why. The same shape is recorded once before, on #234. Both were caught by the agent, not by the seat." Four falsifications and one uncaught evidence error are direct evidence for **#354**'s quote-the-tool-beside-the-proposal mechanism, referenced and not built here, and argue for it quoting the cited *evidence* as well as the cited code; the stale `brief.py:18` docstring round 1 verified against is **#357**.
- *Finding 7 — the unanchored-grep over-count fired its second occurrence (retro 15 inside step 3; this cycle the orchestration seat, eleven reported where five are genuine, refuted by retro 29 before six correct records were "fixed"); ruled up to a **mechanical check** per ADR-0038's second-occurrence shape.* Retro 15's fix anchored the skill's text, and the recurrence came from outside the skill — a text fix in one document does not reach every seat that types the grep, and per #209 an agent is not handed a job a tool can do. Filed **#351** — a `just review-queue` recipe printing the anchored count and file list, with a test that plants a prose mention and asserts it uncounted; its close proposes the gated table row and the skill's step-3 pointer sentence.

**Other findings.**

- *The corpus rung's blindness to the tier's own machinery: adopted, filed as a decision issue.* Two instances, escalating (#304 `tools/probe_verdict.py`, which types every verdict; #70 `spike/run.sh`, which runs every probe and whose change adds a refusing pre-flight — a false positive stops every probe, a false negative restores the fail-open the issue closed); both times `corpus=not_owed` was mechanically right and substantively short, and the orchestrator's run-it-anyway was right both times. Filed **#352** with retro 30's recommendation: the mutation-floor shape over a second prefix list (#70's own shell arm, 4/10 against a 20% floor, proves the arm exists), dependent on #338's scoping fix, with the gated class-5 alternative named. Two instances rather than three, but the question was already twice raised on #302's thread with candidate shapes and no owner — filing an owner is this seat's job; leaving it would re-ask retro 31.
- *`just generate` outside the dispatched allowlist, second rung of `just check`: adopted the cheap remedy.* #310 landed with no lint cover (a stale export suppresses ruff, ty, clippy, HEMTT, gitleaks); #194 found the short-circuit sits before `check-sqf`, so the ban gate never saw a new probe. Filed **#350** — `tools/brief.py` derives the owed-to-orchestrator line for `just generate`/`just build` from the named surfaces, as the corpus line already is. The allowlist widening stays a permissions decision on #294.
- *Two mechanisms caught their own author within hours: corroboration for convention-lands, count proposed.* #308's `worktree_occupied_by_dispatch` refused the seat's own double-dispatch on its first live opportunity, and `--stop` killed seven processes with `verified=no_process_in_worktree` where the morning's hand-rolled `kill` had silently failed — the stop procedure having landed in the same commit as its `docs/agents/recovery.md` section. Proposed ×5 → ×6. #305's sibling instance was weighed and declined by retro 29 and is not re-proposed.
- *The foreign-eligibility distribution holds: adopted as a recorded measurement; no change.* Of 30 `ready-for-agent`, 10 foreign-eligible, 11 gated-semantic, 8 in-world, 1 gates-themselves — #296's measurement after twenty landings, posted to #298 so its objective is designed against the measured distribution.
- *The post-cycle mutation-scope pattern (three modules landed outside the gate: #324, #338, #346) is **deferred to retro 31, agreed**.* All three instances post-date `76d0309`; adopting them here would make the cycle boundary a judgement call, which is the drift boundaries exist to stop. The boundary rule, stated once so retro 31 has one to apply: post-cycle evidence may corroborate a finding whose instances lie in range — as #345 does for finding 2 above — but may never supply the instances; a pattern whose every instance post-dates the boundary waits for the retro whose cycle contains them. The rule's home is not this entry alone — round-2 review found that a rule living only in the audit trail is the shape the round spent its High on — so it is proposed on #217 as a step-1 addition to `.claude/skills/retro/SKILL.md` (gated) and posted on #348 above its candidates, where retro 31's bank carries its own admissibility test. Nothing is lost by waiting: the defect-side issues exist and do not wait on a retro, #348 banks the pattern, and the five-close cadence convenes retro 31 promptly. No cadence finding.
- *In-world discipline held at the corpus's two newest probes: probe-window count proposed.* #312's `order-waypoint` asserts `currentWaypoint` directly — the only assertion that catches the `arg= local` hazard — rather than the Order's record or its display; #194's `identity-window` states the limit it cannot engineer around (a probe can neither stub `getPlayerUID` nor slow propagation) instead of pretending to assert `identity_pending` firing. Proposed ×8 → ×9.
- *Elimination-context reached a finding's coordinates: count proposed.* #146 re-derived every listed site against `ccd7af3`, the commit the pass was filed at, after #174 drifted the line numbers — the line applied to a finding's coordinates rather than its conclusion. Proposed ×16 → ×17, contingent. #194's 1-frame (~10 ms) window measurement likewise proposed as measure-before-building ×7 → ×8, contingent.

**Applied.** This journal entry; implementation issues **#349, #350, #351, #352** (dependency order: #349/#350/#351 independent, #352 after #338); the #298 measurement note; the gated proposals quoted verbatim on #217 and in the retro report. No `AGENTS.md`, ADR, CONTEXT.md, acceptance spec, settings, project-skill, schema or routing-policy edit landed from this dispatch; the only landing path is `docs/process-log.md`.

**Proposed at the human gate, not applied** (verbatim on #217): banner twenty-seven → thirty (atop retros 28's and 29's pending bumps); convention-lands ×5 → ×6 (#308); probe-window ×8 → ×9 (#312/#194); elimination-context ×16 → ×17 (#146, contingent on retros 28's and 29's pending increments); measure-before-building ×7 → ×8 (#194, contingent likewise); retro skill ×29 → ×30 (contingent likewise); the four piled command rows (`just land`, `just brief`, `just occupancy`, `just dispatch --stop`) reaffirmed unchanged.

**Rejected / no change.** A conduct rule from finding 4 (the right refusals are the process working; the correction is in composition, and the bank's third instance did not survive re-derivation). Re-proposing #305's convention-lands count (retro 29 declined it; one weighing stands). Recovery ×19 for the stop procedure (one event, one marker — it is carried under convention-lands ×6). Adopting the post-cycle mutation-scope pattern (outside the audited range; deferral agreed above). Failure classes ×11, ADR-claiming ×7, playtest-brief ×2, playtest-ingest ×1 — no new class exercised at a gate this retro judges, no claim collision, no playtest. The corpus: the only landed path is `docs/process-log.md`, so no path reaches `addons/`, `missions/`, `extension/`, the daemon's world-facing half or the manifests; `just fast` is the gate and `just regress` is not owed — computed against the diff, not assumed. The gate's outcome, so this record does not read as clean: one red, exactly #341's two named tests (`test_a_zai_record_carries_the_plan_charge_block_the_estimator_will_read` and `test_the_dry_run_prints_the_resolved_profile_and_why_that_one`) at 07:24 UTC, inside the 06:00–10:00 UTC band the brief pre-declared; 4328 passed, every `just check` rung green. Re-run green at 16:25 UTC, outside the band, on `62b473f`: 4330 passed, mutation smoke `nothing added or changed against origin/main` — an in-tree gate record covers an earlier form of its entry, never the entry's final form; each fix round's green over its own content is on #318.

**Status.** Proposed, with every gated marker unchanged on-tree until human acceptance; the contingency chain on elimination-context, measure-before-building, the retro skill and the banner is now three deep (retros 28, 29, 30 all pending), which is the queue's growth made visible, not a new finding. Review queue: five pending on-tree (0064, 0066, 0067, 0068, 0069), anchored. Round-1 review of this retro (#318, `opus-xhigh`, seven claims) corrected this entry in place: the residual sentence's two halves un-inverted and quoted durably (the High; mechanism filed as #354, not this seat's to build), the gate's red recorded, the cycle-boundary rule stated, one disposition verb aligned to the criterion's three, and one ruling-4 citation downgraded to the proposal it actually cites. Round-2 review (`opus-xhigh`, eight claims, all accepted at adjudication) reshaped the residual sentence a second time — the halves partition dropped for the operation, per the adjudication — homed the cycle-boundary rule on #348 and #217, recorded the 16:25 green beside the 07:24 red, split one four-fact sentence, and routed the review's two pre-existing finds to the backlog as **#356** (four `tests/unit/test_host_seam.py` tests read `$HOME/.arma-cti/hosts.toml` with no isolating `CTI_HOSTS_FILE`, so `178bef4`'s `bravo` registry row reds them in any tree at that base — the red the lander meets at `just land`'s re-gate after the rebase, which `just build` does not clear; diagnosis corrected by the round-3 review from the shim framing this entry first recorded) and **#357** (`tools/brief.py:18`'s stale invariant-half docstring) — neither this retro's to fix. Round-3 review (`opus-xhigh`, six claims, all accepted) cut the residual sentence to its durable half — the first clause deleted rather than re-enumerated, a third wording having described `compose()` and been contradicted by it — re-diagnosed #356 (the unisolated hosts-registry read above, displacing the shim framing and its skip arm, which would have hidden a live seam failure), scoped the 16:25 green to the content it gated, and aligned one round summary and the #212/#290 attribution to their sources. Arbitration (#318, `opus-max`, ADR-0071 ruling 4) replaced the fourth wording rather than producing a fifth: all four were declarative universals over a jointly-authored artefact, so `compose()` could contradict any of them; the proposal is now an imperative addressed to the seat. The same pass re-derived the finding's evidence and found the bank's #310 instance does not hold.

**Proposed prune record** (per #201/ADR-0060: archive verbatim when the gated marker edits are accepted; lists stay at five).

*Convention-lands (×6):* "#23: landed the `just regress` table row, the lock, and the evidence conventions in the same commit as the recipe."

*Probe-window (×9):* "#35: a Squad teleported to the enemy Base at t=40 met three Squads the real Campaign would long since have marched away — the fix restored the conditions the shortened march stands in for, not more time."

*Measure before building (×8):* "#209: the slot-API arithmetic was declined once actually run"

*Elimination-context (×17):* "#239/#172: mutmut's filed verdict — incompatible with our pytest config — was re-run rather than inherited and failed both ways: the tool runs here (the "incompatibility" was `source_paths` naming a file, which unmade the `cti_daemon` package and let `-n auto` take the blame), and what it reports cannot carry a floor (its three survivors on a sound 75-line module were all equivalent mutants, so 62.5% on good tests is a floor a merely-running suite clears); the replacement gate was designed against the re-derived facts, not the filed verdict — the line's first application to a tool evaluation."

## 2026-08-15 — retro: scheduled, thirteen issues (#320, #321, #322, #323, #324, #325, #318, #326, #327, #341, #344, #356, #369)

Trigger: thirteen closes since retro 30's anchor (`76d0309`), past the restored five-close cadence; scope ends at `169ee27`. The run was gated by the human's ruling of 2026-08-14 on #348 — after the three-retro marker chain lands, and on nothing else — and released by queue item 1's completion at `8debf09`/`169ee27`. Dispatched retro on `claude-native`, reusing the `retro-31` tree. The evidence bank is **#348** — fourth use of #276's precedent, and the first bank to carry candidates added by the human's own rulings mid-cycle (the typed-discriminator candidate banked from the #347 ruling; the run-gating ruling itself). The bank's own admissibility rule — post-cycle evidence may corroborate but never supply instances — ran its **first application**: candidate 1's three instances (#324, #338, #346) all post-date `76d0309`, so retro 30's deferral resolves here as designed. The claude-md-management skill: **listed and erroring on Skill-tool invocation, a third consecutive time**; the pass ran from the 1.0.0 plugin cache. **Review queue at this retro: 5 pending on-tree (0064, 0066, 0067, 0068, 0069)**, counted by the anchored `^Reviewed-by-human: pending` — the unanchored form's 6-for-1 over-count has fired twice (retro 15, retro 30's cycle) and its mechanical check is #351, unbuilt, so the anchor is still typed by hand here.

**Findings and rulings.**

- *Candidate 1 — three modules landed outside the mutation gate: adopted and codified; the remedy is both halves.* `mutation_smoke.in_scope` keys off test modules, so a `tools/` module with no test module is measured by nothing while every gate stays green: #324 (`generate_seats.py` never measured, surfaced only via an unrelated `PS4`/`BASH_SOURCE` defect), #338 (an import-crashing mutant reads `?? could not run`), #346 (production grew `_valueless_stratum` purely to stay scoreable — production shaped by the measuring tool). Three instances of one mechanism clears retro 15's codification bar. #346/#338 own the harness half; the selection half — a `tools/` module with no test module is a **named refusal, not a silence** — is filed as **#370**, with ADR-0064's amendment riding that landing through the human gate. Live risk, not history: #331's fourth acceptance criterion depends on this gate.
- *Candidate 8 — two unnameable `brief.py` survivors: adopted; the intersection now has an owner.* `18/20` of 91 sampled, identical across #325's three rounds; `--report` and an exhaustive run were sandbox-refused to both the implementing and the reviewing seat. #353's shape (a seat cannot run what it judges) meeting #346's (the harness is unreachable where it matters), on the module that renders escalations. Under the #353 reversal the implementer's paste is the review's only mutation evidence, which makes an environment where `--report` actually runs load-bearing. Filed as **#371**.
- *Candidate 2 — the brief's composed text beat a ruling: adopted as corroboration of retro 30's finding 2; no new rule.* `tools/brief.py` composed "Close #N" after ruling 4's role split; the #323 seat obeyed the tool over contrary prose; filed in-cycle as #345 and weighed by retro 30. This retro adds a **live instance from its own dispatch**: the brief that launched this pass, composed 2026-08-15, still carries "Close #348 with a criterion-by-criterion audit", and the orchestrator's handoff separately instructs seats to ignore that line — the correction living in prose while the tool re-asserts the error each dispatch, which is exactly why #345/#349 put the fix in composition. Evidence posted on #345.
- *Candidate 3 — the dry run was most optimistic where the surface is most gated: recorded as the process working; no generalisation.* #344 was found by a seat reasoning correctly from what `just land --dry-run` showed it, filed, fixed in-cycle (`9a38ba3` consults the routing gate on the dry-run path), and its corrected command-table row was approved the same day with the re-derive-never-paste ruling. The general rule ("a dry run states what it did not check") now lives in the tool's own output rather than in a sentence here.
- *Candidate 4 — a known-red gate was scheduled around three times before it was fixed: adopted as a cost record; no new rule.* #341's wall-clock read made `just fast` red 06:00–10:00 UTC daily; #323, #324 and #325's briefs each carried a schedule-around paragraph, and one review round misattributed its red to the z.ai band. The bug was fixed in-cycle; the mechanical home for "a brief claims no open flakes while known reds exist" is already filed as #360. A prioritisation rule was declined — bias toward removal; the cost is recorded, the seam has an owner.
- *Candidate 5 — ruling 4's escalation path fired for real and worked: recorded as deliberately as a failure would be.* #323's held Medium escalated to `codex-sol-high`, which ruled `lands_as_is` with a specified follow-up (#347), and established what neither reviewer nor orchestrator had — the production path cannot emit the collision, checked against the nine existing records. First live validation of the arbiter route; its conditions feed #325's data model. No marker minted for one event.
- *Candidate 6 — four consecutive reviews could not run the gate: superseded by ruling.* The human ruled on #353 (2026-08-14, reversing 2026-08-13): a review is **judgement-only by construction** — it reads the implementer's pasted gate output, now required to include `just mutation` with sampled-or-exhaustive stated, and never executes. The issue's "unfalsifiable green" premise was corrected before the ruling: `just land` re-gates and no flag skips it. Execution of the follow-ons (brief.py stops asking reviews for gate runs; the contract's paste rule) is queued on #217; nothing for this retro to add.
- *Candidate 7 — the orchestrator's self-recorded errors: corroboration for #349; no new rule.* Two of three are brief-composition errors of retro 30's finding-2 shape ("last round" one round early; "five tests" where four were). The third — a subagent launched against a standing instruction, then stopped and the work done directly — is the seat catching itself; recorded, nothing filed.
- *Bank comment 1 — worktree reuse skips the pre-flight: adopted; filed with the dispatcher as the enforcement point.* Retro 30's pass reused its tree with no pre-flight and `rtk proxy` unapprovable; **this retro reproduced the gap from the other side** — it reused `retro-31`, attempted `just worktree check retro-31`, and the sandbox refused it, as it refused `rtk proxy`. The seat inside the tree can be structurally unable to run the check, so the check belongs to the dispatcher at dispatch time. Filed as **#373**; the worktree row's one-sentence widening is proposed at the gate.
- *Candidate 9 — the third rtk-filtered absence acted on: adopted; the rule must bind the reading, not only the running.* The orchestrator ran `just worktree check` through `rtk proxy … | grep`, saw no matching lines, re-dispatched into a tree carrying five files of a failed attempt's uncommitted work; the dispatched seat caught it. The rule as written was followed in form — a check was run — and defeated by a line-removing pipe over its output. RTK.md is the human's global file, amended only by their ruling: wording is proposed on #217. The mechanical half — read a state-bearing check by exit code and typed refusal, never by grepping its output — is in #373's acceptance criteria.
- *The human-banked typed-discriminator candidate: ruled below the codification bar; watched.* Two instances (#323's prose-discriminated `Stratum`, #325's five representations of unreadable-as-silence), found independently, both caught by review — which after #353 is the review's designed job. Retro 15's bar is three instances; #347 carries the concrete fix for the live case. A third instance codifies; until then a convention would be the over-prescription step 2 warns against.
- *False docstrings — four in three days: adopted; the cycle's dominant failure shape named.* `resolve_selection`'s "No other provenance rule remains" was false and sat exactly where a policy writer would read (#327; the same wrong conclusion was re-derived twice from that docstring's first word); the seat-bar docstring enumerated four seats while the map held five (`564cfb7`); the walk test's "would have caught round 2's appointment" was green in the gate it claimed to redden; #344 held `_routing_plan`'s consequences inverted and `_exempt_lane` describing a preferring form the code does not implement; #357 (the invariant-half docstring omitting three sections) is the same defect already filed. Every one passed every gate and fell only to review reading code against prose. The shape is the cycle's recurring one — **a second copy nothing compares**: docstring against code, retro proposal against tree (#354), piled row against moved tree (the re-derive ruling), `AGENTS.md`'s thirty against the skill's ×27 (closed by queue item 1), brief text against ruling (#345). The in-cycle remedy that worked was structural pin tests (`0c7063e`). One strictness sentence is proposed at the gate; the enumeration above is its evidence.
- *#334's unlanded transcription: ruled queue work, not a process finding; the vehicle is filed.* The accepted-and-filed adjudication route amends ADR-0071 ruling 4 by the human's explicit sign-off, and the second decision-clearing session's own index caught that nothing carries its execution ("It is not on the queue above and should be"). The omission was self-caught, so no rule; the missing vehicle is **#372**, which routes the ADR half into the queue's merged ADR-0071 landing rather than opening that surface twice.

**Status upgrades (step 5).** Banner thirty → thirty-one, and the retro skill ×30 → ×31 with its exemplar sentence — both gated, proposed verbatim. Elimination-context ×17 → ×18: #353's filed premise ("an implementer's green is unfalsifiable") was re-derived before the human designed on it — `just land` re-gates and no flag skips it — and the reversal was ruled on the corrected, narrower premise; the line's first application to a ruling's own premise mid-decision-session. No downgrades: failure classes ×11 (no new class exercised at a gate this retro judges), ADR-claiming ×7 (no collision), probe-window ×9, convention-lands ×6, measure-before-building ×8 (no qualifying instance weighed) all stand.

**Applied.** This journal entry; implementation issues **#370, #371, #372, #373** (all independent; #370 after #338/#346 only where the harness half moves first); the evidence comment on #345; the gated proposals quoted verbatim on #217 and in the retro report. No `AGENTS.md`, ADR, CONTEXT.md, acceptance spec, settings, project-skill, schema or routing-policy edit landed from this dispatch; the only written path is `docs/process-log.md`, and this seat neither lands nor closes — the branch is pushed as `retro-31` for the lander.

**Proposed at the human gate, not applied** (verbatim on #217, each quoting the tree text it changes per #354's discipline): banner thirty → thirty-one (`AGENTS.md:5`); retro skill ×30 → ×31 with exemplar (`.claude/skills/retro/SKILL.md:3`); elimination-context ×17 → ×18 with the #353 exemplar and the oldest exemplar pruned (`AGENTS.md:144`); the worktree row's reuse sentence (`AGENTS.md:60`); the strictness paragraph's docstring sentence (`AGENTS.md:116`); RTK.md's reading-binds clause, the human's file and theirs alone to land.

**Rejected / no change.** A prioritisation rule from candidate 4 (cost recorded, #360 owns the seam). A conduct or convention rule from candidate 7 (composition mechanisms #345/#349 own the class). Codifying the typed-discriminator shape at two instances (watched; third instance codifies). Any generalisation of #344 beyond the tool's own corrected output. A cadence finding (the ruling's own reasoning: overdue equally before and after the gate it chose). A rule for the #334 index omission (self-caught; vehicle filed). The corpus: the only written path is `docs/process-log.md`, which reaches none of `addons/`, `missions/`, `extension/`, the daemon's world-facing half or the manifests, so `just regress` is not owed — computed against the diff, not assumed.

**Deviations of this pass, recorded rather than hidden.** The reused tree's pre-flight could not be run from this seat (`just worktree check retro-31` sandbox-refused), and `rtk proxy` was likewise unapprovable, so the exclusivity claim rests on filtered reads agreeing (clean status, HEAD at `169ee27`) plus the structural facts the reviewer of this entry can verify: the retro commit's parent is `169ee27` and its diff touches one file. That is the same compounded weakening retro 30's round-1 review named, now with an issue (#373) rather than only an account. Gate outcome recorded below by the lander's re-gate; `just fast` was run from this tree before push and its result is quoted in the retro report on #348.
