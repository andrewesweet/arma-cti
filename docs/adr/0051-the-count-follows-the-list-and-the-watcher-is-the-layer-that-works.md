# The count follows the list, and the watcher is the layer that works

Delegated-decision: yes
Date: 2026-08-04
Stood-in-for: human sign-off on CLAUDE.md, process-doc and skill marker changes — the
nineteenth retro's amendment batch (session unattended; the second guided review
session had cleared the queue to zero hours earlier)
Reviewed-by-human: pending
Claimed: comment on #186, 2026-08-04, after `git fetch origin` (`docs/adr/` on
`origin/main` topping at 0050, all of 0046–0050 flipped to reviewed the same morning)
and a scan of open-issue comments finding no claim above 0050

Amendment batch for the nineteenth retro (scheduled: five closes since `8fd3e7b` —
#156, #184, #176, #161, #152). Full findings: `docs/process-log.md`, entry 2026-08-04
(nineteenth retro).

## Proportionality

Weighed per the eighteenth's precedent, and against a queue the human cleared to zero
this morning: journal-only was the live default, and three findings still earn
application now rather than a wait. Two are count-and-exemplar moves whose deferral is
this cycle's own headline defect — a count that does not move with its list is exactly
how the fifteenth's approved ×4 got lost for three cycles — and the third is the
skill's own count, which moves every run or lags. Leaving them as proposals in a
report is the failure mode the convention-lands rule names: text living nowhere
durable. One batch ADR, no new rules, queue re-opens at 1.

## Decisions

**1. Recovery runbook ×10 → ×11; the noticing clause records the settled
attribution.** The #159 agent stalled 40 minutes after its corpus run finished green
(21/21 at `838d35f`, pool `20260804T181537Z-1402436-pool`, worktree clean and
unlanded), dispatched with the do-not-park briefing line carried and CLAUDE.md's
watching sentence live — the "stall despite both layers" the eighteenth named as its
escalation trigger. What it escalates to already exists: the standing watcher caught
all three stalls to date (#168 ×2, #159 ×1) and text has prevented none, so the clause
now states the score in place of the open attribution question, and the header takes
the eleventh use with the exemplar in the same edit. No new rule: the watcher is
already codified, the sentence stays because it is true and costs nothing, and the
underlying defect — a parked run's completion does not wake the agent that parked it —
is harness-level, outside these documents and in the orchestrator's hands.

*Overturned by*: a stall the watcher misses (the noticing section is then wrong about
its own coverage), or a run of watcher-less dispatches at cycle scale without a stall,
which would show the text sufficient after all.

**2. Elimination-context ×5 → ×6.** Both of the cycle's batch closes checked their
inherited item lists against the tree that had moved under them: #152 verified that
neither #145 nor #156 had consumed any of its six items before starting; #156's
per-item audit found two already discharged by #164's `3cb4db0` and said so rather
than re-doing them. First applications of the line to an inherited work list rather
than a measurement or rationale — a distinguishable mode, exemplar appended in the
same edit.

*Overturned by*: the exemplar list's own length doing harm — if a future pass finds
the parenthetical unread because it has become a wall, prune it to the load-bearing
few per the bias-toward-removal line, and this count stops pretending to teach.

**3. Retro skill ×18 → ×19.** Nineteenth run, unattended; no procedural
self-amendment. The header records the cycle's finding against the skill's own
history: the fifteenth's commit (`0485d1a`) carried the same-edit clause's second
violation — convention-lands' #131 exemplar landed with the count still ×3 while
ADR-0047 decision 3 recorded ×3 → ×4 — and the sixteenth through eighteenth
propagated ×4 from the log's status lines without reading the file. The pre-priced
escalation ("a second escalates it to a mechanical check per ADR-0038's shape", both
status blocks) therefore fired. The check is filed as #186 rather than built here:
its one open design question — prose exemplars carry no machine delimiter, and
deriving one may mean a format change to CLAUDE.md, which is gated — belongs to an
implementation agent working under #137's fit-the-gate-to-the-corpus discipline, not
to an unattended retro improvising a parser over the project's most sensitive file.

*Overturned by*: #186 closing wontfix or proving unbuildable — the escalation
sentence in both status blocks then needs rewording to whatever the human decides
instead.

## Recorded here, not delegated

- **Convention-lands ×3 → ×4 in CLAUDE.md** is not a new decision: it executes
  ADR-0047 decision 3, approved by the human 2026-08-04, which the fifteenth's commit
  never applied to the file. Listed for transparency because it rides in this batch's
  commit.
- **#186 filed** (`ready-for-agent`): the mechanical marker-count check, per the
  escalation clause. Filing an issue is not a gated surface.
- **#181 data comment**: the three post-filing full-corpus passes (269/346/294 s
  massed-assault at `aa26bb4`/`a6a23f1`/`838d35f`), quoted onto the issue before the
  pool prune takes their evidence — pass runs are the kind the prune deletes.

## What would overturn this

- The human rejecting any decision above at review: each is a count, an exemplar
  sentence, or a header clause, named above, and reverts cleanly.
- The per-decision overturn rows above.
