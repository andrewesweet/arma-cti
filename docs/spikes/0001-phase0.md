# Spike 0001 — Phase 0: toolchain and WSL2 Arma tier

**Run:** 2026-07-30 · **Issue:** [#2](https://github.com/andrewesweet/arma-cti/issues/2) · **Decides:** [ADR-0006](../adr/0006-wsl2-linux-server-test-tier.md)

## Verdict: GO

The development loop works and is fast. A WSL2-native Arma 3 Linux dedicated server boots a
Stratis mission in ~20 s, the Rust shim round-trips a payload to a Python daemon in
**0.37–0.65 ms**, and the whole no-Arma gate (`just check` + `just unit`) runs in **under a
second**. Both shim artefacts build from this box: the Linux `.so` natively and the Windows
`.dll` through `cargo-xwin`.

**The Windows-client join works.** A Windows Arma 3 client on the host reached the WSL2 server,
picked a slot, spawned, and ran `init.sqf` — the server logged both `player_connected name=playtester
… jip=true` and the client's own report back. So the play path in ADR-0006 holds.

**The headless client works.** It enters the mission at ~28 s and executes server-issued code,
reporting back in 227-357 ms across five runs. That is the synthetic-driver capability the
acceptance tier depends on. It took most of the spike to find, because the cause was a single
missing `name` property on the `HeadlessClient_F` entity ([§5](#5-solved-the-headless-client-slot-needs-a-name)).

One item is not proven: the Windows client desynced once in the mission, repeating "No message
received after N seconds" while the server sat at 99 fps and logged nothing
([§6](#6-open-windows-client-desync), issue #8).

ADR-0006's fallback (orchestrating the Windows server binary from WSL2) is **not** needed on the
evidence here. Nothing found argues against the Linux server specifically: the lobby behaviour is
the engine's, and would be identical under the Windows server binary.

## 1. Environment

| | |
|---|---|
| Host | Windows 10.0.26200, WSL2 2.6.3.0, Ubuntu 24.04, kernel 6.6.87.2 |
| VM | 12 CPU threads, 11 GiB RAM, ext4, 917 GB free |
| Networking | `networkingMode=mirrored` (confirmed at run time); private LAN address redacted |
| Arma server | 2.20.152984, Stable, Linux, x64 — matches the Windows client branch |
| Install | 5.1 GiB on disk, **4 m 17 s** via SteamCMD with the dedicated server account |

## 2. Toolchain

Every version is pinned. `rust-toolchain.toml` pins the Rust channel and targets; `.python-version`
plus `uv.lock` pin the Python side; HEMTT, `just` and SQF-VM were fetched as pinned release assets.

| Tool | Version | Install |
|---|---|---|
| HEMTT | 1.20.1 | 3 m 42 s ‡ |
| just | 1.57.0 | 1.0 s |
| rustup + Rust | 1.29.0 / 1.97.1 | 27.6 s |
| cargo-xwin | 0.23.0 | 45.1 s |
| clang + lld | 18.1.3 | 40.8 s |
| uv / CPython | 0.11.11 / 3.13.14 | `uv sync --all-groups` 3.2 s cold |
| SQF-VM | 2.0.0 (`v2026.04.03-ed9f5f5`) | 0.8 s |
| cocogitto | 7.0.0 | pre-installed |

‡ The HEMTT download ran concurrently with the 2 GiB SteamCMD transfer and was bandwidth-starved.
The asset is 23.6 MB; treat this number as an artefact of the run order, not of HEMTT.

Resolved Python dev versions, now pinned in `pyproject.toml` and `uv.lock`: ruff 0.16.0,
ty 0.0.65 (pinned exactly — pre-1.0), pytest 9.1.1, hypothesis 6.163.1, coverage 7.15.2,
mutmut 3.6.0, networkx 3.6.1.

## 3. Gate timings — these decide the tier structure

Warm, median of repeated runs on this box.

| Gate | Time | Tier |
|---|---|---|
| `hemtt check -p -e` | **0.10–0.13 s** | inner loop |
| `hemtt build` | 0.21 s | inner loop |
| `cargo clippy` (warm) | 0.013 s | inner loop |
| `cargo test` | 5.7 s cold, ~0.3 s warm | inner loop |
| `cargo build --release` | 8.6 s | inner loop |
| SQF-VM startup, trivial script | **7–11 ms** (22 ms cold) | inner loop |
| **`just check`** (cog + hemtt + ruff + ty + rustfmt + clippy) | **0.41 s** | **every edit** |
| **`just unit`** (pytest + cargo test) | **0.46 s** | **every edit** |
| `cargo xwin build --release` (Windows `.dll`) | 8.8 s compile; 93.9 s first run incl. MSVC CRT download | pre-play |
| Full `spike/run.sh` Arma cycle | ~2 min | pre-commit |

**Consequence for CLAUDE.md's command table:** `just check` and `just unit` are both sub-second
and belong together on every edit, exactly as written. Nothing here justifies splitting them.

### Claude Code hook latency

Measured over 20 invocations each.

| Hook | Mean |
|---|---|
| `protect-gated-paths.py` (allow and deny paths alike) | 20 ms |
| `block-no-verify.py` | 20 ms |
| `format-on-edit.py` on `.py` (ruff format) | 48 ms |
| `format-on-edit.py` on `.rs` (rustfmt) | 56 ms |

All four are imperceptible. The cost floor is Python interpreter startup (~20 ms), not the work.

**On the deferred lint-after-edit hook** (`docs/process-log.md`, 2026-07-30): affordable, and
worth proposing at retro. A per-file `uv run ruff check` costs 183 ms — dominated by `uv run`'s
environment resolution, not ruff. Whole-project `hemtt check -p -e` at 0.10–0.13 s is cheap
enough to run unconditionally on any `.sqf`/`.cpp` edit. Warm `cargo clippy` is 13 ms.

### One toolchain defect found and fixed

`format-on-edit.py` invoked `rustfmt --edition 2021` while the shim crate is edition 2024, so the
hook reformatted files into a state `cargo fmt --check` then rejected. Fixed to `--edition 2024`.
Worth a retro note: an auto-formatter hook that disagrees with the gate is worse than no hook.

## 4. Extension RPC — the numbers ADR-0004 and ADR-0005 rest on

Three consecutive clean runs. `bench` measures inside the extension (SQF's `diag_tickTime` is
frame-quantised and too coarse for a single call); FFI cost is measured in SQF by amortising 2000
calls across one frame; callback latency is measured in SQF because it deliberately spans frames.

| Measurement | Run 1 | Run 2 | Run 3 |
|---|---|---|---|
| Server host up (s) | 11.3 | 9.3 | 9.3 |
| **Mission running (s)** | 20.3 | 20.9 | **19.6** |
| Headless client network connect (s) | 22.6 | 20.6 | 24.1 |
| Headless client **in mission**, running scripts (s) | 27.6 | 27.7 | 28.8 |
| Headless client `remoteExec` round trip (ms) | 357 | 280 | 296 |
| `callExtension` FFI, no I/O (µs/call) | 0.50 | 0.50 | 0.50 |
| **Daemon round trip, connection per call** — p50 (ms) | 1.34 | 1.62 | 1.61 |
| … min / p95 / max (ms) | 0.90 / 2.15 / 2.89 | 1.17 / 3.18 / 4.83 | 1.15 / 2.42 / 2.54 |
| **Daemon round trip, kept-alive connection** — p50 (ms) | **0.51** | 0.65 | 0.45 |
| … min / p95 / max (ms) | 0.27 / 0.77 / 1.98 | 0.33 / 1.06 / 1.59 | 0.24 / 0.75 / 1.58 |
| **`ExtensionCallback` end to end** — p50 (ms) | 17.0 | 13.0 | 10.0 |
| … min / max (ms) | 8.0 / 18.0 | 9.0 / 20.0 | 9.0 / 19.0 |
| Server peak RSS (MiB) | 1117 | 1116 | 1117 |
| Headless client peak RSS (MiB) | 897 | 897 | 897 |
| Server FPS (`-limitFPS=100`) | 97.6 | 99.4 | 100.6 |

Reading these:

- **`callExtension` itself is free.** 0.50 µs per call means the engine boundary never needs
  budgeting. Design against the daemon's cost, not the FFI's.
- **Keep the connection.** Reusing one TCP connection is roughly 3× faster than reconnecting per
  call (0.45–0.65 ms vs 1.34–1.62 ms p50). The shim should hold a persistent connection and
  reconnect on failure, as the spike shim already does.
- **The callback path costs a frame, not a millisecond.** 10–17 ms p50 against a sub-millisecond
  transport: the engine drains `ExtensionCallback` on its own cadence, so latency is frame-bound
  and independent of daemon speed. Fine for Commander decisions on a 60 s income tick; do not put
  anything per-frame behind it.
- **Memory is not a constraint.** Server plus headless client peak at ~2 GiB of the VM's 11 GiB.

Payload integrity was checked verbatim, not just by timing: a JSON object sent from SQF came back
through the daemon with its fields intact, including the daemon's own receive/send timestamps.

### Findings the research reports did not have

1. **`arma-rs` needs `.allow_no_args()`.** Without it, `"cti_shim" callExtension "ping"` returns
   an empty string and only the array form reaches a handler. Silent, and it cost time.
2. **The Linux server appends `_x64` exactly as Windows does.** SQF says `"cti_shim"`; the engine
   opens `cti_shim_x64.so`. A file named `cti_shim.so` is never found, and
   `"cti_shim_x64" callExtension` fails because the engine appends a second `_x64`.
3. **HEMTT cannot pack missions.** `hemtt utils pbo` only extracts, inspects and unpacks. Missions
   need their own packer, so `tools/pack_pbo.py` was written; output is verified by
   `hemtt utils pbo inspect`, which validates the SHA-1 trailer.
4. **Missions must be PBOs, not folders,** for the Arma tier. An unpacked mission folder cannot be
   transmitted to a joining client.
5. **`banned_commands` works as the SQF contract's enforcement.** A deliberate bare `random`
   produced `error[L-S09]: 'random' is banned by the project` and exit 1. CLAUDE.md's "never
   introduce a bare `random` or `sleep`" rule is now mechanical, and needs no grep lint.
6. **SQF-VM's exit-code trap is real, exactly as researched.** A runtime type error exits **0**;
   only parse errors exit 255. Any future SQF-VM tier must parse stdout and synthesise its own
   verdict.

## 5. Solved — the headless client slot needs a name

**Symptom.** The headless client connected and was accepted — the server logged
`Player headlessclient connected (id=HC<pid>)` — and then stopped. It never loaded the mission,
`init.sqf` never ran on it, `onPlayerConnected` never fired for it, and `entities
"HeadlessClient_F"` and `allPlayers` both stayed empty server-side.

**Cause.** The `HeadlessClient_F` entity in `mission.sqm` had no `name` property. An unnamed
headless-client slot is never assigned, so the client waits in role selection indefinitely. The
wiki states it plainly — *"Don't forget to set NAME property, it is necessary for the Headless
Client to work correctly"* — and KP Liberation, A3Wasteland, ALiVE and Apex Framework all set one.

**Placement matters as much as presence.** At item level, as KP Liberation has it. An attempt with
the name inside `class Attributes` failed identically to having no name at all, which is worth
recording: "I set the name" was not sufficient to conclude the hypothesis had been tested.

```cpp
class Item1
{
    dataType="Logic";
    class PositionInfo { position[]={1710.5,5.5,5660.5}; };
    name="HC1";            // <-- the entire fix
    isPlayable=1;
    id=3;
    type="HeadlessClient_F";
};
```

**Result**, five consecutive runs: headless client in the mission at 27.6-28.8 s, and a
server-issued `remoteExec` round trip of 227-357 ms.

### What the long diagnosis cost, and why

Eleven configuration hypotheses were tested and eliminated before the cause was found: mission as
folder vs PBO, `-filePatching`, BattlEye, adding the slot at all, port collision with the human's
Arma session, server password, the `headlessClients[]` whitelist, `disabledAI`, loopback vs LAN
IP, `-autoInit` ordering, `briefing = 0`, and `steam_appid.txt`. Two more were tried after the
research landed and also made no difference on their own: `joinUnassigned = 0` (reverted — the
engine default of 1 works fine, so it was irrelevant) and `skipLobby`, never needed.

The methodological lesson is that a full day of local experiment did not find a fact that was
written in the first-party documentation. The wiki was unreachable from this environment for most
of the spike — Cloudflare 403s direct fetches, the MediaWiki API and browser-like requests alike —
so the source that held the answer was the one source not consulted. That is why
`docs/reference/arma-wiki/` now exists.

### Two hypotheses formally closed

- **Steam identity was a red herring.** The engine substitutes `HC<pid>` for a headless client's
  UID, which our own logs showed all along. This closes the open question in
  `docs/research/linux-server-steamcmd.md` §3.1: **a headless client needs no Steam session.**
- **The named pipe cannot do this.** `-command=<name>` opens `\\.\pipe\<name>` on a client with
  exactly five commands — `shutdown`, `message`, `reply`, `session`, `connect`. It can drive a
  client to a server and shut it down, which is useful for play-session automation, but it has
  nothing for role selection.

### Also established

`getUserInfo` does **not** exist in the `server.cfg` scripting VM on 2.20. An unknown command
there fails the whole expression, leaving only an `Error in expression` line. `onUserConnected`
itself works and receives an array.

## 6. Open — Windows client desync

Once in the mission the Windows client could not move and reported "No message received after
10/15/20/25… seconds" while remaining connected. The server was unaffected throughout: 99 fps, and
not one bandwidth, packet, timeout or disconnect message in its log.

Untested candidates, in the order worth trying:

1. **`localClient[]` includes the human's address.** The spike config lists `127.0.0.1`,
   `localhost` and the LAN IP, which grants unrestricted bandwidth. That setting is meant for a
   headless client on the same machine, not for a real player arriving across the mirrored-network
   boundary.
2. **`spike/basic.cfg` is hand-written and unvalidated.** `MaxMsgSend = 256`,
   `MaxSizeGuaranteed = 512`, `MaxSizeNonguaranteed = 256`. Testing with no `-cfg=` at all, on
   engine defaults, is the cheap discriminator.
3. **Mirrored-mode UDP may not sustain bidirectional game traffic.** Prior research verified single
   datagrams crossing in each direction; a continuous simulation stream is a different claim. If
   this is the cause it is the one genuinely serious finding against ADR-0006, so it must be
   distinguished from 1 and 2 before drawing any conclusion.

Not reproduced under automation yet, because reaching the in-mission state needs the manual slot
click of §5.

## 7. Constraints this spike adds

1. **The test tier must not use ports 2302–2306.** Under `networkingMode=mirrored` the Windows
   host and the WSL2 VM share a port space, so a spike server on 2302 collides with the Windows
   Arma client. Observed live: a headless client of ours appeared in the human's Windows Arma
   session. The tier is fixed at 2402–2406 (Bohemia wants ≥100 between server port sets).
2. **Steam allows one session per account.** The dedicated server account and the human's
   playing account must stay distinct. The Windows client does *not* need closing for a join test,
   since the accounts differ and the port blocks no longer overlap.
3. **Missions ship as PBOs.** `just build-missions` packs `missions/*/` via `tools/pack_pbo.py`.
4. **`-profiles=` is broken on Linux.** Profiles must live in `~/.local/share/Arma 3` and
   `~/.local/share/Arma 3 - Other Profiles`; the harness creates both.
5. **The Windows client is drivable from WSL2.** `powershell.exe Start-Process` over the interop
   boundary launches `arma3_x64.exe` with `-connect`/`-port`/`-password`, and its RPT is readable
   at `/mnt/c/Users/<user>/Documents/Arma 3/`. Useful for play-session automation and perceptual
   checks; it does not solve §5, because the client still stops at role selection.

## 8. Artefacts

| Path | What |
|---|---|
| `justfile` | `check`, `unit`, `build`, `spike`, `fast` |
| `.hemtt/project.toml`, `addons/main/` | Script-only HEMTT addon skeleton, `banned_commands` configured |
| `extension/` | `arma-rs` 1.12.1 shim: `ping`, `addr`, `rpc`, `rpc_keepalive`, `rpc_async`, `bench`; 5 tests |
| `src/cti_daemon/stub.py` | Throwaway newline-delimited-JSON echo daemon; replaced in Phase 1 |
| `tools/pack_pbo.py` | Mission PBO packer, 7 tests |
| `missions/spike.Stratis/` | Trivial Stratis mission and the in-mission measurement harness |
| `spike/run.sh`, `spike/server.cfg`, `spike/basic.cfg` | Bring-up harness, typed failure classes |

The harness reports CLAUDE.md failure classes rather than a bare non-zero exit, and this was
exercised for real: a stale stub daemon holding port 9099 produced
`verdict=FAIL failure_class=infra_unavailable`, correctly refusing to present itself as a result.
