// source: https://community.bistudio.com/wiki/isDedicated
// revision: 337566 (2023-02-10T01:14:00Z)
// export sha1: sdid4kgylqoairpk5u8rqv4nkm0gk0c
// categories: Category:Arma 2: New Scripting Commands | Category:Arma 2: Operation Arrowhead: Scripting Commands | Category:Arma 2: Scripting Commands | Category:Arma 3: Scripting Commands | Category:Command Group: Multiplayer | Category:Introduced with Arma 2 version 1.00 | Category:Maintenance/RV | Category:Scripting Commands | Category:Take On Helicopters: Scripting Commands
// Vendored snapshot. Do not edit — re-export instead.

{{RV|type=command

|game1= arma2
|version1= 1.00

|game2= arma2oa
|version2= 1.50

|game3= tkoh
|version3= 1.00

|game4= arma3
|version4= 0.50

|gr1= Multiplayer

|descr= Returns [[true]] if the machine executing the command is a dedicated multiplayer server. Returns [[false]] in single player.

|s1= [[isDedicated]] 

|r1= [[Boolean]]

|x1= <sqf>if (isDedicated) then { diag_log "Dedicated Server on the run !"; };</sqf>

|seealso= [[isServer]] [[hasInterface]]
}}

{{Note
|user= Killzone_Kid
|timestamp= 20141221145400
|text= <sqf>
if (isDedicated) then {
	// run on dedicated server only
};

if (isServer) then {
	// run on dedicated server or player host
};

if (hasInterface) then {
	// run on all player clients incl. player host
};

if (!isDedicated) then {
	// run on all player clients incl. player host and headless clients
};

if (!isServer) then {
	// run on all player clients incl. headless clients but not player host
};

if (!hasInterface) then {
	// run on headless clients and dedicated server
};

if (!hasInterface && !isDedicated) then {
	// run on headless clients only
};
</sqf>
}}
